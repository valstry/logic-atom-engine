/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
if you want to view the source, please visit the github repository of this plugin
*/

var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/main.ts
var main_exports = {};
__export(main_exports, {
  default: () => ThreeLibraryPlugin
});
module.exports = __toCommonJS(main_exports);
var import_obsidian6 = require("obsidian");

// src/settings.ts
var import_obsidian = require("obsidian");

// src/workflows.ts
var BUILTIN_WORKFLOW_PRESETS = {
  general: {
    id: "general",
    strategyMode: "general",
    label: "\u81EA\u7531\u7F16\u6392",
    shortLabel: "\u81EA\u7531",
    description: "\u901A\u7528\u5165\u53E3\u3002\u8BA9\u5F15\u64CE\u81EA\u52A8\u6311\u9009\u539F\u5B50\uFF0C\u5148\u63A2\u7D22\u6700\u5408\u9002\u7684\u5904\u7406\u94FE\u8DEF\u3002",
    actionLabel: "\u81EA\u52A8\u9009\u62E9\u5E76\u6267\u884C",
    atoms: null,
    inputBinding: "previous",
    inputPlaceholder: "\u8F93\u5165\u4F60\u7684\u95EE\u9898\uFF0C\u6216\u7C98\u8D34\u4EFB\u610F\u6587\u672C/\u4EE3\u7801\u3002\n\n\u793A\u4F8B\uFF1A\n- \u770B\u61C2\u8FD9\u6BB5\u4E13\u4E1A\u5185\u5BB9\n- \u628A\u8FD9\u6BB5\u4FE1\u606F\u538B\u6210\u8BB0\u5FC6\u70B9\n- \u7ED9\u6211\u4E0B\u4E00\u6B65\u53EF\u6267\u884C\u52A8\u4F5C",
    stageContract: "\u901A\u7528\u65B9\u6CD5\u6267\u884C\uFF1A\u4F18\u5148\u7ED3\u6784\u5316\uFF0C\u518D\u7ED9\u51FA\u6700\u5C0F\u53EF\u6267\u884C\u7ED3\u8BBA\u3002"
  },
  "reading-understand": {
    id: "reading-understand",
    strategyMode: "reading-understand",
    label: "\u7ED3\u6784\u62C6\u8BFB",
    shortLabel: "\u62C6\u8BFB",
    description: "\u5148\u628A\u539F\u6587 A \u62C6\u6210\u77E5\u8BC6\u63A5\u53E3\u5019\u9009\uFF0C\u518D\u8BC6\u522B\u9690\u542B\u524D\u63D0\uFF0C\u4E0D\u505A\u9010\u53E5\u590D\u8FF0\u3002",
    actionLabel: "\u6309\u7ED3\u6784\u62C6\u8BFB\u6267\u884C",
    atoms: ["decompose", "abstract"],
    inputBinding: "original",
    inputPlaceholder: "\u7C98\u8D34\u4E00\u6BB5\u6587\u7AE0\u3001\u4EE3\u7801\u6216\u6750\u6599\u3002\n\n\u8F93\u51FA\u91CD\u70B9\uFF1A\n- \u539F\u6587\u9AA8\u67B6\n- \u63A5\u53E3\u5019\u9009\n- \u9690\u542B\u524D\u63D0\n- \u6700\u77ED\u7406\u89E3\u8DEF\u5F84",
    objective: "\u628A\u539F\u6587 A \u5148\u62C6\u6210\u77E5\u8BC6\u63A5\u53E3\u5019\u9009\uFF1A\u5BF9\u8C61\u3001\u5B9A\u4E49\u3001\u7A0B\u5EA6\u8BCD\u3001\u5173\u7CFB\u8BCD\u3001\u52A8\u4F5C\u8BCD\u4E0E\u9690\u542B\u524D\u63D0\uFF0C\u4E3A\u540E\u7EED\u751F\u6210 A' \u505A\u51C6\u5907\u3002",
    synthesisGuidance: "\u8F93\u51FA\u65F6\u4F18\u5148\u7ED9\u51FA\u63A5\u53E3\u5019\u9009\uFF0C\u800C\u4E0D\u662F\u6CDB\u6CDB\u6458\u8981\u3002\u5EFA\u8BAE\u7ED3\u6784\uFF1A1) \u539F\u6587\u9AA8\u67B6 2) \u63A5\u53E3\u5019\u9009 3) \u6BCF\u4E2A\u63A5\u53E3\u4EE3\u8868\u4EC0\u4E48 4) \u5FC5\u987B\u8865\u7684\u524D\u63D0\u3002",
    stageContract: "\u5FC5\u987B\u663E\u5F0F\u63D0\u53D6\u63A5\u53E3\u5019\u9009\u548C\u9690\u542B\u524D\u63D0\uFF0C\u4E0D\u505A\u9010\u53E5\u590D\u8FF0\u3002"
  },
  "reading-connect": {
    id: "reading-connect",
    strategyMode: "reading-connect",
    label: "\u77E5\u8BC6\u5DEE\u5206",
    shortLabel: "\u5DEE\u5206",
    description: "\u5BF9\u7167\u539F\u6587 A \u548C\u5F53\u524D\u7B14\u8BB0\u96CF\u5F62 A'\uFF0C\u8BC6\u522B\u7F3A\u53E3\u3001\u8BEF\u5224\u548C\u51B2\u7A81\u3002",
    actionLabel: "\u6309\u5DEE\u5206\u6D41\u7A0B\u6267\u884C",
    atoms: ["decompose", "associate", "abstract"],
    inputBinding: "original+previous",
    inputPlaceholder: "\u7C98\u8D34\u5F53\u524D\u5185\u5BB9\u3002\n\n\u8F93\u51FA\u91CD\u70B9\uFF1A\n- \u5F53\u524D\u5361\u70B9\n- \u5DF2\u6709\u63A5\u53E3\n- \u7F3A\u5931\u63A5\u53E3\n- \u51B2\u7A81/\u8BEF\u5224\n- \u6700\u5C0F\u8865\u9F50\u8DEF\u5F84",
    objective: "\u5BF9\u7167\u539F\u6587 A \u548C\u5F53\u524D\u7B14\u8BB0\u96CF\u5F62 A'\uFF0C\u627E\u51FA\u63A5\u53E3\u4E0D\u51C6\u786E\u3001\u4E0D\u5B8C\u6574\u3001\u6216\u7F3A\u5C11\u524D\u7F6E\u77E5\u8BC6\u7684\u5730\u65B9\u3002",
    synthesisGuidance: "\u8F93\u51FA\u65F6\u56F4\u7ED5 A \u548C A' \u7684\u5DEE\u5F02\u5C55\u5F00\uFF1A1) \u5F53\u524D\u5361\u70B9 2) \u5DF2\u6709\u63A5\u53E3 3) \u7F3A\u5931\u63A5\u53E3 4) \u51B2\u7A81/\u8BEF\u5224 5) \u6700\u5C0F\u8865\u9F50\u8DEF\u5F84\u3002",
    stageContract: "\u5FC5\u987B\u540C\u65F6\u53C2\u8003\u539F\u6587\u548C\u4E0A\u4E00\u9636\u6BB5\u7ED3\u679C\uFF0C\u8F93\u51FA A \u4E0E A' \u7684\u5DEE\u5F02\uFF0C\u800C\u4E0D\u662F\u5355\u72EC\u6458\u8981\u4EFB\u610F\u4E00\u8FB9\u3002"
  },
  "reading-apply": {
    id: "reading-apply",
    strategyMode: "reading-apply",
    label: "\u8BB0\u5FC6\u538B\u7F29",
    shortLabel: "\u538B\u7F29",
    description: "\u628A\u5F53\u524D\u7406\u89E3\u538B\u6210\u53EF\u8C03\u7528\u7684 A' \u63A5\u53E3\u96C6\uFF0C\u9010\u6B65\u903C\u8FD1\u53E3\u8BC0\u6216\u5173\u952E\u8BCD\u4E32\u3002",
    actionLabel: "\u6309\u538B\u7F29\u6D41\u7A0B\u6267\u884C",
    atoms: ["decompose", "abstract", "evaluate"],
    inputBinding: "original+previous",
    inputPlaceholder: "\u7C98\u8D34\u4F60\u8981\u538B\u7F29\u7684\u5185\u5BB9\u3002\n\n\u8F93\u51FA\u91CD\u70B9\uFF1A\n- A' \u63A5\u53E3\u96C6\n- \u6BCF\u4E2A\u63A5\u53E3\u4EE3\u8868\u4EC0\u4E48\n- \u6700\u77ED\u53E3\u8BC0/\u5173\u952E\u8BCD\u4E32\n- \u4E0B\u4E00\u6B65\u6700\u5C0F\u52A8\u4F5C",
    objective: "\u628A\u5F53\u524D\u7406\u89E3\u538B\u7F29\u6210\u6700\u7EC8\u53EF\u8C03\u7528\u7684 A' \u63A5\u53E3\u96C6\uFF0C\u8981\u6C42\u540C\u65F6\u6EE1\u8DB3\u7B80\u6D01\u3001\u7CBE\u51C6\u3001\u76F8\u5BF9\u5168\u9762\uFF0C\u5E76\u9010\u6B65\u903C\u8FD1\u53E3\u8BC0\u5316\u8868\u8FBE\u3002",
    synthesisGuidance: "\u8F93\u51FA\u65F6\u4F18\u5148\u7ED9\u51FA\uFF1A1) A' \u63A5\u53E3\u96C6 2) \u6BCF\u4E2A\u63A5\u53E3\u7684\u5C55\u5F00\u8BF4\u660E 3) \u6700\u77ED\u53E3\u8BC0/\u5173\u952E\u8BCD\u4E32 4) \u4E0B\u4E00\u6B65\u6700\u5C0F\u52A8\u4F5C\u3002\u907F\u514D\u957F\u6458\u8981\u3002",
    stageContract: "\u5FC5\u987B\u4ECE\u201C\u539F\u6587 A + \u5F53\u524D A'\u201D\u505A\u5BF9\u7167\u538B\u7F29\uFF0C\u6307\u51FA\u54EA\u4E9B\u4FDD\u7559\u3001\u54EA\u4E9B\u5220\u53BB\u3001\u54EA\u4E9B\u4ECD\u9700\u6821\u6B63\u3002"
  },
  "reading-advanced": {
    id: "reading-advanced",
    strategyMode: "reading-advanced",
    label: "\u4E00\u6B65\u8FDB\u9636",
    shortLabel: "\u8FDB\u9636",
    description: "\u57FA\u4E8E\u5DF2\u7ECF\u6210\u578B\u7684 A' \u63A5\u53E3\u96C6\u505A\u518D\u5224\u65AD\u4E0E\u5EF6\u4F38\uFF0C\u4E0D\u505A\u6CDB\u5316\u53D1\u6563\u3002",
    actionLabel: "\u6309\u8FDB\u9636\u6D41\u7A0B\u6267\u884C",
    atoms: ["associate", "evaluate", "abstract"],
    inputBinding: "previous",
    inputPlaceholder: "\u7C98\u8D34\u538B\u7F29\u540E\u7684\u63A5\u53E3\u96C6\u6216\u53E3\u8BC0\u3002\n\n\u8F93\u51FA\u91CD\u70B9\uFF1A\n- \u5EF6\u4F38\u95EE\u9898\n- \u4E00\u6B65\u63A8\u65AD\n- \u63A8\u65AD\u4F9D\u636E\u6765\u81EA\u54EA\u4E2A\u63A5\u53E3\n- \u4E0B\u4E00\u6B65\u7EC3\u4E60",
    objective: "\u57FA\u4E8E\u5DF2\u7ECF\u6210\u578B\u7684 A' \u63A5\u53E3\u96C6\u505A\u518D\u5224\u65AD\u4E0E\u5EF6\u4F38\uFF0C\u4E0D\u6CDB\u6CDB\u53D1\u6563\uFF0C\u4F18\u5148\u5173\u6CE8\u4F5C\u8005\u7ACB\u573A\u3001\u80CC\u666F\u5047\u8BBE\u3001\u9057\u6F0F\u5B9A\u4E49\u548C\u5199\u4F5C\u610F\u56FE\u3002",
    synthesisGuidance: "\u8F93\u51FA\u65F6\u4F18\u5148\u7ED9\u51FA\uFF1A1) \u5EF6\u4F38\u95EE\u9898 2) \u4E00\u6B65\u63A8\u65AD 3) \u63A8\u65AD\u4F9D\u636E\u6765\u81EA\u54EA\u4E2A\u63A5\u53E3 4) \u4E0B\u4E00\u6B65\u7EC3\u4E60\u3002\u907F\u514D\u65E0\u6839\u636E\u53D1\u6563\u3002",
    stageContract: "\u95EE\u9898\u5FC5\u987B\u4ECE A' \u63A5\u53E3\u96C6\u76F4\u63A5\u5EF6\u4F38\uFF0C\u4E0D\u80FD\u8131\u79BB\u63A5\u53E3\u96C6\u5408\u968F\u610F\u53D1\u6563\u3002"
  }
};
function cloneAtoms(atoms) {
  if (!atoms)
    return null;
  return [...atoms];
}
function cloneWorkflowPreset(preset) {
  return {
    ...preset,
    atoms: cloneAtoms(preset.atoms)
  };
}
function getBuiltInWorkflowPreset(mode) {
  return cloneWorkflowPreset(BUILTIN_WORKFLOW_PRESETS[mode]);
}
function listBuiltInWorkflowPresets() {
  const order = [
    "general",
    "reading-understand",
    "reading-connect",
    "reading-apply",
    "reading-advanced"
  ];
  return order.map(getBuiltInWorkflowPreset);
}
function buildWorkspaceWorkflowFromMode(mode, override = {}) {
  const base = getBuiltInWorkflowPreset(mode);
  return {
    ...base,
    ...override,
    id: override.id || base.id,
    strategyMode: override.strategyMode || base.strategyMode,
    atoms: override.atoms !== void 0 ? cloneAtoms(override.atoms) : cloneAtoms(base.atoms)
  };
}

// src/methods/step-resolver.ts
function resolveStepToAtoms(intent, ctx) {
  switch (intent) {
    case "extract_hidden_assumptions":
      return [
        { atom: "decompose", reason: "\u62C6\u51FA\u5173\u952E\u8BCD\u3001\u5B9A\u4E49\u548C\u9690\u542B\u524D\u63D0" },
        ...ctx.gapScore > 0.55 ? [{ atom: "associate", reason: "\u8865\u5145\u524D\u63D0\u5173\u7CFB\u94FE\u8DEF" }] : []
      ];
    case "diagnose_knowledge_gap":
      return [
        { atom: "associate", reason: "\u5224\u65AD\u5DF2\u6709/\u7F3A\u5931/\u51B2\u7A81" },
        { atom: "evaluate", reason: "\u5BF9\u5361\u70B9\u8FDB\u884C\u4F18\u5148\u7EA7\u6392\u5E8F" }
      ];
    case "build_prereq_patch":
      return [
        { atom: "abstract", reason: "\u8F93\u51FA\u524D\u7F6E\u77E5\u8BC6\u8865\u4E01\u7ED3\u6784" },
        ...ctx.difficulty > 0.6 ? [{ atom: "transform", reason: "\u751F\u6210\u4F4E\u95E8\u69DB\u89E3\u91CA\u7248\u672C" }] : []
      ];
    case "inject_and_rewrite":
      return [
        { atom: "transform", reason: "\u5C06\u8865\u4E01\u6CE8\u5165\u539F\u6587\u5E76\u751F\u6210\u7B2C\u4E00\u7248 A'" },
        { atom: "abstract", reason: "\u6574\u7406\u6210\u53EF\u8C03\u7528\u7684\u7ED3\u6784\u5316\u7B14\u8BB0" }
      ];
    case "compress_notes":
      return [
        { atom: "decompose", reason: "\u62BD\u53D6\u8BB0\u5FC6\u5355\u4F4D" },
        { atom: "abstract", reason: "\u538B\u7F29\u6210\u77ED\u7B14\u8BB0\u63A5\u53E3\u7ED3\u6784" },
        ...ctx.wantsAction ? [{ atom: "evaluate", reason: "\u9009\u51FA\u6700\u5C0F\u53EF\u6267\u884C\u52A8\u4F5C" }] : []
      ];
    case "one_step_advance":
      return [
        { atom: "associate", reason: "\u751F\u6210\u5EF6\u4F38\u95EE\u9898\u4E0E\u4E00\u6B65\u63A8\u65AD" },
        { atom: "evaluate", reason: "\u7ED9\u51FA\u57FA\u7840/\u8FDB\u9636\u4E0B\u4E00\u6B65" }
      ];
    case "iterate_to_readable":
      return [
        { atom: "iterate", reason: `\u7B2C ${ctx.iteration + 1} \u8F6E\u63A5\u53E3\u6821\u6B63\u8FED\u4EE3` }
      ];
    default:
      return [{ atom: "decompose", reason: "\u9ED8\u8BA4\u62C6\u89E3\u8D77\u6B65" }];
  }
}

// src/settings.ts
function cloneWorkflows(workflows) {
  if (!Array.isArray(workflows))
    return [];
  return workflows.map((workflow) => cloneWorkflowPreset(workflow));
}
function cloneWorkspaceProfile(profile) {
  var _a;
  const workflows = cloneWorkflows(profile.workflows);
  const defaultWorkflowId = profile.defaultWorkflowId || ((_a = workflows[0]) == null ? void 0 : _a.id) || "general";
  return {
    id: profile.id,
    name: profile.name,
    description: profile.description,
    panelTitle: profile.panelTitle,
    methodObjective: profile.methodObjective || "",
    outputStyleGuide: profile.outputStyleGuide || "",
    workflows,
    defaultWorkflowId
  };
}
function resolveIntentAtoms(intent, ctx) {
  const calls = resolveStepToAtoms(intent, ctx);
  const deduped = [];
  for (const call of calls) {
    if (!deduped.includes(call.atom)) {
      deduped.push(call.atom);
    }
  }
  return deduped.length > 0 ? deduped : ["decompose"];
}
function buildKnowledgeBarrierWorkflowChain() {
  const baseCtx = {
    difficulty: 0.75,
    gapScore: 0.72,
    wantsAction: true,
    iteration: 0
  };
  const withMode = (mode, override) => buildWorkspaceWorkflowFromMode(mode, override);
  return [
    withMode("reading-understand", {
      id: "kb-assumption-extract",
      label: "\u63A5\u53E3\u62C6\u6750",
      shortLabel: "\u62C6\u6750",
      description: "\u4ECE\u539F\u6587 A \u62BD\u53D6\u5BF9\u8C61\u3001\u5B9A\u4E49\u3001\u7A0B\u5EA6\u8BCD\u3001\u5173\u7CFB\u8BCD\u3001\u52A8\u4F5C\u8BCD\u4E0E\u9690\u542B\u524D\u63D0\uFF0C\u5F62\u6210\u63A5\u53E3\u5019\u9009\u3002",
      actionLabel: "\u6267\u884C\u63A5\u53E3\u62C6\u6750",
      atoms: resolveIntentAtoms("extract_hidden_assumptions", baseCtx),
      inputBinding: "original",
      objective: "\u628A\u539F\u6587 A \u5148\u62C6\u6210\u540E\u7EED\u53EF\u8C03\u7528\u7684\u63A5\u53E3\u5019\u9009\uFF0C\u4E3A\u751F\u6210 A' \u505A\u51C6\u5907\u3002",
      synthesisGuidance: "\u8F93\u51FA\u5EFA\u8BAE\uFF1A1) \u539F\u6587\u9AA8\u67B6 2) \u63A5\u53E3\u5019\u9009 3) \u6BCF\u4E2A\u63A5\u53E3\u4EE3\u8868\u4EC0\u4E48 4) \u9690\u542B\u524D\u63D0\u4E0E\u6700\u5C0F\u4F8B\u5B50\u3002",
      stageContract: "\u5FC5\u987B\u663E\u5F0F\u5217\u51FA\u63A5\u53E3\u5019\u9009\u4E0E\u9690\u542B\u524D\u63D0\uFF0C\u4E0D\u5141\u8BB8\u53EA\u505A\u6458\u8981\u3002",
      inputPlaceholder: "\u7C98\u8D34\u4E13\u4E1A\u6587\u672C\u6216\u4EE3\u7801\u7247\u6BB5\uFF0C\u5148\u505A\u201C\u63A5\u53E3\u62C6\u6750\u201D\u3002"
    }),
    withMode("reading-connect", {
      id: "kb-gap-diagnosis",
      label: "\u5DEE\u5206\u8BCA\u65AD",
      shortLabel: "\u5DEE\u5206",
      description: "\u5BF9\u7167\u539F\u6587 A \u548C\u4E0A\u4E00\u6B65\u5F62\u6210\u7684 A' \u96CF\u5F62\uFF0C\u8F93\u51FA\u5DF2\u6709\u3001\u7F3A\u5931\u4E0E\u51B2\u7A81\u3002",
      actionLabel: "\u6267\u884C\u5DEE\u5206\u8BCA\u65AD",
      atoms: resolveIntentAtoms("diagnose_knowledge_gap", baseCtx),
      inputBinding: "original+previous",
      objective: "\u628A\u201C\u770B\u4E0D\u61C2\u201D\u7684\u539F\u56E0\u5B9A\u4F4D\u4E3A\u5177\u4F53\u7F3A\u53E3\uFF0C\u5224\u65AD\u5F53\u524D A' \u54EA\u4E9B\u5730\u65B9\u4E0D\u51C6\u3001\u4E0D\u5168\u3001\u6216\u7F3A\u5C11\u524D\u7F6E\u77E5\u8BC6\u3002",
      synthesisGuidance: "\u8F93\u51FA\u5EFA\u8BAE\uFF1A1) \u5F53\u524D\u5361\u70B9 2) \u5DF2\u6709\u63A5\u53E3 3) \u7F3A\u5931\u63A5\u53E3 4) \u51B2\u7A81/\u8BEF\u5224 5) \u4E09\u6B65\u8865\u9F50\u987A\u5E8F\u3002",
      stageContract: "\u5FC5\u987B\u505A A \u4E0E A' \u7684\u5DEE\u5206\uFF0C\u4E0D\u5141\u8BB8\u53EA\u7ED9\u7EDF\u79F0\u7ED3\u8BBA\u3002",
      inputPlaceholder: "\u7CFB\u7EDF\u4F1A\u81EA\u52A8\u4F7F\u7528\u539F\u6587 A \u4E0E\u4E0A\u4E00\u6B65\u7ED3\u679C\u505A\u5DEE\u5206\u3002"
    }),
    withMode("reading-connect", {
      id: "kb-prereq-patch",
      label: "\u524D\u7F6E\u8865\u4E01",
      shortLabel: "\u8865\u4E01",
      description: "\u751F\u6210\u524D\u7F6E\u77E5\u8BC6\u8865\u4E01\uFF0C\u628A\u5FC5\u9700\u57FA\u7840\u91CD\u65B0\u8865\u56DE\u7406\u89E3\u8DEF\u5F84\u3002",
      actionLabel: "\u6267\u884C\u524D\u7F6E\u8865\u4E01",
      atoms: resolveIntentAtoms("build_prereq_patch", { ...baseCtx, difficulty: 0.85 }),
      inputBinding: "original+previous",
      objective: "\u628A\u7F3A\u5931\u5E38\u8BC6\u62C6\u6210\u53EF\u8865\u7684\u5C0F\u5757\uFF0C\u91CD\u65B0\u63A5\u56DE\u5F53\u524D A \u5230 A' \u7684\u6784\u9020\u8DEF\u5F84\u3002",
      synthesisGuidance: "\u8F93\u51FA\u5EFA\u8BAE\uFF1A\u7F3A\u5931\u524D\u7F6E\u5217\u8868 + \u6BCF\u4E2A\u524D\u7F6E\u7684\u767D\u8BDD\u89E3\u91CA + \u56DE\u770B\u539F\u6587\u65F6\u8981\u91CD\u70B9\u5BF9\u7167\u7684\u4F4D\u7F6E\u3002",
      stageContract: "\u6BCF\u4E2A\u524D\u7F6E\u8865\u4E01\u90FD\u5FC5\u987B\u8BF4\u660E\u201C\u4E3A\u4EC0\u4E48\u9700\u8981\u5B83\u201D\u4EE5\u53CA\u201C\u8865\u5B8C\u540E\u56DE\u539F\u6587\u770B\u54EA\u91CC\u201D\u3002",
      inputPlaceholder: "\u7CFB\u7EDF\u4F1A\u81EA\u52A8\u7ED3\u5408\u539F\u6587\u548C\u5DEE\u5206\u7ED3\u679C\u751F\u6210\u8865\u4E01\u3002"
    }),
    withMode("reading-understand", {
      id: "kb-rewrite",
      label: "\u751F\u6210 A'",
      shortLabel: "A'",
      description: "\u628A\u8865\u4E01\u6CE8\u5165\u539F\u6587\u8BED\u5883\uFF0C\u751F\u6210\u7B2C\u4E00\u7248\u7ED3\u6784\u5316\u7B14\u8BB0 A'\u3002",
      actionLabel: "\u6267\u884C A' \u751F\u6210",
      atoms: resolveIntentAtoms("inject_and_rewrite", baseCtx),
      inputBinding: "original+previous",
      objective: "\u5728\u4FDD\u6301\u51C6\u786E\u7684\u524D\u63D0\u4E0B\uFF0C\u628A\u539F\u6587\u91CD\u7EC4\u4E3A\u7B2C\u4E00\u7248\u7ED3\u6784\u5316\u7B14\u8BB0 A'\uFF0C\u800C\u4E0D\u662F\u7B80\u5355\u767D\u8BDD\u590D\u8FF0\u3002",
      synthesisGuidance: "\u5148\u7ED9\u63A5\u53E3\u5316\u7406\u89E3\uFF0C\u518D\u4FDD\u7559\u5FC5\u8981\u672F\u8BED\u5B9A\u4E49\uFF1B\u6BCF\u4E00\u6BB5\u6700\u597D\u7ED9\u4E00\u4E2A\u6700\u5C0F\u4F8B\u5B50\u3002",
      stageContract: "\u5FC5\u987B\u4FDD\u7559\u4E13\u4E1A\u51C6\u786E\u6027\uFF0C\u5E76\u8BA9\u6BCF\u4E2A\u5173\u952E\u672F\u8BED\u90FD\u6709\u5BF9\u5E94\u7684\u53EF\u8C03\u7528\u8BF4\u660E\u3002",
      inputPlaceholder: "\u7CFB\u7EDF\u4F1A\u81EA\u52A8\u57FA\u4E8E\u539F\u6587 A \u548C\u524D\u7F6E\u8865\u4E01\u751F\u6210\u7B2C\u4E00\u7248 A'\u3002"
    }),
    withMode("reading-apply", {
      id: "kb-compress-notes",
      label: "\u538B\u7F29 A'",
      shortLabel: "\u538B\u7F29",
      description: "\u628A\u5F53\u524D A' \u538B\u6210\u66F4\u77ED\u7684\u63A5\u53E3\u96C6\uFF0C\u903C\u8FD1\u5173\u952E\u8BCD\u4E32\u6216\u53E3\u8BC0\u3002",
      actionLabel: "\u6267\u884C A' \u538B\u7F29",
      atoms: resolveIntentAtoms("compress_notes", baseCtx),
      inputBinding: "previous",
      objective: "\u5F62\u6210\u53EF\u590D\u7528\u7684 A' \u63A5\u53E3\u96C6\uFF1A\u5BF9\u8C61\u3001\u5B9A\u4E49\u3001\u63CF\u8FF0\u3001\u5173\u7CFB\u3001\u52A8\u4F5C\uFF0C\u5E76\u5C3D\u91CF\u538B\u5230\u80FD\u76F4\u63A5\u8C03\u7528\u7684\u77ED\u53E5\u6216\u53E3\u8BC0\u3002",
      synthesisGuidance: "\u8F93\u51FA\u5EFA\u8BAE\uFF1AA' \u63A5\u53E3\u96C6 + \u6BCF\u4E2A\u63A5\u53E3\u7684\u538B\u7F29\u8868\u8FBE + \u4E00\u6B65\u63A8\u65AD + \u6700\u5C0F\u52A8\u4F5C\u3002",
      stageContract: "\u7981\u6B62\u957F\u6458\u8981\uFF0C\u5FC5\u987B\u6536\u655B\u4E3A\u77ED\u7B14\u8BB0\u7ED3\u6784\u6216\u63A5\u53E3\u4E32\u3002",
      inputPlaceholder: "\u7CFB\u7EDF\u4F1A\u81EA\u52A8\u538B\u7F29\u4E0A\u4E00\u6B65\u751F\u6210\u7684 A'\u3002"
    }),
    withMode("reading-apply", {
      id: "kb-compare-optimize",
      label: "\u6821\u6B63\u63A5\u53E3",
      shortLabel: "\u6821\u6B63",
      description: "\u56DE\u5230\u539F\u6587 A \u6821\u6B63\u5F53\u524D A'\uFF0C\u8BA9\u538B\u7F29\u540E\u7684\u63A5\u53E3\u96C6\u59CB\u7EC8\u4E0E\u539F\u6587\u4FDD\u6301\u4E00\u81F4\u3002",
      actionLabel: "\u6267\u884C\u63A5\u53E3\u6821\u6B63",
      atoms: ["iterate", "abstract", "evaluate"],
      inputBinding: "original+previous",
      objective: "\u7528\u539F\u6587 A \u6821\u6B63\u5F53\u524D A'\uFF1A\u4F18\u5148\u4FDD\u8BC1\u8BED\u4E49\u4E00\u81F4\uFF0C\u518D\u7EE7\u7EED\u4F18\u5316\u7B80\u6D01\u3001\u7CBE\u51C6\u548C\u76F8\u5BF9\u5168\u9762\uFF0C\u8F93\u51FA\u66F4\u7A33\u7684\u65B0\u7248\u672C A'\u3002",
      synthesisGuidance: "\u8F93\u51FA\u5EFA\u8BAE\uFF1A\u4FEE\u6B63\u4F9D\u636E + \u4FEE\u6B63\u540E\u7684 A' + \u5982\u6709\u5FC5\u8981\u7684\u4E0B\u4E00\u8F6E\u5173\u6CE8\u70B9\u3002\u4E0D\u8981\u628A\u91CD\u70B9\u653E\u5728\u5DEE\u5F02\u5C55\u793A\u4E0A\u3002",
      stageContract: "\u5FC5\u987B\u8F93\u51FA\u4E0E\u539F\u6587 A \u4FDD\u6301\u4E00\u81F4\u7684\u65B0\u7248\u672C A'\uFF0C\u5E76\u81F3\u5C11\u7ED9\u51FA\u4E00\u6761\u4FEE\u6B63\u4F9D\u636E\uFF1B\u5DEE\u5F02\u53EA\u80FD\u4F5C\u4E3A\u6821\u6B63\u4F9D\u636E\uFF0C\u4E0D\u80FD\u4F5C\u4E3A\u4E3B\u8F93\u51FA\u3002",
      inputPlaceholder: "\u7CFB\u7EDF\u4F1A\u81EA\u52A8\u4F7F\u7528\u539F\u6587 A + \u5F53\u524D A' \u505A\u5BF9\u7167\u4F18\u5316\u3002"
    }),
    withMode("reading-advanced", {
      id: "kb-one-step-advance",
      label: "\u4E00\u6B65\u5EF6\u4F38",
      shortLabel: "\u5EF6\u4F38",
      description: "\u57FA\u4E8E\u6700\u7EC8 A' \u63A5\u53E3\u96C6\u505A\u53D7\u7EA6\u675F\u7684\u63D0\u95EE\u4E0E\u4E00\u6B65\u63A8\u65AD\u3002",
      actionLabel: "\u6267\u884C\u4E00\u6B65\u5EF6\u4F38",
      atoms: resolveIntentAtoms("one_step_advance", baseCtx),
      inputBinding: "previous",
      objective: "\u57FA\u4E8E\u77ED\u7B14\u8BB0\u6216\u53E3\u8BC0\u505A\u9AD8\u9636\u5E94\u7528\uFF1A\u4F5C\u8005\u7ACB\u573A\u3001\u80CC\u666F\u5047\u8BBE\u3001\u9057\u6F0F\u5B9A\u4E49\u3001\u5199\u4F5C\u610F\u56FE\uFF0C\u4F46\u53EA\u524D\u8FDB\u4E00\u6B65\u3002",
      synthesisGuidance: "\u8F93\u51FA\u5EFA\u8BAE\uFF1A2-3 \u4E2A\u5EF6\u4F38\u95EE\u9898\u3001\u4E00\u6B65\u63A8\u65AD\u3001\u63A8\u65AD\u4F9D\u636E\u3001\u4E0B\u4E00\u6B65\u7EC3\u4E60\u3002",
      stageContract: "\u95EE\u9898\u5FC5\u987B\u4E0E\u6700\u7EC8 A' \u63A5\u53E3\u96C6\u76F4\u63A5\u76F8\u5173\uFF0C\u7981\u6B62\u6CDB\u5316\u53D1\u6563\u3002",
      inputPlaceholder: "\u7CFB\u7EDF\u4F1A\u81EA\u52A8\u57FA\u4E8E\u201C\u63A5\u53E3\u6821\u6B63\u201D\u540E\u7684 A' \u505A\u5EF6\u4F38\u3002"
    })
  ];
}
var DEFAULT_WORKSPACE_PROFILES = [
  {
    id: "logic-atom-system",
    name: "\u903B\u8F91\u539F\u5B50\u4F53\u7CFB",
    description: "\u901A\u7528\u65B9\u6CD5\u6846\u67B6\uFF1A\u540C\u4E00\u5957\u539F\u5B50\u53EF\u4EE5\u7F16\u6392\u6210\u4E0D\u540C\u5B66\u4E60\u548C\u5206\u6790\u65B9\u6CD5\u3002",
    panelTitle: "\u903B\u8F91\u539F\u5B50\u901A\u7528\u6846\u67B6",
    methodObjective: "\u5148\u7ED3\u6784\u5316\u8F93\u5165\uFF0C\u518D\u63D0\u70BC\u5173\u952E\u70B9\uFF0C\u6700\u540E\u53EA\u63A8\u8FDB\u4E00\u4E2A\u6700\u5C0F\u53EF\u6267\u884C\u7ED3\u8BBA\u3002",
    outputStyleGuide: "\u8F93\u51FA\u8981\u7ED3\u6784\u5316\u3001\u77ED\u6BB5\u843D\u3001\u53EF\u590D\u5236\u5230\u7B14\u8BB0\uFF0C\u4F18\u5148\u7ED9\u51FA\u660E\u786E\u4E0B\u4E00\u6B65\u3002",
    workflows: listBuiltInWorkflowPresets(),
    defaultWorkflowId: "general"
  },
  {
    id: "knowledge-barrier",
    name: "\u77E5\u8BC6\u58C1\u5792\u573A\u666F",
    description: "\u573A\u666F\u793A\u4F8B\uFF1A\u628A\u539F\u6587 A \u9010\u6B65\u538B\u6210\u53EF\u8C03\u7528\u7684 A' \u63A5\u53E3\u96C6\uFF0C\u518D\u57FA\u4E8E\u63A5\u53E3\u96C6\u505A\u5EF6\u4F38\u5224\u65AD\u3002",
    panelTitle: "\u77E5\u8BC6\u58C1\u5792\u62C6\u89E3",
    methodObjective: "\u4F18\u5148\u663E\u5316\u4E13\u5BB6\u9ED8\u8BA4\u77E5\u8BC6\uFF0C\u518D\u6784\u9020 A'\u3001\u6821\u6B63 A'\u3001\u538B\u7F29 A'\uFF0C\u6700\u7EC8\u5F62\u6210\u53EF\u8C03\u7528\u7684\u53E3\u8BC0\u6216\u63A5\u53E3\u4E32\u3002",
    outputStyleGuide: "\u5148\u7ED9\u63A5\u53E3\u96C6\uFF0C\u518D\u89E3\u91CA\u6BCF\u4E2A\u63A5\u53E3\u4EE3\u8868\u4EC0\u4E48\uFF1B\u4FDD\u6301\u51C6\u786E\u3001\u7B80\u6D01\u3001\u53EF\u56DE\u770B\u539F\u6587\u3002",
    workflows: buildKnowledgeBarrierWorkflowChain(),
    defaultWorkflowId: "kb-assumption-extract"
  }
];
function getDefaultWorkspaceProfiles() {
  return DEFAULT_WORKSPACE_PROFILES.map(cloneWorkspaceProfile);
}
var DEFAULT_SETTINGS = {
  apiUrl: "https://api.evermind.ai/api/v0",
  apiKey: "",
  userId: "vault_user",
  userName: "User",
  groupId: "obsidian_vault",
  groupName: "Obsidian Vault",
  defaultRetrieveMethod: "hybrid",
  openRouterApiKey: "",
  llmModel: "gpt-4o-mini",
  llmBaseUrl: "https://api.openai.com/v1",
  defaultAtoms: "decompose,evaluate",
  maxSearchResults: 10,
  workspaceProfiles: getDefaultWorkspaceProfiles(),
  activeWorkspaceId: "logic-atom-system"
};
var ThreeLibrarySettingTab = class extends import_obsidian.PluginSettingTab {
  constructor(app, plugin) {
    super(app, plugin);
    this.plugin = plugin;
  }
  display() {
    const { containerEl } = this;
    containerEl.empty();
    containerEl.createEl("h2", { text: "\u903B\u8F91\u539F\u5B50\u5F15\u64CE\u8BBE\u7F6E" });
    containerEl.createEl("h3", { text: "EverMemOS \u4E91\u7AEF API" });
    new import_obsidian.Setting(containerEl).setName("API \u5730\u5740").setDesc("EverMemOS API \u7AEF\u70B9").addText(
      (text) => text.setPlaceholder("https://api.evermind.ai/api/v0").setValue(this.plugin.settings.apiUrl).onChange(async (value) => {
        this.plugin.settings.apiUrl = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName("API \u5BC6\u94A5").setDesc("\u4F60\u7684 EverMemOS API Key").addText(
      (text) => text.setPlaceholder("\u8F93\u5165 API Key").setValue(this.plugin.settings.apiKey).onChange(async (value) => {
        this.plugin.settings.apiKey = value.trim();
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName("\u7528\u6237 ID").setDesc("\u8BB0\u5FC6\u64CD\u4F5C\u4F7F\u7528\u7684\u7528\u6237\u6807\u8BC6").addText(
      (text) => text.setPlaceholder("vault_user").setValue(this.plugin.settings.userId).onChange(async (value) => {
        this.plugin.settings.userId = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName("\u7528\u6237\u540D").setDesc("\u5B58\u50A8\u8BB0\u5FC6\u65F6\u663E\u793A\u7684\u540D\u79F0").addText(
      (text) => text.setPlaceholder("User").setValue(this.plugin.settings.userName).onChange(async (value) => {
        this.plugin.settings.userName = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName("\u5206\u7EC4 ID").setDesc("\u8BB0\u5FC6\u547D\u540D\u7A7A\u95F4\u7684\u5206\u7EC4\u6807\u8BC6").addText(
      (text) => text.setPlaceholder("obsidian_vault").setValue(this.plugin.settings.groupId).onChange(async (value) => {
        this.plugin.settings.groupId = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName("\u5206\u7EC4\u540D\u79F0").setDesc("\u5206\u7EC4\u7684\u663E\u793A\u540D\u79F0").addText(
      (text) => text.setPlaceholder("Obsidian Vault").setValue(this.plugin.settings.groupName).onChange(async (value) => {
        this.plugin.settings.groupName = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName("\u68C0\u7D22\u65B9\u5F0F").setDesc("\u9ED8\u8BA4\u7684\u8BB0\u5FC6\u641C\u7D22\u65B9\u5F0F").addDropdown(
      (d) => d.addOptions({ hybrid: "\u6DF7\u5408\u68C0\u7D22", keyword: "\u5173\u952E\u8BCD", vector: "\u5411\u91CF", rrf: "RRF \u878D\u5408" }).setValue(this.plugin.settings.defaultRetrieveMethod).onChange(async (value) => {
        this.plugin.settings.defaultRetrieveMethod = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName("\u6D4B\u8BD5\u8FDE\u63A5").setDesc("\u9A8C\u8BC1 API \u662F\u5426\u53EF\u7528").addButton(
      (btn) => btn.setButtonText("\u6D4B\u8BD5").onClick(async () => {
        btn.setButtonText("\u6D4B\u8BD5\u4E2D...");
        const result = await this.plugin.testApiConnection();
        btn.setButtonText(result.ok ? "\u8FDE\u63A5\u6210\u529F" : "\u8FDE\u63A5\u5931\u8D25");
        setTimeout(() => btn.setButtonText("\u6D4B\u8BD5"), 3e3);
      })
    );
    containerEl.createEl("h3", { text: "LLM \u914D\u7F6E" });
    new import_obsidian.Setting(containerEl).setName("LLM API Key").setDesc("\u539F\u5B50\u9009\u62E9\u548C LLM \u5224\u65AD\u6240\u9700\uFF0C\u652F\u6301\u4EFB\u610F OpenAI \u517C\u5BB9 API").addText(
      (text) => text.setPlaceholder("\u8F93\u5165 API Key").setValue(this.plugin.settings.openRouterApiKey).onChange(async (value) => {
        this.plugin.settings.openRouterApiKey = value.trim();
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName("\u6A21\u578B").setDesc("\u6A21\u578B ID\uFF0C\u4F8B\u5982 gpt-4o-mini \u6216 claude-3-haiku").addText(
      (text) => text.setPlaceholder("gpt-4o-mini").setValue(this.plugin.settings.llmModel).onChange(async (value) => {
        this.plugin.settings.llmModel = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName("API \u57FA\u7840\u5730\u5740").setDesc("OpenAI \u517C\u5BB9\u7684 API \u5730\u5740").addText(
      (text) => text.setPlaceholder("https://api.openai.com/v1").setValue(this.plugin.settings.llmBaseUrl).onChange(async (value) => {
        this.plugin.settings.llmBaseUrl = value.trim();
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName("\u6D4B\u8BD5 LLM \u8FDE\u63A5").setDesc("\u9A8C\u8BC1 LLM Key / Base URL / \u6A21\u578B\u662F\u5426\u53EF\u7528").addButton(
      (btn) => btn.setButtonText("\u6D4B\u8BD5").onClick(async () => {
        btn.setButtonText("\u6D4B\u8BD5\u4E2D...");
        const result = await this.plugin.testLlmConnection();
        btn.setButtonText(result.ok ? "\u8FDE\u63A5\u6210\u529F" : "\u8FDE\u63A5\u5931\u8D25");
        setTimeout(() => btn.setButtonText("\u6D4B\u8BD5"), 3e3);
      })
    );
    containerEl.createEl("h3", { text: "\u5F15\u64CE\u914D\u7F6E" });
    new import_obsidian.Setting(containerEl).setName("\u9ED8\u8BA4\u539F\u5B50").setDesc("\u624B\u52A8\u6A21\u5F0F\u4E0B\u7684\u9ED8\u8BA4\u539F\u5B50\u7EC4\u5408\uFF0C\u9017\u53F7\u5206\u9694\uFF0C\u4F8B\u5982 decompose,associate,transform,abstract,evaluate,iterate").addText(
      (text) => text.setPlaceholder("decompose,evaluate").setValue(this.plugin.settings.defaultAtoms).onChange(async (value) => {
        this.plugin.settings.defaultAtoms = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName("\u6700\u5927\u641C\u7D22\u7ED3\u679C\u6570").setDesc("\u6BCF\u6B21\u641C\u7D22\u8FD4\u56DE\u7684\u6700\u5927\u8BB0\u5FC6\u6570\u91CF\uFF081-20\uFF09").addSlider(
      (slider) => slider.setLimits(1, 20, 1).setValue(this.plugin.settings.maxSearchResults).setDynamicTooltip().onChange(async (value) => {
        this.plugin.settings.maxSearchResults = value;
        await this.plugin.saveSettings();
      })
    );
    containerEl.createEl("h3", { text: "\u5DE5\u4F5C\u533A" });
    new import_obsidian.Setting(containerEl).setName("\u5F53\u524D\u5DE5\u4F5C\u533A").setDesc("\u9009\u62E9\u9ED8\u8BA4\u5DE5\u4F5C\u533A\uFF0C\u9762\u677F\u4E2D\u53EF\u968F\u65F6\u5207\u6362").addDropdown((dropdown) => {
      for (const profile of this.plugin.settings.workspaceProfiles) {
        dropdown.addOption(profile.id, profile.name);
      }
      dropdown.setValue(this.plugin.settings.activeWorkspaceId).onChange(async (value) => {
        this.plugin.settings.activeWorkspaceId = value;
        await this.plugin.saveSettings();
      });
    });
    new import_obsidian.Setting(containerEl).setName("\u5DE5\u4F5C\u533A\u6570\u91CF").setDesc(`\u5F53\u524D\u5DF2\u914D\u7F6E ${this.plugin.settings.workspaceProfiles.length} \u4E2A\u5DE5\u4F5C\u533A`).addButton(
      (btn) => btn.setButtonText("\u6062\u590D\u9ED8\u8BA4\u5DE5\u4F5C\u533A").onClick(async () => {
        this.plugin.settings.workspaceProfiles = getDefaultWorkspaceProfiles();
        this.plugin.settings.activeWorkspaceId = "logic-atom-system";
        await this.plugin.saveSettings();
        this.display();
      })
    );
    const workspaceDesc = containerEl.createDiv();
    workspaceDesc.addClass("setting-item-description");
    workspaceDesc.setText(
      "\u63D2\u4EF6\u662F\u901A\u7528\u6846\u67B6\uFF1A\u540C\u4E00\u5957\u903B\u8F91\u539F\u5B50\u53EF\u4EE5\u5728\u4E0D\u540C\u5DE5\u4F5C\u533A\u7F16\u6392\u6210\u4E0D\u540C\u5DE5\u4F5C\u6D41\u3002\u4F60\u53EF\u4EE5\u5728\u9762\u677F\u4E2D\u590D\u5236\u5DE5\u4F5C\u533A\u3001\u8C03\u6574\u539F\u5B50\u94FE\u3001\u7F16\u8F91\u65B9\u6CD5\u76EE\u6807\u4E0E\u8F93\u51FA\u98CE\u683C\u3002"
    );
  }
};

// src/api/client.ts
var https = __toESM(require("https"));
var http = __toESM(require("http"));
var _EverMemOSClient = class _EverMemOSClient {
  constructor(apiUrl, apiKey, userId, userName, groupId, groupName, retrieveMethod = "hybrid") {
    this.apiUrl = apiUrl;
    this.apiKey = apiKey;
    this.userId = userId;
    this.userName = userName;
    this.groupId = groupId;
    this.groupName = groupName;
    this.retrieveMethod = retrieveMethod;
  }
  updateConfig(apiUrl, apiKey, userId, userName, groupId, groupName, retrieveMethod) {
    this.apiUrl = apiUrl;
    this.apiKey = apiKey;
    this.userId = userId;
    this.userName = userName;
    this.groupId = groupId;
    this.groupName = groupName;
    if (retrieveMethod)
      this.retrieveMethod = retrieveMethod;
  }
  get headers() {
    const h = { "Content-Type": "application/json" };
    if (this.apiKey)
      h["Authorization"] = `Bearer ${this.apiKey}`;
    return h;
  }
  normalizeSearchQuery(query, maxLength = _EverMemOSClient.MAX_SEARCH_QUERY_LENGTH) {
    const compact = (query || "").replace(/\s+/g, " ").trim();
    if (compact.length <= maxLength && encodeURIComponent(compact).length <= _EverMemOSClient.MAX_SEARCH_QUERY_ENCODED_LENGTH) {
      return compact;
    }
    const tokens = compact.split(/[，。；：.!?()\[\]{}<>"'`~|\\/:\s]+/).map((token) => token.trim()).filter((token) => token.length >= 2);
    const deduped = [];
    for (const token of tokens) {
      if (!deduped.includes(token)) {
        deduped.push(token);
      }
    }
    let reduced = "";
    for (const token of deduped) {
      const next = reduced ? `${reduced} ${token}` : token;
      if (next.length > maxLength || encodeURIComponent(next).length > _EverMemOSClient.MAX_SEARCH_QUERY_ENCODED_LENGTH) {
        break;
      }
      reduced = next;
    }
    if (reduced) {
      return reduced;
    }
    let fallback = compact.slice(0, maxLength);
    while (fallback.length > 1 && encodeURIComponent(fallback).length > _EverMemOSClient.MAX_SEARCH_QUERY_ENCODED_LENGTH) {
      fallback = fallback.slice(0, -1);
    }
    return fallback;
  }
  httpRequest(params) {
    return new Promise((resolve, reject) => {
      const parsedUrl = new URL(params.url);
      const isHttps = parsedUrl.protocol === "https:";
      const client = isHttps ? https : http;
      const bodyData = params.body || "";
      const headers = {
        "Content-Type": "application/json",
        ...params.headers
      };
      if (bodyData) {
        headers["Content-Length"] = Buffer.byteLength(bodyData).toString();
      }
      const options = {
        hostname: parsedUrl.hostname,
        port: parsedUrl.port ? parseInt(parsedUrl.port, 10) : isHttps ? 443 : 80,
        path: parsedUrl.pathname + (parsedUrl.search || ""),
        method: params.method || "GET",
        headers
      };
      console.log("[LogicAtom] Request:", {
        method: options.method,
        path: parsedUrl.pathname,
        hasBody: !!bodyData
      });
      const req = client.request(options, (res) => {
        let data = "";
        res.on("data", (chunk) => {
          data += chunk.toString();
        });
        res.on("end", () => {
          console.log("[LogicAtom] Response:", { status: res.statusCode, path: parsedUrl.pathname });
          if (res.statusCode && res.statusCode >= 400) {
            const status = res.statusCode;
            const endpoint = `${options.method} ${parsedUrl.pathname}`;
            if (status === 401 || status === 403) {
              reject(new Error(`[EverMind ${status}] \u9274\u6743\u5931\u8D25\uFF1A\u8BF7\u68C0\u67E5 API Key / user_id / group_id \u914D\u7F6E\uFF08${endpoint}\uFF09`));
              return;
            }
            if (status === 414) {
              reject(new Error(`[EverMind 414] Search query too long for ${endpoint}`));
              return;
            }
            reject(new Error(`HTTP ${status}: ${data.slice(0, 200)}`));
            return;
          }
          try {
            resolve(JSON.parse(data));
          } catch (e) {
            reject(new Error(`Invalid JSON response: ${data.slice(0, 200)}`));
          }
        });
      });
      req.on("error", (err) => {
        console.error("[LogicAtom] Request error:", err.message, err.code || "");
        reject(err);
      });
      if (bodyData)
        req.write(bodyData);
      req.end();
    });
  }
  async storeMemory(content, messageId) {
    const id = messageId || `atom_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`;
    const body = {
      message_id: id,
      create_time: (/* @__PURE__ */ new Date()).toISOString(),
      sender: this.userId,
      sender_name: this.userName,
      content,
      group_id: this.groupId,
      flush: true
    };
    const response = await this.httpRequest({
      url: `${this.apiUrl}/memories`,
      method: "POST",
      headers: this.headers,
      body: JSON.stringify(body)
    });
    return { response, eventId: id };
  }
  async deleteMemory(eventId) {
    const body = { event_id: eventId };
    console.log("[LogicAtom] Deleting memory:", eventId);
    await this.httpRequest({
      url: `${this.apiUrl}/memories`,
      method: "DELETE",
      headers: this.headers,
      body: JSON.stringify(body)
    });
    console.log("[LogicAtom] Memory deleted:", eventId);
  }
  async searchMemories(query, topK = 10, method) {
    const normalizedQuery = this.normalizeSearchQuery(query);
    const params = new URLSearchParams({
      query: normalizedQuery,
      user_id: this.userId,
      group_id: this.groupId,
      retrieve_method: method || this.retrieveMethod,
      top_k: String(topK)
    });
    const data = await this.httpRequest({
      url: `${this.apiUrl}/memories/search?${params}`,
      method: "GET",
      headers: this.headers
    });
    if (!data || !data.result)
      return [];
    return this.normalizeResults(data);
  }
  async getMemories() {
    const params = new URLSearchParams({ user_id: this.userId });
    const data = await this.httpRequest({
      url: `${this.apiUrl}/memories?${params}`,
      method: "GET",
      headers: this.headers
    });
    if (!data || !data.result)
      return [];
    return (data.result.memories || []).map((item, i) => this.itemToResult(item, i));
  }
  normalizeResults(data) {
    const results = [];
    const memories = data.result.memories || [];
    const profiles = data.result.profiles || [];
    for (let i = 0; i < profiles.length; i++) {
      const item = { ...profiles[i], memory_type: "profile" };
      results.push(this.itemToResult(item, i));
    }
    for (let i = 0; i < memories.length; i++) {
      results.push(this.itemToResult(memories[i], profiles.length + i));
    }
    return results;
  }
  itemToResult(item, index) {
    var _a;
    const content = item.content || item.summary || item.episode || item.subject || "";
    const title = item.subject || item.summary || (content.length > 60 ? `${content.slice(0, 60)}...` : content) || `Memory #${index}`;
    return {
      id: item.id || `mem_${index}`,
      title,
      content,
      type: item.memory_type === "profile" ? "profile" : "memory",
      memoryType: item.memory_type || "episodic_memory",
      score: (_a = item.score) != null ? _a : 0,
      createdAt: item.created_at || "",
      raw: item
    };
  }
  async testConnection() {
    console.log("[LogicAtom] Testing connection...", {
      apiUrl: this.apiUrl,
      userId: this.userId,
      groupId: this.groupId,
      hasKey: !!this.apiKey
    });
    try {
      const results = await this.searchMemories("test", 1);
      return { ok: true, message: `\u8FDE\u63A5\u6210\u529F\uFF0C\u627E\u5230 ${results.length} \u6761\u8BB0\u5FC6` };
    } catch (e) {
      const msg = (e == null ? void 0 : e.message) || String(e);
      const code = (e == null ? void 0 : e.code) || "";
      console.error("[LogicAtom] Connection test failed:", msg, code);
      return { ok: false, message: `\u8FDE\u63A5\u5931\u8D25\uFF1A${msg}` };
    }
  }
};
_EverMemOSClient.MAX_SEARCH_QUERY_LENGTH = 180;
_EverMemOSClient.MAX_SEARCH_QUERY_ENCODED_LENGTH = 320;
var EverMemOSClient = _EverMemOSClient;

// src/utils/llm-client.ts
var import_obsidian2 = require("obsidian");
var LLMClient = class {
  constructor(apiKey, model = "gpt-4o-mini", baseUrl = "https://api.openai.com/v1") {
    this.apiKey = (apiKey || "").trim();
    this.model = model;
    this.baseUrl = this.normalizeBaseUrl(baseUrl || "https://api.openai.com/v1");
  }
  updateConfig(apiKey, model, baseUrl) {
    this.apiKey = (apiKey || "").trim();
    if (model)
      this.model = model;
    if (baseUrl)
      this.baseUrl = this.normalizeBaseUrl(baseUrl);
  }
  normalizeBaseUrl(url) {
    return (url || "").trim().replace(/\/+$/, "");
  }
  detectKeyProvider(apiKey) {
    if (!apiKey)
      return "unknown";
    if (apiKey.startsWith("sk-or-"))
      return "openrouter";
    if (apiKey.startsWith("sk-proj-") || apiKey.startsWith("sk-"))
      return "openai";
    return "unknown";
  }
  resolveEndpointAndHeaders(apiKey) {
    const keyProvider = this.detectKeyProvider(apiKey);
    let base = this.normalizeBaseUrl(this.baseUrl || "https://api.openai.com/v1");
    const isOpenAIBase = /api\.openai\.com/i.test(base);
    const isOpenRouterBase = /openrouter\.ai/i.test(base);
    let autoFixHint;
    if (keyProvider === "openrouter" && isOpenAIBase) {
      base = "https://openrouter.ai/api/v1";
      autoFixHint = "\u68C0\u6D4B\u5230 OpenRouter Key\uFF0C\u5DF2\u81EA\u52A8\u5207\u6362\u5230 OpenRouter Base URL\u3002";
    } else if (keyProvider === "openai" && isOpenRouterBase) {
      base = "https://api.openai.com/v1";
      autoFixHint = "\u68C0\u6D4B\u5230 OpenAI Key\uFF0C\u5DF2\u81EA\u52A8\u5207\u6362\u5230 OpenAI Base URL\u3002";
    }
    const url = /\/chat\/completions$/i.test(base) ? base : `${base}/chat/completions`;
    const headers = {
      "Content-Type": "application/json",
      Authorization: `Bearer ${apiKey}`
    };
    if (/openrouter\.ai/i.test(base)) {
      headers["HTTP-Referer"] = "https://obsidian.md";
      headers["X-Title"] = "\u903B\u8F91\u539F\u5B50\u5F15\u64CE";
    }
    return { url, headers, autoFixHint };
  }
  async chat(messages, temperature = 0.3) {
    const apiKey = (this.apiKey || "").trim();
    if (!apiKey) {
      throw new Error("LLM API Key \u672A\u914D\u7F6E\uFF0C\u8BF7\u5148\u5728\u63D2\u4EF6\u8BBE\u7F6E\u4E2D\u586B\u5199\u3002");
    }
    const { url, headers, autoFixHint } = this.resolveEndpointAndHeaders(apiKey);
    if (autoFixHint) {
      console.warn("[LogicAtom LLM] Auto fixed provider mismatch:", autoFixHint);
    }
    console.log("[LogicAtom LLM] Request:", { providerUrl: url, model: this.model });
    try {
      const response = await (0, import_obsidian2.requestUrl)({
        url,
        method: "POST",
        headers,
        body: JSON.stringify({
          model: this.model,
          messages,
          temperature,
          max_tokens: 2e3
        })
      });
      const data = response.json;
      return {
        content: data.choices[0].message.content,
        model: data.model,
        usage: data.usage
      };
    } catch (e) {
      const rawMessage = (e == null ? void 0 : e.message) || String(e);
      const status = (e == null ? void 0 : e.status) || (e == null ? void 0 : e.statusCode);
      const is401 = status === 401 || /401/.test(rawMessage);
      const is403 = status === 403 || /403/.test(rawMessage);
      if (is401 || is403) {
        const keyProvider = this.detectKeyProvider(apiKey);
        const mismatchHint = keyProvider === "openrouter" && /openai\.com/i.test(url) ? "\uFF08OpenRouter Key \u4E0E OpenAI URL \u4E0D\u5339\u914D\uFF09" : keyProvider === "openai" && /openrouter\.ai/i.test(url) ? "\uFF08OpenAI Key \u4E0E OpenRouter URL \u4E0D\u5339\u914D\uFF09" : "";
        console.error("[LogicAtom LLM] Auth Error:", rawMessage);
        throw new Error(
          `[LLM ${is401 ? "401" : "403"}] \u9274\u6743\u5931\u8D25\uFF1A\u8BF7\u68C0\u67E5 LLM API Key\u3001Base URL \u548C\u6A21\u578B\u6743\u9650\u662F\u5426\u5339\u914D${mismatchHint}`
        );
      }
      console.error("[LogicAtom LLM] Error:", rawMessage);
      throw new Error(`[LLM] \u8BF7\u6C42\u5931\u8D25\uFF1A${rawMessage}`);
    }
  }
  async complete(systemPrompt, userPrompt, temperature = 0.3) {
    const resp = await this.chat([
      { role: "system", content: systemPrompt },
      { role: "user", content: userPrompt }
    ], temperature);
    return resp.content;
  }
  async testConnection() {
    try {
      const resp = await this.chat([
        { role: "system", content: "You are a connectivity checker. Reply with only OK." },
        { role: "user", content: "health_check" }
      ], 0);
      return {
        ok: true,
        message: `LLM \u8FDE\u63A5\u6210\u529F\uFF0C\u6A21\u578B\uFF1A${resp.model || this.model}`
      };
    } catch (e) {
      return {
        ok: false,
        message: `LLM \u8FDE\u63A5\u5931\u8D25\uFF1A${(e == null ? void 0 : e.message) || String(e)}`
      };
    }
  }
};

// src/utils/search-query.ts
var CHINESE_STOP_WORDS = /* @__PURE__ */ new Set([
  "\u6211\u4EEC",
  "\u4F60\u4EEC",
  "\u4ED6\u4EEC",
  "\u8FD9\u4E2A",
  "\u90A3\u4E2A",
  "\u8FD9\u4E9B",
  "\u90A3\u4E9B",
  "\u4E00\u79CD",
  "\u4E00\u4E9B",
  "\u4EE5\u53CA",
  "\u7136\u540E",
  "\u56E0\u4E3A",
  "\u6240\u4EE5",
  "\u5982\u679C",
  "\u5C31\u662F",
  "\u4E0D\u662F",
  "\u5DF2\u7ECF",
  "\u53EF\u4EE5",
  "\u9700\u8981",
  "\u8FDB\u884C",
  "\u901A\u8FC7",
  "\u5BF9\u4E8E",
  "\u5173\u4E8E",
  "\u5176\u4E2D",
  "\u6CA1\u6709",
  "\u600E\u4E48",
  "\u4E3A\u4EC0\u4E48",
  "\u4EC0\u4E48",
  "\u65F6\u5019",
  "\u53EF\u80FD",
  "\u9700\u8981\u5148",
  "\u4E00\u4E2A",
  "\u4E00\u79CD\u65B9\u6CD5",
  "\u6587\u7AE0",
  "\u5185\u5BB9",
  "\u539F\u6587",
  "\u8FC7\u7A0B",
  "\u7ED3\u679C",
  "\u95EE\u9898",
  "\u4E1C\u897F"
]);
var ENGLISH_STOP_WORDS = /* @__PURE__ */ new Set([
  "the",
  "and",
  "for",
  "with",
  "that",
  "this",
  "from",
  "into",
  "about",
  "then",
  "than",
  "have",
  "has",
  "had",
  "will",
  "would",
  "could",
  "should",
  "their",
  "there",
  "what",
  "when",
  "where",
  "which",
  "while",
  "using",
  "used",
  "into",
  "through",
  "because",
  "result",
  "input",
  "output",
  "analysis",
  "context"
]);
function normalizeWhitespace(text) {
  return (text || "").replace(/\s+/g, " ").trim();
}
function trimDecorators(text) {
  return text.replace(/^[-*#\d.\s:：_\[\]()"']+/, "").replace(/[：:]\s*(对象|定义|程度|关系|动作|前提|说明|示例)\s*$/i, "").replace(/\[[^\]]*\]/g, " ").replace(/[()（）"'`]/g, " ").replace(/\s+/g, " ").trim();
}
function isMeaningfulHandle(text) {
  const value = text.trim();
  if (value.length < 2)
    return false;
  if (/^\d+$/.test(value))
    return false;
  const lower = value.toLowerCase();
  if (CHINESE_STOP_WORDS.has(value) || ENGLISH_STOP_WORDS.has(lower))
    return false;
  return true;
}
function splitClauses(text) {
  return text.replace(/\r?\n/g, " ").split(/[，。；：、,.!?()\[\]{}<>/\\|]+/).map((part) => normalizeWhitespace(part)).filter(Boolean);
}
function splitEnglishWords(text) {
  return text.split(/\s+/).map((word) => word.trim()).filter((word) => /^[a-z0-9][a-z0-9-]{1,}$/i.test(word));
}
function sanitizeHandle(raw, maxLength = 18) {
  const trimmed = trimDecorators(normalizeWhitespace(raw));
  if (!trimmed)
    return "";
  const clauses = splitClauses(trimmed).map((part) => part.length > maxLength ? part.slice(0, maxLength) : part).filter(isMeaningfulHandle);
  if (clauses.length > 0) {
    return clauses[0];
  }
  const words = splitEnglishWords(trimmed).filter((word) => !ENGLISH_STOP_WORDS.has(word.toLowerCase())).slice(0, 3);
  if (words.length > 0) {
    return words.join(" ");
  }
  return trimmed.slice(0, maxLength);
}
function extractInterfaceHandles(text, maxCount = 5) {
  const normalized = normalizeWhitespace(text);
  if (!normalized)
    return [];
  const clauses = splitClauses(normalized);
  const rawCandidates = [];
  for (const clause of clauses) {
    if (clause.length >= 2 && clause.length <= 18) {
      rawCandidates.push(clause);
      continue;
    }
    if (clause.length > 18) {
      rawCandidates.push(clause.slice(0, 18));
    }
  }
  const tokenMatches = normalized.match(/[A-Za-z][A-Za-z0-9-]{1,}|[\u4e00-\u9fa5]{2,12}/g) || [];
  rawCandidates.push(...tokenMatches);
  const unique = [];
  for (const candidate of rawCandidates) {
    const handle = sanitizeHandle(candidate);
    if (!isMeaningfulHandle(handle))
      continue;
    if (!unique.includes(handle)) {
      unique.push(handle);
    }
    if (unique.length >= maxCount)
      break;
  }
  return unique;
}
function sanitizeGeneratedHandles(handles, fallbackText, maxCount = 5) {
  const unique = [];
  for (const handle of handles) {
    const cleaned = sanitizeHandle(handle);
    if (!isMeaningfulHandle(cleaned))
      continue;
    if (!unique.includes(cleaned)) {
      unique.push(cleaned);
    }
    if (unique.length >= maxCount)
      break;
  }
  if (unique.length > 0) {
    return unique;
  }
  return extractInterfaceHandles(fallbackText, maxCount);
}
function buildInterfaceSearchQuery(text, options = {}) {
  const handles = extractInterfaceHandles(text, options.maxHandles || 4);
  const parts = [];
  if (options.prefix) {
    const prefix = sanitizeHandle(options.prefix, 12);
    if (prefix)
      parts.push(prefix);
  }
  parts.push(...handles);
  return normalizeWhitespace(parts.join(" "));
}

// src/utils/interface-graph.ts
function splitParagraphs(input) {
  return (input || "").split(/\n{2,}/).map((block) => block.replace(/\s+/g, " ").trim()).filter(Boolean);
}
function clipPreview(text, maxLength = 80) {
  return text.length > maxLength ? `${text.slice(0, maxLength)}...` : text;
}
function buildInterfaceGraph(input) {
  const paragraphs = splitParagraphs(input);
  const nodeMap = /* @__PURE__ */ new Map();
  const paragraphSummaries = [];
  paragraphs.forEach((paragraph, index) => {
    const handles = extractInterfaceHandles(paragraph, 5);
    paragraphSummaries.push({
      index,
      handles,
      preview: clipPreview(paragraph)
    });
    for (const handle of handles) {
      if (!nodeMap.has(handle)) {
        nodeMap.set(handle, {
          weight: 0,
          paragraphs: /* @__PURE__ */ new Set(),
          neighbors: /* @__PURE__ */ new Set()
        });
      }
      const node = nodeMap.get(handle);
      node.weight += 1;
      node.paragraphs.add(index);
    }
    for (let i = 0; i < handles.length; i++) {
      for (let j = i + 1; j < handles.length; j++) {
        const left = nodeMap.get(handles[i]);
        const right = nodeMap.get(handles[j]);
        left == null ? void 0 : left.neighbors.add(handles[j]);
        right == null ? void 0 : right.neighbors.add(handles[i]);
      }
    }
  });
  const nodes = Array.from(nodeMap.entries()).map(([handle, node]) => ({
    handle,
    weight: node.weight,
    paragraphs: Array.from(node.paragraphs).sort((a, b) => a - b),
    neighbors: Array.from(node.neighbors).sort()
  })).sort((a, b) => b.weight - a.weight || a.handle.localeCompare(b.handle)).slice(0, 24);
  return {
    nodes,
    paragraphSummaries,
    topHandles: nodes.slice(0, 6).map((node) => node.handle)
  };
}
function summarizeInterfaceGraph(graph, maxNodes = 6, maxParagraphs = 3) {
  const nodeLines = graph.nodes.slice(0, maxNodes).map((node, index) => {
    const neighborText = node.neighbors.slice(0, 3).join("\u3001") || "\u65E0";
    return `${index + 1}. ${node.handle} | \u6BB5\u843D: ${node.paragraphs.map((n) => n + 1).join(", ")} | \u90BB\u63A5: ${neighborText}`;
  });
  const paragraphLines = graph.paragraphSummaries.slice(0, maxParagraphs).map((item) => {
    const handles = item.handles.join("\u3001") || "\u65E0\u63A5\u53E3";
    return `\u6BB5\u843D${item.index + 1}: ${handles} | ${item.preview}`;
  });
  return [
    "\u63A5\u53E3\u56FE\u6458\u8981\uFF1A",
    ...nodeLines.length > 0 ? nodeLines : ["- \u65E0\u63A5\u53E3\u8282\u70B9"],
    "",
    "\u6BB5\u843D\u63A5\u53E3\u5206\u5E03\uFF1A",
    ...paragraphLines.length > 0 ? paragraphLines : ["- \u65E0\u6BB5\u843D\u6458\u8981"]
  ].join("\n");
}

// src/atoms/engine.ts
var AtomEngine = class {
  constructor(apiClient, llmClient) {
    this.atoms = /* @__PURE__ */ new Map();
    this.apiClient = apiClient;
    this.llmClient = llmClient;
  }
  registerAtom(atom) {
    this.atoms.set(atom.name, atom);
  }
  getAtom(name) {
    return this.atoms.get(name);
  }
  getRegisteredAtoms() {
    return Array.from(this.atoms.keys());
  }
  async executeChain(input, atomNames, onProgress, options = {}) {
    const startTime = Date.now();
    const initializationWarnings = [];
    const localGraph = buildInterfaceGraph(input);
    if (options.persistExecutionPlan) {
      const executionPlanContent = [
        "[ExecutionPlan]",
        `workspace: ${options.workspaceName || "default"}`,
        `workflow: ${options.workflowLabel || options.mode || "general"}`,
        `atom_order: ${atomNames.join(" -> ")}`,
        `input_preview: ${input.slice(0, 240)}`,
        `timestamp: ${(/* @__PURE__ */ new Date()).toISOString()}`
      ].join("\n");
      try {
        await this.apiClient.storeMemory(
          executionPlanContent,
          `exec_plan_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`
        );
      } catch (e) {
        console.error("[LogicAtom] Failed to store execution plan:", e);
      }
    }
    const initialQuery = this.buildInitialSearchQuery(input, options.mode, localGraph.topHandles);
    let initialMemories = [];
    try {
      initialMemories = await this.apiClient.searchMemories(initialQuery, 10);
    } catch (e) {
      initializationWarnings.push(`\u521D\u59CB\u5316\u8BB0\u5FC6\u68C0\u7D22\u5931\u8D25\uFF1A${(e == null ? void 0 : e.message) || String(e)}`);
      initialMemories = [];
    }
    const initialProfiles = initialMemories.filter((m) => m.type === "profile");
    const steps = [];
    const ctx = {
      input,
      initialMemories,
      initialProfiles,
      localGraph,
      previousSteps: [],
      conversationId: "obsidian-three-library",
      mode: options.mode || "general",
      objective: options.objective,
      synthesisGuidance: options.synthesisGuidance
    };
    for (let i = 0; i < atomNames.length; i++) {
      const atomName = atomNames[i];
      const atom = this.atoms.get(atomName);
      if (!atom) {
        throw new Error(`Atom "${atomName}" not registered`);
      }
      let stepResult;
      try {
        stepResult = await atom.execute(ctx);
      } catch (e) {
        console.error(`[LogicAtom] Atom "${atomName}" failed:`, e.message);
        stepResult = {
          atom: atomName,
          summary: `\u6267\u884C\u5931\u8D25\uFF1A${e.message}`,
          output: `\u539F\u5B50 "${atomName}" \u6267\u884C\u51FA\u9519\uFF1A${e.message}`,
          memoriesRetrieved: [],
          memoriesStored: [],
          searchCount: 0,
          storeCount: 0,
          duration: Date.now() - startTime
        };
      }
      steps.push(stepResult);
      ctx.previousSteps = [...steps];
      if (onProgress) {
        onProgress(stepResult, i, atomNames.length);
      }
    }
    const finalOutput = await this.synthesize(
      input,
      steps,
      options,
      initialProfiles,
      initializationWarnings
    );
    return {
      input,
      atomNames,
      steps,
      finalOutput,
      totalSearches: steps.reduce((sum, s) => sum + s.searchCount, 0),
      totalStores: steps.reduce((sum, s) => sum + s.storeCount, 0),
      totalDuration: Date.now() - startTime
    };
  }
  buildInitialSearchQuery(input, mode, preferredHandles = []) {
    const prefix = mode && mode.startsWith("reading-") ? "\u9605\u8BFB\u63A5\u53E3" : "\u4E3B\u9898";
    const handles = preferredHandles.slice(0, mode && mode.startsWith("reading-") ? 5 : 4);
    if (handles.length > 0) {
      return [prefix, ...handles].join(" ");
    }
    return buildInterfaceSearchQuery(input, {
      prefix,
      maxHandles: mode && mode.startsWith("reading-") ? 5 : 4
    });
  }
  async synthesize(input, steps, options, initialProfiles, initializationWarnings = []) {
    const stepsContext = steps.map((s, i) => `Step ${i + 1} [${s.atom}]: ${s.summary}
Output: ${s.output}`).join("\n\n");
    const profileContext = initialProfiles.length > 0 ? `
User profile hints from memory:
${initialProfiles.slice(0, 5).map((p, i) => `${i + 1}. ${p.title}: ${p.content.slice(0, 120)}`).join("\n")}
` : "\nUser profile hints from memory:\n(none)\n";
    const guidance = options.synthesisGuidance ? `
Additional synthesis guidance:
${options.synthesisGuidance}
` : "";
    const objective = options.objective ? `
Current workflow objective:
${options.objective}
` : "";
    const graphSummary = `
${this.buildGraphSummary(input)}
`;
    const prompt = `Based on the following structured thinking steps, synthesize a final result that treats the original material as A and the compressed note result as A'.

Original Question or Material (A):
${input}
${profileContext}${objective}
${graphSummary}

Thinking Steps:
${stepsContext}${guidance}
Provide a clear, well-structured synthesis that emphasizes:
1. The final compact interface set or mnemonic-like handles for A'
2. What each handle stands for
3. How A' stays semantically consistent with A after compression
4. What was corrected in A' to keep that consistency
5. One-step extensions or deeper judgments grounded in A'
Use the language of the original question or material. Do not turn the result into a difference report.`;
    try {
      return await this.llmClient.complete(
        "You are a synthesis engine. Combine multi-step thinking results into a coherent final answer. When the workflow is about reading and compression, treat the goal as constructing a compact but callable interface set A' for the original material A. A' must stay semantically consistent with A after compression. Respect any requested structure and output language.",
        prompt
      );
    } catch (e) {
      return this.buildFallbackSynthesis(
        input,
        steps,
        initializationWarnings,
        (e == null ? void 0 : e.message) || String(e)
      );
    }
  }
  buildGraphSummary(input) {
    const graph = buildInterfaceGraph(input);
    return summarizeInterfaceGraph(graph);
  }
  buildFallbackSynthesis(input, steps, initializationWarnings, synthError) {
    const stepSummary = steps.map((s, i) => `${i + 1}. [${s.atom}] ${s.summary}`).join("\n");
    const stepOutputs = steps.map((s, i) => {
      const clipped = s.output.length > 800 ? `${s.output.slice(0, 800)}
...(\u5DF2\u622A\u65AD)` : s.output;
      return `### Step ${i + 1} - ${s.atom}
${clipped}`;
    }).join("\n\n");
    const warningLines = [
      ...initializationWarnings,
      `\u7EFC\u5408\u9636\u6BB5\u964D\u7EA7\uFF1A${synthError}`
    ];
    return [
      "## \u7EFC\u5408\u7ED3\u679C\uFF08\u964D\u7EA7\u6A21\u5F0F\uFF09",
      "",
      `\u539F\u59CB\u8F93\u5165\uFF1A${input}`,
      "",
      "### \u6267\u884C\u6458\u8981",
      stepSummary || "- \u65E0\u6B65\u9AA4\u7ED3\u679C",
      "",
      "### \u53EF\u7528\u8F93\u51FA",
      stepOutputs || "- \u65E0\u53EF\u7528\u8F93\u51FA",
      "",
      "### \u6267\u884C\u544A\u8B66",
      warningLines.map((w, i) => `${i + 1}. ${w}`).join("\n")
    ].join("\n");
  }
};

// src/atoms/selector.ts
var VALID_ATOMS = ["decompose", "associate", "transform", "abstract", "evaluate", "iterate"];
var SELECTOR_SYSTEM_PROMPT = `You are an atom selector for a structured thinking engine. Given a user's input, select 2-4 thinking atoms that would best help process the input.

Available atoms:
- decompose: Break complex problems into sub-questions. Best for: multi-faceted questions, complex tasks, "how to" questions.
- associate: Find connections between different concepts. Best for: brainstorming, finding patterns across domains, creative thinking.
- transform: View a topic from multiple perspectives. Best for: reframing problems, understanding different viewpoints, creative solutions.
- abstract: Extract high-level patterns and principles. Best for: finding rules, generalizing from specifics, identifying meta-patterns.
- evaluate: Score and compare options systematically. Best for: decision-making, comparisons, pros/cons analysis.
- iterate: Refine through multiple rounds of feedback. Best for: improving drafts, refining ideas, optimization.

Rules:
1. Select 2-4 atoms (never more, never less)
2. Order matters \u2014 atoms execute sequentially, each building on previous results
3. Respond with ONLY a JSON array of atom names, nothing else

Examples:
Input: "How should I learn machine learning?"
Output: ["decompose", "evaluate"]

Input: "What connects philosophy and programming?"
Output: ["associate", "abstract"]

Input: "Compare React vs Vue for my project"
Output: ["decompose", "evaluate", "abstract"]

Input: "Help me improve my essay about climate change"
Output: ["decompose", "transform", "iterate"]`;
var AtomSelector = class {
  constructor(llmClient) {
    this.llmClient = llmClient;
  }
  async selectAtoms(input) {
    try {
      const response = await this.llmClient.complete(
        SELECTOR_SYSTEM_PROMPT,
        input,
        0.2
      );
      return this.parseAtomNames(response);
    } catch (e) {
      console.warn("[LogicAtom Selector] fallback due to selector error:", (e == null ? void 0 : e.message) || String(e));
      return ["decompose", "abstract", "evaluate"];
    }
  }
  parseAtomNames(response) {
    const match = response.match(/\[([^\]]+)\]/);
    if (!match) {
      return ["decompose", "evaluate"];
    }
    try {
      const parsed = JSON.parse(match[0]);
      const valid = parsed.filter(
        (name) => VALID_ATOMS.includes(name)
      );
      if (valid.length < 2)
        return ["decompose", "evaluate"];
      if (valid.length > 4)
        return valid.slice(0, 4);
      return valid;
    } catch (e) {
      return ["decompose", "evaluate"];
    }
  }
};

// src/atoms/decompose.ts
var DecomposeAtom = class {
  constructor(api, llm) {
    this.name = "decompose";
    this.description = "Break complex problems into sub-questions, search memory for each";
    this.api = api;
    this.llm = llm;
  }
  extractFallbackHandles(input, maxCount = 5) {
    const handles = extractInterfaceHandles(input, maxCount);
    return handles.length > 0 ? handles : [input.slice(0, 24)];
  }
  buildInterfaceLabel(index) {
    return String.fromCharCode("a".charCodeAt(0) + index);
  }
  async execute(ctx) {
    const start = Date.now();
    let searchCount = 0;
    let storeCount = 0;
    const allMemories = [];
    const stored = [];
    const warnings = [];
    const previousContext = ctx.previousSteps.length > 0 ? `
Previous analysis:
${ctx.previousSteps.map((s) => `[${s.atom}]: ${s.summary}`).join("\n")}` : "";
    const memoryContext = ctx.initialMemories.length > 0 ? `
Relevant memories:
${ctx.initialMemories.slice(0, 3).map((m) => `- ${m.title}`).join("\n")}` : "";
    const profileContext = ctx.initialProfiles.length > 0 ? `
User profile hints:
${ctx.initialProfiles.slice(0, 3).map((p) => `- ${p.title}`).join("\n")}` : "";
    const objectiveContext = ctx.objective ? `
Current objective:
${ctx.objective}` : "";
    const graphContext = ctx.localGraph ? `
${summarizeInterfaceGraph(ctx.localGraph, 5, 3)}` : "";
    const readingMode = ctx.mode.startsWith("reading-");
    let instruction = "Decompose this into 3-5 sub-questions.";
    if (readingMode) {
      instruction = "Read the material in a skimming way and extract 3-5 interface candidates. Each candidate should be a short callable handle such as a keyword, noun definition, degree descriptor, relation, action, or hidden prerequisite. Do not summarize sentence by sentence. Prefer handles that can later become compact notes like a/b/c or a mnemonic.";
    }
    if (ctx.mode === "reading-connect") {
      instruction = "Extract 3-5 prerequisite interface candidates a beginner would need before fully understanding this material. Each handle should name a foundational concept, missing definition, relation, or blocking point.";
    } else if (ctx.mode === "reading-apply") {
      instruction = "Extract 3-5 memory-interface units for note compression. Each unit should point to object, definition, descriptor, relation, or action, and should be short enough to become a compact handle later.";
    }
    let subQuestions;
    try {
      const decomposition = await this.llm.complete(
        "You decompose inputs into 3-5 focused interface candidates. For reading materials, prefer keywords, noun definitions, key descriptors, relation words, degree words, memorable facts, and hidden prerequisites. Each candidate should be short enough to become a reusable note handle. Respond with ONLY a JSON array of strings.",
        `${instruction}

"${ctx.input}"${objectiveContext}${previousContext}${memoryContext}${profileContext}${graphContext}

Respond with a JSON array of strings.`
      );
      const match = decomposition.match(/\[[\s\S]*\]/);
      subQuestions = match ? JSON.parse(match[0]) : this.extractFallbackHandles(ctx.input, 5);
    } catch (e) {
      warnings.push(`\u62C6\u89E3\u964D\u7EA7\uFF1A${(e == null ? void 0 : e.message) || String(e)}`);
      subQuestions = this.extractFallbackHandles(ctx.input, 5);
    }
    subQuestions = sanitizeGeneratedHandles(subQuestions, ctx.input, 5);
    const subResults = [];
    for (const q of subQuestions) {
      try {
        const memories = await this.api.searchMemories(q, 5);
        searchCount++;
        allMemories.push(...memories);
        subResults.push({ question: q, memories });
      } catch (e) {
        warnings.push(`\u68C0\u7D22\u5931\u8D25\uFF08${q}\uFF09\uFF1A${(e == null ? void 0 : e.message) || String(e)}`);
        subResults.push({ question: q, memories: [] });
      }
    }
    const structurePrefix = readingMode ? "[Interface Candidates]" : "[Decomposition]";
    const structureContent = `${structurePrefix} ${ctx.input.slice(0, 120)}
` + subResults.map(
      (sr, i) => `handle_${this.buildInterfaceLabel(i)}: ${sr.question} (found ${sr.memories.length} related memories)`
    ).join("\n");
    try {
      const { eventId } = await this.api.storeMemory(structureContent);
      storeCount++;
      stored.push({ eventId, content: structureContent });
    } catch (e) {
      warnings.push(`\u62C6\u89E3\u7ED3\u679C\u5B58\u50A8\u5931\u8D25\uFF1A${(e == null ? void 0 : e.message) || String(e)}`);
    }
    const outputHeader = readingMode ? "### \u63A5\u53E3\u5019\u9009\u6293\u53D6" : "### \u95EE\u9898\u62C6\u89E3";
    const output = subResults.map((sr, i) => {
      const handleName = this.buildInterfaceLabel(i);
      const memSummary = sr.memories.length > 0 ? sr.memories.slice(0, 3).map((m) => `  - ${m.title}`).join("\n") : "  (no related memories found)";
      const title = readingMode ? `### \u63A5\u53E3 ${handleName}: ${sr.question}` : `### Sub-question ${i + 1}: ${sr.question}`;
      return readingMode ? `${title}
\u7C7B\u578B\u5EFA\u8BAE\uFF1A\u5BF9\u8C61 / \u5B9A\u4E49 / \u7A0B\u5EA6 / \u5173\u7CFB / \u52A8\u4F5C / \u524D\u63D0 \u4E2D\u62E9\u4E00
\u53EF\u8C03\u7528\u8BF4\u660E\uFF1A\u540E\u7EED\u5E94\u80FD\u7528\u201C${handleName}\u201D\u4EE3\u6307\u8FD9\u5757\u77E5\u8BC6
Related memories:
${memSummary}` : `${title}
Related memories:
${memSummary}`;
    }).join("\n\n");
    return {
      atom: this.name,
      summary: warnings.length > 0 ? `\u5DF2\u964D\u7EA7\u6267\u884C\uFF1A\u62C6\u89E3 ${subQuestions.length} \u9879\uFF0C\u68C0\u7D22 ${searchCount} \u6B21\uFF0C\u544A\u8B66 ${warnings.length} \u6761` : readingMode ? `\u6293\u51FA ${subQuestions.length} \u4E2A\u63A5\u53E3\u5019\u9009\uFF0C\u4F9B\u540E\u7EED\u538B\u7F29\u4E3A A'\uFF0C\u68C0\u7D22 ${searchCount} \u6B21` : `Decomposed into ${subQuestions.length} sub-questions, searched ${searchCount} times`,
      output: warnings.length > 0 ? `${outputHeader}

${output}

### \u6267\u884C\u544A\u8B66
${warnings.map((w, i) => `${i + 1}. ${w}`).join("\n")}` : `${outputHeader}

${output}`,
      memoriesRetrieved: allMemories,
      memoriesStored: stored,
      searchCount,
      storeCount,
      duration: Date.now() - start
    };
  }
};

// src/atoms/associate.ts
var AssociateAtom = class {
  constructor(api, llm) {
    this.name = "associate";
    this.description = "Find connections between different concepts across memory domains";
    this.api = api;
    this.llm = llm;
  }
  extractFallbackConcepts(input, maxCount = 5) {
    const handles = extractInterfaceHandles(input, maxCount);
    return handles.length > 0 ? handles : [input.slice(0, 24)];
  }
  buildFallbackAssociations(conceptResults) {
    const lines = conceptResults.map((cr, idx) => {
      const topTitles = cr.memories.slice(0, 3).map((m) => m.title).join("\u3001");
      return `${idx + 1}. \u63A5\u53E3\u300C${cr.concept}\u300D\u4E0E\u8BB0\u5FC6\u7684\u76F4\u63A5\u5173\u8054\uFF1A${topTitles || "\u6682\u65E0\u5339\u914D\u8BB0\u5FC6\uFF0C\u5EFA\u8BAE\u5148\u8865\u524D\u7F6E\u5B9A\u4E49"}\u3002`;
    });
    return lines.join("\n");
  }
  async execute(ctx) {
    const start = Date.now();
    let searchCount = 0;
    let storeCount = 0;
    const allMemories = [];
    const stored = [];
    const warnings = [];
    const previousOutput = ctx.previousSteps.length > 0 ? ctx.previousSteps[ctx.previousSteps.length - 1].output : "";
    const objectiveContext = ctx.objective ? `
Current objective: ${ctx.objective}` : "";
    const profileContext = ctx.initialProfiles.length > 0 ? `
User profile hints:
${ctx.initialProfiles.slice(0, 3).map((p) => `- ${p.title}`).join("\n")}` : "";
    const graphContext = ctx.localGraph ? `
${summarizeInterfaceGraph(ctx.localGraph, 6, 3)}` : "";
    let concepts;
    try {
      const conceptExtraction = await this.llm.complete(
        "Extract 3-5 key interface handles from the text. For reading workflows, prioritize noun definitions, descriptors, relation words, and memorable facts that can later be used as compact note handles. Respond with ONLY a JSON array of strings.",
        `Extract interface handles from:
"${ctx.input}"${objectiveContext}${profileContext}${graphContext}
${previousOutput ? `Context: ${previousOutput.slice(0, 500)}` : ""}

JSON array of handle strings:`
      );
      const match = conceptExtraction.match(/\[[\s\S]*\]/);
      concepts = match ? JSON.parse(match[0]) : this.extractFallbackConcepts(ctx.input, 5);
    } catch (e) {
      warnings.push(`\u5173\u952E\u8BCD\u63D0\u53D6\u964D\u7EA7\uFF1A${(e == null ? void 0 : e.message) || String(e)}`);
      concepts = this.extractFallbackConcepts(ctx.input, 5);
    }
    concepts = sanitizeGeneratedHandles(concepts, ctx.input, 5);
    const conceptResults = [];
    for (const concept of concepts) {
      try {
        const memories = await this.api.searchMemories(concept, 5);
        searchCount++;
        allMemories.push(...memories);
        conceptResults.push({ concept, memories });
      } catch (e) {
        warnings.push(`\u8BB0\u5FC6\u68C0\u7D22\u5931\u8D25\uFF08${concept}\uFF09\uFF1A${(e == null ? void 0 : e.message) || String(e)}`);
        conceptResults.push({ concept, memories: [] });
      }
    }
    const associationContext = conceptResults.map(
      (cr) => `Concept: "${cr.concept}" \u2192 Memories: ${cr.memories.map((m) => m.title).join(", ") || "none"}`
    ).join("\n");
    let associationTask = "Find meaningful associations between these interface handles and their related memories.";
    if (ctx.mode === "reading-connect") {
      associationTask = "Find the strongest direct relationship between these interface handles and identify what prerequisite knowledge experts implicitly assume but beginners usually lack. Keep it concrete and tie every relation back to the original material.";
    } else if (ctx.mode === "reading-advanced") {
      associationTask = "Based on the compressed interface set A', generate extension questions and exactly one step of inference. Focus on author stance, background assumptions, omitted definitions, and missing viewpoints. Do not create multi-hop reasoning.";
    }
    let associations = "";
    try {
      associations = await this.llm.complete(
        "You identify meaningful connections between interface handles based on memory search results. When asked for one-step inference, stay constrained and explain only the strongest direct link. Treat each handle as a compact index into a larger knowledge block.",
        `${associationTask}
${profileContext}

${associationContext}

List each discovered association as a clear statement. If possible, state which handle supports which other handle.`
      );
    } catch (e) {
      warnings.push(`\u5173\u8054\u63A8\u7406\u964D\u7EA7\uFF1A${(e == null ? void 0 : e.message) || String(e)}`);
      associations = this.buildFallbackAssociations(conceptResults);
    }
    const prefix = ctx.mode === "reading-advanced" ? "[Advanced Application]" : ctx.mode === "reading-connect" ? "[Foundation Gap Mapping]" : "[Association]";
    const storeContent = `${prefix} ${ctx.input.slice(0, 120)}
Handles: ${concepts.join(", ")}

Discovered associations:
${associations}`;
    try {
      const { eventId: eid1 } = await this.api.storeMemory(storeContent);
      storeCount++;
      stored.push({ eventId: eid1, content: storeContent });
    } catch (e) {
      warnings.push(`\u7ED3\u679C\u5B58\u50A8\u5931\u8D25\uFF1A${(e == null ? void 0 : e.message) || String(e)}`);
    }
    if (allMemories.length > 5) {
      const crossDomainSummary = `[Cross-domain links] ${concepts.join(" \u2194 ")}: ${associations.slice(0, 200)}`;
      try {
        const { eventId: eid2 } = await this.api.storeMemory(crossDomainSummary);
        storeCount++;
        stored.push({ eventId: eid2, content: crossDomainSummary });
      } catch (e) {
        warnings.push(`\u8DE8\u57DF\u6458\u8981\u5B58\u50A8\u5931\u8D25\uFF1A${(e == null ? void 0 : e.message) || String(e)}`);
      }
    }
    return {
      atom: this.name,
      summary: warnings.length > 0 ? `\u5DF2\u964D\u7EA7\u6267\u884C\uFF1A\u6982\u5FF5 ${concepts.length} \u4E2A\uFF0C\u76F8\u5173\u8BB0\u5FC6 ${allMemories.length} \u6761\uFF0C\u544A\u8B66 ${warnings.length} \u6761` : `Explored ${concepts.length} concepts, found ${allMemories.length} related memories`,
      output: warnings.length > 0 ? `### \u63A5\u53E3\u5173\u7CFB

${associations}

### \u6267\u884C\u544A\u8B66
${warnings.map((w, i) => `${i + 1}. ${w}`).join("\n")}` : `### \u63A5\u53E3\u5173\u7CFB

${associations}`,
      memoriesRetrieved: allMemories,
      memoriesStored: stored,
      searchCount,
      storeCount,
      duration: Date.now() - start
    };
  }
};

// src/atoms/transform.ts
var TransformAtom = class {
  constructor(api, llm) {
    this.name = "transform";
    this.description = "View a topic from multiple different perspectives";
    this.api = api;
    this.llm = llm;
  }
  buildFallbackTransformation(input, perspectiveResults) {
    const lensLabel = {
      analytical: "\u5206\u6790\u89C6\u89D2",
      practical: "\u5B9E\u8DF5\u89C6\u89D2",
      "minimal-action": "\u6700\u5C0F\u52A8\u4F5C\u89C6\u89D2"
    };
    return perspectiveResults.map((pr) => {
      const titles = pr.memories.slice(0, 3).map((m) => m.title).join("\u3001");
      const label = lensLabel[pr.lens] || pr.lens;
      return `### ${label}
- \u4E3B\u9898\uFF1A${input.slice(0, 80)}
- \u5173\u8054\u8BB0\u5FC6\uFF1A${titles || "\u6682\u65E0\u5339\u914D\u8BB0\u5FC6"}
- \u5EFA\u8BAE\uFF1A\u5148\u56F4\u7ED5\u8BE5\u89C6\u89D2\u8865\u4E00\u4E2A\u6700\u5C0F\u53EF\u6267\u884C\u7ED3\u8BBA\u3002`;
    }).join("\n\n");
  }
  async execute(ctx) {
    const start = Date.now();
    let searchCount = 0;
    let storeCount = 0;
    const allMemories = [];
    const stored = [];
    const warnings = [];
    const lenses = [
      { name: "analytical", query: buildInterfaceSearchQuery(ctx.input, { prefix: "\u5206\u6790", maxHandles: 4 }) },
      { name: "practical", query: buildInterfaceSearchQuery(ctx.input, { prefix: "\u5E94\u7528", maxHandles: 4 }) },
      { name: "minimal-action", query: buildInterfaceSearchQuery(ctx.input, { prefix: "\u52A8\u4F5C", maxHandles: 4 }) }
    ];
    const perspectiveResults = [];
    for (const lens of lenses) {
      try {
        const memories = await this.api.searchMemories(lens.query, 5);
        searchCount++;
        allMemories.push(...memories);
        perspectiveResults.push({ lens: lens.name, memories });
      } catch (e) {
        warnings.push(`\u89C6\u89D2\u68C0\u7D22\u5931\u8D25\uFF08${lens.name}\uFF09\uFF1A${(e == null ? void 0 : e.message) || String(e)}`);
        perspectiveResults.push({ lens: lens.name, memories: [] });
      }
    }
    const previousContext = ctx.previousSteps.length > 0 ? `Previous analysis: ${ctx.previousSteps[ctx.previousSteps.length - 1].summary}` : "";
    const objectiveContext = ctx.objective ? `
Current objective: ${ctx.objective}` : "";
    const memoryInsights = perspectiveResults.map(
      (pr) => `${pr.lens} perspective memories: ${pr.memories.map((m) => m.title).join(", ") || "none"}`
    ).join("\n");
    const transformationTask = ctx.mode === "reading-apply" ? "Reframe the reading material into three compact views: what it means, how it could be used, and what the smallest useful action would be." : "Reframe this topic from 3 perspectives (analytical, creative, practical).";
    let transformation = "";
    try {
      transformation = await this.llm.complete(
        "You reframe topics from multiple perspectives. For each perspective, provide a unique insight that differs from the others.",
        `${transformationTask}

"${ctx.input}"

${previousContext}${objectiveContext}
Memory context:
${memoryInsights}

Provide each perspective as a distinct paragraph.`
      );
    } catch (e) {
      warnings.push(`\u8F6C\u5316\u751F\u6210\u964D\u7EA7\uFF1A${(e == null ? void 0 : e.message) || String(e)}`);
      transformation = this.buildFallbackTransformation(ctx.input, perspectiveResults);
    }
    for (const pr of perspectiveResults) {
      if (pr.memories.length > 0) {
        const content = `[Transform/${pr.lens}] ${ctx.input.slice(0, 120)}: perspective with ${pr.memories.length} memory references`;
        try {
          const { eventId } = await this.api.storeMemory(content);
          storeCount++;
          stored.push({ eventId, content });
        } catch (e) {
          warnings.push(`\u8F6C\u5316\u5B58\u50A8\u5931\u8D25\uFF08${pr.lens}\uFF09\uFF1A${(e == null ? void 0 : e.message) || String(e)}`);
        }
      }
    }
    return {
      atom: this.name,
      summary: warnings.length > 0 ? `\u5DF2\u964D\u7EA7\u6267\u884C\uFF1A\u89C6\u89D2 ${lenses.length} \u4E2A\uFF0C\u8BB0\u5FC6 ${allMemories.length} \u6761\uFF0C\u544A\u8B66 ${warnings.length} \u6761` : `Examined ${lenses.length} perspectives, found ${allMemories.length} supporting memories`,
      output: warnings.length > 0 ? `### \u591A\u89C6\u89D2\u8F6C\u5316

${transformation}

### \u6267\u884C\u544A\u8B66
${warnings.map((w, i) => `${i + 1}. ${w}`).join("\n")}` : `### \u591A\u89C6\u89D2\u8F6C\u5316

${transformation}`,
      memoriesRetrieved: allMemories,
      memoriesStored: stored,
      searchCount,
      storeCount,
      duration: Date.now() - start
    };
  }
};

// src/atoms/abstract.ts
var AbstractAtom = class {
  constructor(api, llm) {
    this.name = "abstract";
    this.description = "Extract high-level patterns and principles from specific cases";
    this.api = api;
    this.llm = llm;
  }
  buildFallbackAbstraction(input, memories, mode) {
    const topMemoryLines = memories.slice(0, 5).map((m, i) => `${i + 1}. ${m.title}`);
    if (mode === "reading-understand") {
      return [
        "1. \u8FD9\u6BB5\u5728\u8BB2\u4EC0\u4E48\uFF08\u767D\u8BDD\uFF09",
        `${input.slice(0, 120)}...`,
        "",
        "2. \u5FC5\u61C2\u6982\u5FF5\uFF08\u5148\u8865\u57FA\u7840\uFF09",
        topMemoryLines.length > 0 ? topMemoryLines.join("\n") : "- \u6682\u65E0\u53EF\u7528\u8BB0\u5FC6\uFF0C\u5EFA\u8BAE\u5148\u8865\u5173\u952E\u8BCD\u5B9A\u4E49",
        "",
        "3. \u6700\u77ED\u7406\u89E3\u8DEF\u5F84",
        "- \u5148\u770B\u6838\u5FC3\u5BF9\u8C61\u5B9A\u4E49",
        "- \u518D\u770B\u5BF9\u8C61\u4E4B\u95F4\u5173\u7CFB",
        "- \u6700\u540E\u56DE\u5230\u539F\u6587\u9A8C\u8BC1"
      ].join("\n");
    }
    if (mode === "reading-apply") {
      return [
        "1. \u8BB0\u5FC6\u5355\u4F4D\uFF08\u5BF9\u8C61/\u5B9A\u4E49/\u5173\u7CFB\uFF09",
        `- \u5BF9\u8C61\uFF1A${input.slice(0, 40)}`,
        "- \u5B9A\u4E49\uFF1A\u5F85\u8865\u5145\uFF08LLM \u6682\u4E0D\u53EF\u7528\uFF09",
        "- \u5173\u7CFB\uFF1A\u5F85\u8865\u5145\uFF08LLM \u6682\u4E0D\u53EF\u7528\uFF09",
        "",
        "2. \u4E0B\u4E00\u6B65\u52A8\u4F5C",
        "- \u57FA\u4E8E\u5173\u952E\u8BCD\u624B\u52A8\u8865\u4E00\u4E2A\u6700\u5C0F\u5B9A\u4E49\u540E\u518D\u6267\u884C\u4E00\u6B21"
      ].join("\n");
    }
    return [
      "1. \u6838\u5FC3\u4E3B\u9898",
      input.slice(0, 120),
      "",
      "2. \u53EF\u590D\u7528\u6A21\u5F0F",
      topMemoryLines.length > 0 ? topMemoryLines.join("\n") : "- \u6682\u65E0\u53EF\u590D\u7528\u8BB0\u5FC6",
      "",
      "3. \u4E0B\u4E00\u6B65",
      "- \u5148\u8865\u5145\u57FA\u7840\u5B9A\u4E49\uFF0C\u518D\u505A\u7CBE\u70BC\u62BD\u8C61"
    ].join("\n");
  }
  async execute(ctx) {
    const start = Date.now();
    let searchCount = 0;
    let storeCount = 0;
    const allMemories = [];
    const stored = [];
    const warnings = [];
    const patternQuery = buildInterfaceSearchQuery(ctx.input, { prefix: "\u6A21\u5F0F", maxHandles: 4 });
    try {
      const patternMemories = await this.api.searchMemories(patternQuery, 5);
      searchCount++;
      allMemories.push(...patternMemories);
    } catch (e) {
      warnings.push(`\u6A21\u5F0F\u68C0\u7D22\u5931\u8D25\uFF1A${(e == null ? void 0 : e.message) || String(e)}`);
    }
    const topicSeed = buildInterfaceSearchQuery(ctx.input, { prefix: "\u4E3B\u9898", maxHandles: 5 });
    try {
      const topicMemories = await this.api.searchMemories(topicSeed, 10);
      searchCount++;
      allMemories.push(...topicMemories);
    } catch (e) {
      warnings.push(`\u4E3B\u9898\u68C0\u7D22\u5931\u8D25\uFF1A${(e == null ? void 0 : e.message) || String(e)}`);
    }
    const previousStepsContext = ctx.previousSteps.map(
      (s) => `[${s.atom}]: ${s.output.slice(0, 300)}`
    ).join("\n\n");
    const memoryContext = allMemories.slice(0, 10).map(
      (m) => `- ${m.title}: ${m.content.slice(0, 150)}`
    ).join("\n");
    const profileContext = ctx.initialProfiles.slice(0, 5).map(
      (p) => `- ${p.title}: ${p.content.slice(0, 120)}`
    ).join("\n");
    const objectiveContext = ctx.objective ? `
Current objective:
${ctx.objective}` : "";
    const graphContext = ctx.localGraph ? `
${summarizeInterfaceGraph(ctx.localGraph, 6, 3)}` : "";
    let readingTask = "Identify 2-4 key patterns or principles. For each, state the pattern clearly and explain why it matters.";
    if (ctx.mode === "reading-understand") {
      readingTask = "Explain the material for a beginner: what it means in plain language, explicitly surface hidden assumptions experts usually keep in mind, list must-know concepts with definitions, identify prerequisite blockers, and provide one minimal example.";
    } else if (ctx.mode === "reading-connect") {
      readingTask = "Output a foundation-gap bridge: explicitly list hidden assumptions, inferred known basics, missing prerequisites, and a 3-step path to close gaps before revisiting the original text.";
    } else if (ctx.mode === "reading-apply") {
      readingTask = "Compress into memory units: object, definition/property, descriptor words, relation/action, and a compressed expression (phrase/symbol/mnemonic). Add one-step inference and one small action.";
    } else if (ctx.mode === "reading-advanced") {
      readingTask = "Based on compressed notes, output 2-3 extension questions, exactly one-step inference, evidence, and next practice action with basic vs advanced gradient.";
    }
    let abstraction = "";
    try {
      abstraction = await this.llm.complete(
        "You extract high-level patterns, principles, and memorable knowledge from context. Focus on reusable insights and keep the result compact.",
        `Extract patterns and memorable knowledge from the following context about "${ctx.input}":

Previous analysis:
${previousStepsContext || "(none)"}

Related memories:
${memoryContext || "(none)"}

User profile hints:
${profileContext || "(none)"}${objectiveContext}${graphContext}

${readingTask}`
      );
    } catch (e) {
      warnings.push(`\u62BD\u8C61\u751F\u6210\u964D\u7EA7\uFF1A${(e == null ? void 0 : e.message) || String(e)}`);
      abstraction = this.buildFallbackAbstraction(ctx.input, allMemories, ctx.mode);
    }
    const prefix = ctx.mode.startsWith("reading-") ? "[Learning Workflow Notes]" : "[Pattern]";
    const storeContent = `${prefix} ${ctx.input.slice(0, 120)}
${abstraction}`;
    try {
      const { eventId } = await this.api.storeMemory(storeContent);
      storeCount++;
      stored.push({ eventId, content: storeContent });
    } catch (e) {
      warnings.push(`\u62BD\u8C61\u7ED3\u679C\u5B58\u50A8\u5931\u8D25\uFF1A${(e == null ? void 0 : e.message) || String(e)}`);
    }
    const summaryByMode = {
      "reading-understand": `\u5DF2\u751F\u6210\u63A5\u53E3\u5316\u8BFB\u61C2\u7ED3\u679C\uFF08\u53C2\u8003\u8BB0\u5FC6 ${allMemories.length} \u6761\uFF09`,
      "reading-connect": `\u5DF2\u751F\u6210\u524D\u7F6E\u8865\u9F50\u8DEF\u5F84\uFF08\u53C2\u8003\u8BB0\u5FC6 ${allMemories.length} \u6761\uFF09`,
      "reading-apply": `\u5DF2\u751F\u6210 A' \u538B\u7F29\u7ED3\u6784\uFF08\u53C2\u8003\u8BB0\u5FC6 ${allMemories.length} \u6761\uFF09`,
      "reading-advanced": `\u5DF2\u751F\u6210\u9AD8\u9636\u63D0\u95EE\u4E0E\u4E00\u6B65\u63A8\u65AD\uFF08\u53C2\u8003\u8BB0\u5FC6 ${allMemories.length} \u6761\uFF09`
    };
    const rawSummary = summaryByMode[ctx.mode] || `Extracted patterns from ${allMemories.length} memories`;
    const summary = warnings.length > 0 ? `${rawSummary}\uFF08\u964D\u7EA7\u6267\u884C\uFF0C\u544A\u8B66 ${warnings.length} \u6761\uFF09` : rawSummary;
    return {
      atom: this.name,
      summary,
      output: warnings.length > 0 ? `### \u7ED3\u6784\u5316\u7B14\u8BB0

${abstraction}

### \u6267\u884C\u544A\u8B66
${warnings.map((w, i) => `${i + 1}. ${w}`).join("\n")}` : `### \u7ED3\u6784\u5316\u7B14\u8BB0

${abstraction}`,
      memoriesRetrieved: allMemories,
      memoriesStored: stored,
      searchCount,
      storeCount,
      duration: Date.now() - start
    };
  }
};

// src/atoms/evaluate.ts
var EvaluateAtom = class {
  constructor(api, llm) {
    this.name = "evaluate";
    this.description = "Systematically score and compare options";
    this.api = api;
    this.llm = llm;
  }
  extractFallbackOptions(input, maxCount = 5) {
    const handles = extractInterfaceHandles(input, maxCount);
    if (handles.length >= 2)
      return handles;
    return ["\u9009\u9879A", "\u9009\u9879B"];
  }
  buildFallbackEvaluation(optionResults) {
    if (optionResults.length === 0) {
      return "\u6682\u65E0\u53EF\u8BC4\u4F30\u9009\u9879\u3002\u8BF7\u5148\u8865\u5145\u81F3\u5C11\u4E24\u4E2A\u5019\u9009\u9879\u540E\u91CD\u8BD5\u3002";
    }
    const lines = optionResults.map((or, index) => {
      const score = Math.min(10, Math.max(3, 4 + or.memories.length));
      const evidence = or.memories.slice(0, 2).map((m) => m.title).join("\u3001");
      return [
        `${index + 1}. ${or.option}`,
        `- \u7B80\u6D01\u5EA6\uFF1A${score}/10`,
        `- \u4F18\u52BF\uFF1A${or.memories.length > 0 ? "\u6709\u5386\u53F2\u8BB0\u5FC6\u652F\u6301\uFF0C\u4FBF\u4E8E\u6210\u4E3A\u7A33\u5B9A\u63A5\u53E3" : "\u8303\u56F4\u6E05\u6670\uFF0C\u4FBF\u4E8E\u5148\u8BD5"} `,
        `- \u98CE\u9669\uFF1A${or.memories.length > 0 ? "\u53EF\u80FD\u53D7\u5386\u53F2\u6837\u672C\u504F\u5DEE\u5F71\u54CD\uFF0C\u9700\u56DE\u539F\u6587\u6821\u6B63" : "\u7F3A\u5C11\u8BB0\u5FC6\u8BC1\u636E\uFF0C\u9700\u8981\u5FEB\u901F\u9A8C\u8BC1"} `,
        `- \u4F9D\u636E\uFF1A${evidence || "\u6682\u65E0\u76F8\u5173\u8BB0\u5FC6"}`
      ].join("\n");
    });
    const best = optionResults.reduce((acc, cur) => cur.memories.length > acc.memories.length ? cur : acc, optionResults[0]);
    return `${lines.join("\n\n")}

\u603B\u4F53\u5EFA\u8BAE\uFF1A\u5148\u6267\u884C\u300C${best.option}\u300D\u7684\u6700\u5C0F\u7248\u672C\u5E76\u8BB0\u5F55\u53CD\u9988\u3002`;
  }
  async execute(ctx) {
    const start = Date.now();
    let searchCount = 0;
    let storeCount = 0;
    const allMemories = [];
    const stored = [];
    const warnings = [];
    const previousContext = ctx.previousSteps.length > 0 ? `
Previous analysis:
${ctx.previousSteps.map((s) => `[${s.atom}]: ${s.summary}`).join("\n")}` : "";
    const objectiveContext = ctx.objective ? `
Current objective:
${ctx.objective}` : "";
    const profileContext = ctx.initialProfiles.length > 0 ? `
User profile hints:
${ctx.initialProfiles.slice(0, 3).map((p) => `- ${p.title}`).join("\n")}` : "";
    const graphContext = ctx.localGraph ? `
${summarizeInterfaceGraph(ctx.localGraph, 5, 3)}` : "";
    let options;
    try {
      const optionExtraction = await this.llm.complete(
        "Extract 2-5 distinct interface candidates, actions, or compact note units to evaluate from the text. Respond with ONLY a JSON array of strings.",
        `What are the key interface candidates / items to evaluate in:
"${ctx.input}"${previousContext}${objectiveContext}${profileContext}${graphContext}

JSON array of strings:`
      );
      const match = optionExtraction.match(/\[[\s\S]*\]/);
      options = match ? JSON.parse(match[0]) : this.extractFallbackOptions(ctx.input, 5);
    } catch (e) {
      warnings.push(`\u9009\u9879\u63D0\u53D6\u964D\u7EA7\uFF1A${(e == null ? void 0 : e.message) || String(e)}`);
      options = this.extractFallbackOptions(ctx.input, 5);
    }
    options = sanitizeGeneratedHandles(options, ctx.input, 5);
    if (options.length === 0) {
      options = this.extractFallbackOptions(ctx.input, 5);
    }
    const optionResults = [];
    for (const opt of options) {
      try {
        const memories = await this.api.searchMemories(opt, 5);
        searchCount++;
        allMemories.push(...memories);
        optionResults.push({ option: opt, memories });
      } catch (e) {
        warnings.push(`\u8BC4\u4F30\u68C0\u7D22\u5931\u8D25\uFF08${opt}\uFF09\uFF1A${(e == null ? void 0 : e.message) || String(e)}`);
        optionResults.push({ option: opt, memories: [] });
      }
    }
    const evaluationContext = optionResults.map(
      (or) => `Option: "${or.option}"
Related memories: ${or.memories.map((m) => `${m.title} (score: ${m.score})`).join(", ") || "none"}`
    ).join("\n\n");
    let evaluationTask = "For each option, judge whether it can serve as a good knowledge handle. Provide: 1. Concision 2. Precision 3. Coverage 4. Verdict. End with an overall recommendation.";
    if (ctx.mode === "reading-apply") {
      evaluationTask = "Judge which compressed note units are best suited to become the current A' interface set. Favor units that are short, accurate, and still cover the original material. End by naming the best compact version and one next action.";
    } else if (ctx.mode === "reading-advanced") {
      evaluationTask = "Select the best advanced extension path from the interface set. Output one prioritized question, one inference exercise, and two-level guidance (basic vs advanced). Prefer author stance, context, omission, and motive over generic questions.";
    }
    let evaluation = "";
    try {
      evaluation = await this.llm.complete(
        "You are a systematic evaluator. Judge each candidate as a knowledge interface handle. Optimize for brevity, precision, and relative completeness. Use evidence from memories when available.",
        `Evaluate these options for: "${ctx.input}"

${evaluationContext}${profileContext}${objectiveContext}${graphContext}

${evaluationTask}`
      );
    } catch (e) {
      warnings.push(`\u8BC4\u4F30\u751F\u6210\u964D\u7EA7\uFF1A${(e == null ? void 0 : e.message) || String(e)}`);
      evaluation = this.buildFallbackEvaluation(optionResults);
    }
    const prefix = ctx.mode === "reading-advanced" ? "[Advanced Guidance]" : ctx.mode === "reading-apply" ? "[Interface Quality Check]" : "[Evaluation]";
    const storeContent = `${prefix} ${ctx.input.slice(0, 120)}
Options: ${options.join(", ")}

${evaluation}`;
    try {
      const { eventId } = await this.api.storeMemory(storeContent);
      storeCount++;
      stored.push({ eventId, content: storeContent });
    } catch (e) {
      warnings.push(`\u8BC4\u4F30\u7ED3\u679C\u5B58\u50A8\u5931\u8D25\uFF1A${(e == null ? void 0 : e.message) || String(e)}`);
    }
    return {
      atom: this.name,
      summary: warnings.length > 0 ? `\u5DF2\u964D\u7EA7\u6267\u884C\uFF1A\u8BC4\u4F30 ${options.length} \u9879\uFF0C\u68C0\u7D22 ${searchCount} \u6B21\uFF0C\u544A\u8B66 ${warnings.length} \u6761` : ctx.mode === "reading-apply" ? `\u5DF2\u8BC4\u4F30 ${options.length} \u4E2A\u63A5\u53E3\u5019\u9009\uFF0C\u5B8C\u6210 ${searchCount} \u6B21\u68C0\u7D22` : ctx.mode === "reading-advanced" ? `\u5DF2\u7B5B\u9009\u4E00\u6B65\u5EF6\u4F38\u8DEF\u5F84\uFF0C\u5B8C\u6210 ${searchCount} \u6B21\u68C0\u7D22` : `\u5DF2\u8BC4\u4F30 ${options.length} \u9879\uFF0C\u5B8C\u6210 ${searchCount} \u6B21\u68C0\u7D22`,
      output: warnings.length > 0 ? `### \u9009\u9879\u8BC4\u4F30

${evaluation}

### \u6267\u884C\u544A\u8B66
${warnings.map((w, i) => `${i + 1}. ${w}`).join("\n")}` : `### \u9009\u9879\u8BC4\u4F30

${evaluation}`,
      memoriesRetrieved: allMemories,
      memoriesStored: stored,
      searchCount,
      storeCount,
      duration: Date.now() - start
    };
  }
};

// src/atoms/iterate.ts
var MAX_ITERATIONS = 3;
var IterateAtom = class {
  constructor(api, llm) {
    this.name = "iterate";
    this.description = "Use A to repeatedly correct A' so the compressed interface set stays aligned with the source";
    this.api = api;
    this.llm = llm;
  }
  buildFallbackFeedback(round) {
    return [
      `\u7B2C ${round} \u8F6E\u4E00\u81F4\u6027\u6821\u6B63\u53CD\u9988\uFF1A`,
      "- \u4FDD\u7559\u5F53\u524D A' \u7684\u6838\u5FC3\u63A5\u53E3\uFF0C\u4E0D\u8981\u6574\u4F53\u6539\u5199",
      "- \u5220\u53BB\u8FC7\u957F\u3001\u91CD\u590D\u6216\u65E0\u6CD5\u76F4\u63A5\u8C03\u7528\u7684\u8868\u8FBE",
      "- \u4F18\u5148\u4FEE\u6B63\u4E0E\u539F\u6587 A \u4E0D\u4E00\u81F4\u7684\u63CF\u8FF0",
      "- \u5982\u679C\u5220\u51CF\u540E\u4F1A\u5931\u771F\uFF0C\u5C31\u56DE\u8865\u4E00\u4E2A\u66F4\u51C6\u786E\u7684\u77ED\u63A5\u53E3"
    ].join("\n");
  }
  buildFallbackImprovedDraft(draft, feedback, round) {
    return `${draft}

[\u4E00\u81F4\u6027\u6821\u6B63 ${round}] \u6839\u636E\u4EE5\u4E0B\u53CD\u9988\u7EE7\u7EED\u4FEE\u6B63 A'\uFF0C\u8BA9\u5B83\u548C\u539F\u6587 A \u4FDD\u6301\u4E00\u81F4\uFF1A
${feedback}`;
  }
  async execute(ctx) {
    const start = Date.now();
    let searchCount = 0;
    let storeCount = 0;
    const allMemories = [];
    const stored = [];
    const warnings = [];
    const graphContext = ctx.localGraph ? `
${summarizeInterfaceGraph(ctx.localGraph, 5, 3)}` : "";
    let currentDraft = ctx.previousSteps.length > 0 ? ctx.previousSteps[ctx.previousSteps.length - 1].output : ctx.input;
    const iterations = [];
    const iterationSeed = buildInterfaceSearchQuery(ctx.input, { prefix: "\u6821\u6B63", maxHandles: 4 });
    for (let round = 1; round <= MAX_ITERATIONS; round++) {
      let iterMemories = [];
      try {
        iterMemories = await this.api.searchMemories(iterationSeed, 5);
        searchCount++;
        allMemories.push(...iterMemories);
      } catch (e) {
        warnings.push(`\u7B2C ${round} \u8F6E\u68C0\u7D22\u5931\u8D25\uFF1A${(e == null ? void 0 : e.message) || String(e)}`);
      }
      const pastIterations = iterMemories.length > 0 ? `
Past iteration insights:
${iterMemories.slice(0, 3).map((m) => `- ${m.title}`).join("\n")}` : "";
      let feedback = "";
      try {
        feedback = await this.llm.complete(
          "You are correcting an A' interface set against the original A. The goal is not to display differences, but to use the differences to repair A' until it stays semantically consistent with A. Do not rewrite broadly. Identify only the compact corrections needed to improve concision, precision, and consistency.",
          `Round ${round}/${MAX_ITERATIONS} consistency-correction review.

Original material (A):
"${ctx.input}"${graphContext}

Current draft (A'):
${currentDraft.slice(0, 1500)}${pastIterations}

Provide 2-3 specific corrections focused on:
1. semantic consistency with A
2. concision
3. precision
4. relative coverage`
        );
      } catch (e) {
        warnings.push(`\u7B2C ${round} \u8F6E\u53CD\u9988\u964D\u7EA7\uFF1A${(e == null ? void 0 : e.message) || String(e)}`);
        feedback = this.buildFallbackFeedback(round);
      }
      let improved = "";
      try {
        improved = await this.llm.complete(
          "You improve A' based on correction feedback. Keep the interface-set structure, make it shorter and sharper, and preserve only what is supported by A. The new A' must remain semantically aligned with A after compression.",
          `Correct the current A' based on the feedback.

Original material (A):
"${ctx.input}"${graphContext}

Current A':
${currentDraft.slice(0, 1500)}

Feedback:
${feedback}

Return:
1. a revised A' that stays consistent with A
2. it should be shorter if possible
3. do not output a difference report`
        );
      } catch (e) {
        warnings.push(`\u7B2C ${round} \u8F6E\u6539\u5199\u964D\u7EA7\uFF1A${(e == null ? void 0 : e.message) || String(e)}`);
        improved = this.buildFallbackImprovedDraft(currentDraft, feedback, round);
      }
      iterations.push({ round, feedback, improved });
      currentDraft = improved;
      const iterContent = `[A-A' Consistency Correction ${round}] ${ctx.input}
Feedback: ${feedback.slice(0, 200)}
Status: ${round === MAX_ITERATIONS ? "final" : "continuing"}`;
      try {
        const { eventId } = await this.api.storeMemory(iterContent);
        storeCount++;
        stored.push({ eventId, content: iterContent });
      } catch (e) {
        warnings.push(`\u7B2C ${round} \u8F6E\u5B58\u50A8\u5931\u8D25\uFF1A${(e == null ? void 0 : e.message) || String(e)}`);
      }
    }
    const output = iterations.map(
      (it) => `### \u7B2C ${it.round} \u8F6E\u4E00\u81F4\u6027\u6821\u6B63
**\u6821\u6B63\u4F9D\u636E\uFF1A** ${it.feedback}

**\u4FEE\u6B63\u540E\u7684 A'\uFF1A**
${it.improved}`
    ).join("\n\n---\n\n");
    return {
      atom: this.name,
      summary: warnings.length > 0 ? `\u5DF2\u964D\u7EA7\u6267\u884C\uFF1AA \u4E0E A' \u4E00\u81F4\u6027\u6821\u6B63 ${MAX_ITERATIONS} \u8F6E\uFF0C\u544A\u8B66 ${warnings.length} \u6761` : `\u5DF2\u5B8C\u6210 A \u4E0E A' \u4E00\u81F4\u6027\u6821\u6B63 ${MAX_ITERATIONS} \u8F6E`,
      output: warnings.length > 0 ? `### A \u4E0E A' \u4E00\u81F4\u6027\u6821\u6B63\uFF08${MAX_ITERATIONS} \u8F6E\uFF09

${output}

### \u6700\u7EC8 A'
${currentDraft}

### \u6267\u884C\u544A\u8B66
${warnings.map((w, i) => `${i + 1}. ${w}`).join("\n")}` : `### A \u4E0E A' \u4E00\u81F4\u6027\u6821\u6B63\uFF08${MAX_ITERATIONS} \u8F6E\uFF09

${output}

### \u6700\u7EC8 A'
${currentDraft}`,
      memoriesRetrieved: allMemories,
      memoriesStored: stored,
      searchCount,
      storeCount,
      duration: Date.now() - start
    };
  }
};

// src/views/atom-panel.ts
var import_obsidian3 = require("obsidian");
var ATOM_PANEL_VIEW = "three-library-atom-panel";
var ATOM_ICONS = {
  decompose: "split",
  associate: "link",
  transform: "rotate-cw",
  abstract: "layers",
  evaluate: "bar-chart-2",
  iterate: "refresh-cw"
};
var ATOM_LABELS = {
  decompose: "\u62C6\u89E3",
  associate: "\u5173\u8054",
  transform: "\u53D8\u6362",
  abstract: "\u62BD\u8C61",
  evaluate: "\u8BC4\u4F30",
  iterate: "\u8FED\u4EE3"
};
var STRATEGY_MODE_LABELS = {
  general: "\u81EA\u7531\u7F16\u6392",
  "reading-understand": "\u63A5\u53E3\u62C6\u6750",
  "reading-connect": "\u5DEE\u5206\u8BCA\u65AD",
  "reading-apply": "\u538B\u7F29\u4E0E\u6821\u6B63",
  "reading-advanced": "\u4E00\u6B65\u5EF6\u4F38"
};
var WORKFLOW_INPUT_BINDING_LABELS = {
  original: "\u539F\u6587",
  previous: "\u4E0A\u4E00\u6B65\u8F93\u51FA",
  "original+previous": "\u539F\u6587 + \u4E0A\u4E00\u6B65\u8F93\u51FA"
};
var AtomPanelView = class extends import_obsidian3.ItemView {
  constructor(leaf, plugin) {
    super(leaf);
    this.workflowButtons = /* @__PURE__ */ new Map();
    this.activeWorkspace = getDefaultWorkspaceProfiles()[0];
    this.activeWorkflowId = "general";
    this.selectedAtoms = [];
    this.selectedResultCardEl = null;
    this.selectionStoredInMemory = /* @__PURE__ */ new Set();
    this.runCounter = 0;
    this.isRunning = false;
    this.lastResult = null;
    this.lastExecutionWorkflow = getBuiltInWorkflowPreset("general");
    this.plugin = plugin;
  }
  getViewType() {
    return ATOM_PANEL_VIEW;
  }
  getDisplayText() {
    return "\u903B\u8F91\u539F\u5B50\u9762\u677F";
  }
  getIcon() {
    return "brain";
  }
  async onOpen() {
    const container = this.contentEl;
    container.empty();
    container.addClass("three-library-panel");
    const header = container.createDiv({ cls: "tl-header" });
    this.titleEl = header.createEl("h3", { text: this.activeWorkspace.panelTitle || this.activeWorkspace.name, cls: "tl-title" });
    const workspaceSection = container.createDiv({ cls: "tl-workspace-section" });
    workspaceSection.createEl("label", { text: "\u5DE5\u4F5C\u533A\uFF1A", cls: "tl-label" });
    const workspaceControls = workspaceSection.createDiv({ cls: "tl-workspace-controls" });
    this.workspaceSelectEl = workspaceControls.createEl("select", { cls: "tl-workspace-select" });
    this.workspaceSelectEl.addEventListener("change", () => {
      void this.switchWorkspace(this.workspaceSelectEl.value);
    });
    this.saveChainBtn = workspaceControls.createEl("button", {
      text: "\u4FDD\u5B58\u539F\u5B50\u94FE",
      cls: "tl-btn tl-btn-small"
    });
    this.saveChainBtn.addEventListener("click", () => void this.saveCurrentModeChain());
    this.editWorkspaceBtn = workspaceControls.createEl("button", {
      text: "\u7F16\u8F91\u5DE5\u4F5C\u533A",
      cls: "tl-btn tl-btn-small"
    });
    this.editWorkspaceBtn.addEventListener("click", () => void this.editCurrentWorkspace());
    this.duplicateWorkspaceBtn = workspaceControls.createEl("button", {
      text: "\u590D\u5236\u5DE5\u4F5C\u533A",
      cls: "tl-btn tl-btn-small"
    });
    this.duplicateWorkspaceBtn.addEventListener("click", () => void this.duplicateCurrentWorkspace());
    this.deleteWorkspaceBtn = workspaceControls.createEl("button", {
      text: "\u5220\u9664\u5DE5\u4F5C\u533A",
      cls: "tl-btn tl-btn-small"
    });
    this.deleteWorkspaceBtn.addEventListener("click", () => void this.deleteCurrentWorkspace());
    this.workspaceHintEl = workspaceSection.createDiv({ cls: "tl-workspace-hint" });
    const workflowSection = container.createDiv({ cls: "tl-workflow-section" });
    workflowSection.createEl("label", { text: "\u5DE5\u4F5C\u6D41\uFF1A", cls: "tl-label" });
    this.workflowGridEl = workflowSection.createDiv({ cls: "tl-workflow-grid" });
    this.workflowControlsEl = workflowSection.createDiv({ cls: "tl-workflow-controls" });
    this.addWorkflowBtn = this.workflowControlsEl.createEl("button", {
      text: "\u65B0\u589E\u6D41\u7A0B",
      cls: "tl-btn tl-btn-small"
    });
    this.addWorkflowBtn.addEventListener("click", () => void this.createWorkflowFromCurrent());
    this.editWorkflowBtn = this.workflowControlsEl.createEl("button", {
      text: "\u7F16\u8F91\u6D41\u7A0B",
      cls: "tl-btn tl-btn-small"
    });
    this.editWorkflowBtn.addEventListener("click", () => void this.editCurrentWorkflow());
    this.deleteWorkflowBtn = this.workflowControlsEl.createEl("button", {
      text: "\u5220\u9664\u6D41\u7A0B",
      cls: "tl-btn tl-btn-small"
    });
    this.deleteWorkflowBtn.addEventListener("click", () => void this.deleteCurrentWorkflow());
    this.setDefaultWorkflowBtn = this.workflowControlsEl.createEl("button", {
      text: "\u8BBE\u4E3A\u9ED8\u8BA4",
      cls: "tl-btn tl-btn-small"
    });
    this.setDefaultWorkflowBtn.addEventListener("click", () => void this.setCurrentWorkflowAsDefault());
    this.moveWorkflowUpBtn = this.workflowControlsEl.createEl("button", {
      text: "\u6D41\u7A0B\u4E0A\u79FB",
      cls: "tl-btn tl-btn-small"
    });
    this.moveWorkflowUpBtn.addEventListener("click", () => void this.moveCurrentWorkflow(-1));
    this.moveWorkflowDownBtn = this.workflowControlsEl.createEl("button", {
      text: "\u6D41\u7A0B\u4E0B\u79FB",
      cls: "tl-btn tl-btn-small"
    });
    this.moveWorkflowDownBtn.addEventListener("click", () => void this.moveCurrentWorkflow(1));
    this.workflowHintEl = workflowSection.createDiv({ cls: "tl-workflow-hint" });
    const inputSection = container.createDiv({ cls: "tl-input-section" });
    this.inputEl = inputSection.createEl("textarea", { cls: "tl-input" });
    const selectorSection = container.createDiv({ cls: "tl-selector-section" });
    selectorSection.createEl("label", { text: "\u539F\u5B50\u94FE\uFF08\u53EF\u624B\u52A8\u5FAE\u8C03\uFF09\uFF1A", cls: "tl-label" });
    this.atomChipsEl = selectorSection.createDiv({ cls: "tl-atom-chips" });
    const allAtoms = ["decompose", "associate", "transform", "abstract", "evaluate", "iterate"];
    for (const atom of allAtoms) {
      const chip = this.atomChipsEl.createDiv({ cls: "tl-chip", attr: { "data-atom": atom } });
      const iconSpan = chip.createSpan({ cls: "tl-chip-icon" });
      (0, import_obsidian3.setIcon)(iconSpan, ATOM_ICONS[atom]);
      chip.createSpan({ text: ATOM_LABELS[atom], cls: "tl-chip-label" });
      chip.addEventListener("click", () => this.toggleAtom(atom, chip));
    }
    this.orderEl = selectorSection.createDiv({ cls: "tl-order-display" });
    this.orderEl.style.display = "none";
    selectorSection.createDiv({
      cls: "tl-workflow-hint",
      text: "\u8C03\u5E8F\u65B9\u5F0F\uFF1A\u53D6\u6D88\u67D0\u4E2A\u539F\u5B50\u540E\u91CD\u65B0\u9009\u62E9\uFF0C\u5B83\u4F1A\u8FFD\u52A0\u5230\u672B\u5C3E\u5E76\u6539\u53D8\u6267\u884C\u987A\u5E8F\u3002"
    });
    const actions = container.createDiv({ cls: "tl-actions" });
    this.autoBtn = actions.createEl("button", { cls: "tl-btn tl-btn-primary" });
    this.autoBtn.addEventListener("click", () => this.runWorkspacePipeline());
    this.executeBtn = actions.createEl("button", { text: "\u6267\u884C\u5DF2\u9009\u539F\u5B50", cls: "tl-btn tl-btn-secondary" });
    this.executeBtn.addEventListener("click", () => this.runSelected());
    container.createDiv({
      cls: "tl-workflow-hint",
      text: "\u63D0\u793A\uFF1A\u70B9\u51FB\u4EFB\u610F\u6B65\u9AA4\u5361\u7247\u6216\u6700\u7EC8\u7ED3\u679C\u5361\u7247\uFF0C\u4F1A\u81EA\u52A8\u5199\u5165\u753B\u50CF\u8BB0\u5FC6\uFF1B\u540C\u4E00\u5F20\u5361\u7247\u6BCF\u8F6E\u53EA\u5199\u4E00\u6B21\u3002"
    });
    this.stepsEl = container.createDiv({ cls: "tl-steps" });
    this.statsEl = container.createDiv({ cls: "tl-stats" });
    this.resultEl = container.createDiv({ cls: "tl-result" });
    this.initializeWorkspace();
  }
  getWorkspaces() {
    if (Array.isArray(this.plugin.settings.workspaceProfiles) && this.plugin.settings.workspaceProfiles.length > 0) {
      return this.plugin.settings.workspaceProfiles;
    }
    this.plugin.settings.workspaceProfiles = getDefaultWorkspaceProfiles();
    this.plugin.settings.activeWorkspaceId = this.plugin.settings.workspaceProfiles[0].id;
    return this.plugin.settings.workspaceProfiles;
  }
  getWorkspaceWorkflows(workspace) {
    if (!Array.isArray(workspace.workflows) || workspace.workflows.length === 0) {
      workspace.workflows = [getBuiltInWorkflowPreset("general")];
      workspace.defaultWorkflowId = workspace.workflows[0].id;
    }
    return workspace.workflows;
  }
  getActiveWorkflowPreset() {
    var _a, _b;
    const workflows = this.getWorkspaceWorkflows(this.activeWorkspace);
    const preset = (_b = (_a = workflows.find((w) => w.id === this.activeWorkflowId)) != null ? _a : workflows[0]) != null ? _b : getBuiltInWorkflowPreset("general");
    return preset;
  }
  isStrategyMode(value) {
    return ["general", "reading-understand", "reading-connect", "reading-apply", "reading-advanced"].includes(value);
  }
  isWorkflowInputBinding(value) {
    return ["original", "previous", "original+previous"].includes(value);
  }
  updateWorkflowControlStates() {
    const workflows = this.getWorkspaceWorkflows(this.activeWorkspace);
    const hasActive = workflows.some((w) => w.id === this.activeWorkflowId);
    const activeIndex = workflows.findIndex((w) => w.id === this.activeWorkflowId);
    this.editWorkflowBtn.disabled = !hasActive;
    this.deleteWorkflowBtn.disabled = workflows.length <= 1 || !hasActive;
    this.setDefaultWorkflowBtn.disabled = !hasActive || this.activeWorkspace.defaultWorkflowId === this.activeWorkflowId;
    this.moveWorkflowUpBtn.disabled = !hasActive || activeIndex <= 0;
    this.moveWorkflowDownBtn.disabled = !hasActive || activeIndex < 0 || activeIndex >= workflows.length - 1;
  }
  initializeWorkspace() {
    var _a;
    const workspaces = this.getWorkspaces();
    this.workspaceSelectEl.empty();
    for (const workspace of workspaces) {
      this.workspaceSelectEl.createEl("option", {
        text: workspace.name,
        value: workspace.id
      });
    }
    const activeId = this.plugin.settings.activeWorkspaceId;
    const nextWorkspace = (_a = workspaces.find((w) => w.id === activeId)) != null ? _a : workspaces[0];
    this.workspaceSelectEl.value = nextWorkspace.id;
    this.applyWorkspace(nextWorkspace);
  }
  renderWorkflowButtons(workspace) {
    this.workflowGridEl.empty();
    this.workflowButtons.clear();
    const workflows = this.getWorkspaceWorkflows(workspace);
    for (const preset of workflows) {
      const isDefault = workspace.defaultWorkflowId === preset.id;
      const btn = this.workflowGridEl.createEl("button", {
        text: isDefault ? `\u2605 ${preset.shortLabel}` : preset.shortLabel,
        cls: "tl-workflow-btn",
        attr: { title: `${preset.label}\uFF1A${preset.description}` }
      });
      btn.toggleClass("tl-workflow-btn-default", isDefault);
      btn.addEventListener("click", () => this.applyWorkflow(preset.id));
      this.workflowButtons.set(preset.id, btn);
    }
  }
  applyWorkspace(workspace) {
    var _a, _b, _c;
    this.activeWorkspace = workspace;
    this.titleEl.textContent = workspace.panelTitle || workspace.name || "\u903B\u8F91\u539F\u5B50\u9762\u677F";
    const hints = [`${workspace.name}\uFF1A${workspace.description}`];
    if ((_a = workspace.methodObjective) == null ? void 0 : _a.trim()) {
      hints.push(`\u65B9\u6CD5\u76EE\u6807\uFF1A${workspace.methodObjective.trim()}`);
    }
    if ((_b = workspace.outputStyleGuide) == null ? void 0 : _b.trim()) {
      hints.push(`\u8F93\u51FA\u98CE\u683C\uFF1A${workspace.outputStyleGuide.trim()}`);
    }
    this.workspaceHintEl.textContent = hints.join(" | ");
    this.renderWorkflowButtons(workspace);
    const workflows = this.getWorkspaceWorkflows(workspace);
    const fallbackWorkflowId = ((_c = workflows[0]) == null ? void 0 : _c.id) || "general";
    const targetWorkflowId = workflows.some((w) => w.id === this.activeWorkflowId) ? this.activeWorkflowId : workflows.some((w) => w.id === workspace.defaultWorkflowId) ? workspace.defaultWorkflowId : fallbackWorkflowId;
    this.deleteWorkspaceBtn.disabled = this.getWorkspaces().length <= 1;
    this.applyWorkflow(targetWorkflowId);
    this.updateWorkflowControlStates();
  }
  async switchWorkspace(workspaceId) {
    const workspace = this.getWorkspaces().find((w) => w.id === workspaceId);
    if (!workspace)
      return;
    this.plugin.settings.activeWorkspaceId = workspace.id;
    await this.plugin.saveSettings();
    this.applyWorkspace(workspace);
    new import_obsidian3.Notice(`\u5DF2\u5207\u6362\u5230\u5DE5\u4F5C\u533A\uFF1A${workspace.name}`);
  }
  async saveCurrentModeChain() {
    if (!this.activeWorkspace)
      return;
    const workflow = this.getActiveWorkflowPreset();
    workflow.atoms = [...this.selectedAtoms];
    await this.plugin.saveSettings();
    new import_obsidian3.Notice(`\u5DF2\u4FDD\u5B58\u300C${this.activeWorkspace.name} / ${workflow.label}\u300D\u539F\u5B50\u94FE`);
  }
  async editCurrentWorkspace() {
    if (!this.activeWorkspace)
      return;
    const name = window.prompt("\u5DE5\u4F5C\u533A\u540D\u79F0\uFF1A", this.activeWorkspace.name);
    if (name === null)
      return;
    const panelTitle = window.prompt("\u9762\u677F\u6807\u9898\uFF1A", this.activeWorkspace.panelTitle || this.activeWorkspace.name);
    if (panelTitle === null)
      return;
    const description = window.prompt("\u5DE5\u4F5C\u533A\u63CF\u8FF0\uFF1A", this.activeWorkspace.description);
    if (description === null)
      return;
    const methodObjective = window.prompt(
      "\u65B9\u6CD5\u76EE\u6807\uFF08\u53EF\u7559\u7A7A\uFF09\uFF1A",
      this.activeWorkspace.methodObjective || ""
    );
    if (methodObjective === null)
      return;
    const outputStyleGuide = window.prompt(
      "\u8F93\u51FA\u98CE\u683C\uFF08\u53EF\u7559\u7A7A\uFF09\uFF1A",
      this.activeWorkspace.outputStyleGuide || ""
    );
    if (outputStyleGuide === null)
      return;
    const setCurrentAsDefault = window.confirm("\u662F\u5426\u5C06\u201C\u5F53\u524D\u9009\u4E2D\u6D41\u7A0B\u201D\u8BBE\u4E3A\u6B64\u5DE5\u4F5C\u533A\u9ED8\u8BA4\u6D41\u7A0B\uFF1F");
    this.activeWorkspace.name = name.trim() || this.activeWorkspace.name;
    this.activeWorkspace.panelTitle = panelTitle.trim() || this.activeWorkspace.panelTitle;
    this.activeWorkspace.description = description.trim() || this.activeWorkspace.description;
    this.activeWorkspace.methodObjective = methodObjective.trim();
    this.activeWorkspace.outputStyleGuide = outputStyleGuide.trim();
    if (setCurrentAsDefault) {
      this.activeWorkspace.defaultWorkflowId = this.activeWorkflowId;
    }
    await this.plugin.saveSettings();
    this.initializeWorkspace();
    new import_obsidian3.Notice(`\u5DF2\u66F4\u65B0\u5DE5\u4F5C\u533A\uFF1A${this.activeWorkspace.name}`);
  }
  buildWorkflowId(base) {
    const normalized = base.toLowerCase().replace(/[^a-z0-9\u4e00-\u9fa5]+/g, "-").replace(/^-+|-+$/g, "").slice(0, 32);
    const prefix = normalized || "workflow";
    return `${prefix}-${Date.now()}`;
  }
  async createWorkflowFromCurrent() {
    const current = this.getActiveWorkflowPreset();
    const label = window.prompt("\u65B0\u6D41\u7A0B\u540D\u79F0\uFF1A", `${current.label} \u526F\u672C`);
    if (!label)
      return;
    const shortLabel = window.prompt("\u65B0\u6D41\u7A0B\u77ED\u540D\u79F0\uFF08\u6309\u94AE\u663E\u793A\uFF09\uFF1A", current.shortLabel);
    if (!shortLabel)
      return;
    const description = window.prompt("\u6D41\u7A0B\u63CF\u8FF0\uFF1A", current.description);
    if (description === null)
      return;
    const strategyInput = window.prompt(
      `\u7B56\u7565\u7C7B\u578B\uFF08${Object.keys(STRATEGY_MODE_LABELS).join(" / ")}\uFF09\uFF1A`,
      current.strategyMode
    );
    if (!strategyInput)
      return;
    const strategyMode = strategyInput.trim();
    if (!this.isStrategyMode(strategyMode)) {
      new import_obsidian3.Notice("\u7B56\u7565\u7C7B\u578B\u65E0\u6548\uFF0C\u521B\u5EFA\u53D6\u6D88");
      return;
    }
    const objective = window.prompt("\u6D41\u7A0B\u76EE\u6807\uFF08\u53EF\u7559\u7A7A\uFF09\uFF1A", current.objective || "");
    if (objective === null)
      return;
    const synthesisGuidance = window.prompt("\u8F93\u51FA\u6307\u5BFC\uFF08\u53EF\u7559\u7A7A\uFF09\uFF1A", current.synthesisGuidance || "");
    if (synthesisGuidance === null)
      return;
    const inputBindingInput = window.prompt(
      "\u9636\u6BB5\u8F93\u5165\u6765\u6E90\uFF08original / previous / original+previous\uFF09\uFF1A",
      current.inputBinding || "previous"
    );
    if (inputBindingInput === null)
      return;
    const inputBinding = inputBindingInput.trim();
    if (!this.isWorkflowInputBinding(inputBinding)) {
      new import_obsidian3.Notice("\u8F93\u5165\u6765\u6E90\u65E0\u6548\uFF0C\u521B\u5EFA\u53D6\u6D88");
      return;
    }
    const stageContract = window.prompt("\u9636\u6BB5\u5951\u7EA6\uFF08\u53EF\u7559\u7A7A\uFF09\uFF1A", current.stageContract || "");
    if (stageContract === null)
      return;
    const inputPlaceholder = window.prompt("\u8F93\u5165\u6846\u5360\u4F4D\u6587\u6848\uFF1A", current.inputPlaceholder || "");
    if (inputPlaceholder === null)
      return;
    const newWorkflow = {
      ...cloneWorkflowPreset(current),
      id: this.buildWorkflowId(label),
      label: label.trim(),
      shortLabel: shortLabel.trim(),
      description: description.trim() || current.description,
      strategyMode,
      atoms: [...this.selectedAtoms],
      actionLabel: `\u6267\u884C\uFF1A${label.trim()}`,
      objective: objective.trim() || "",
      synthesisGuidance: synthesisGuidance.trim() || "",
      inputBinding,
      stageContract: stageContract.trim() || "",
      inputPlaceholder: inputPlaceholder.trim() || current.inputPlaceholder
    };
    this.activeWorkspace.workflows.push(newWorkflow);
    this.activeWorkflowId = newWorkflow.id;
    await this.plugin.saveSettings();
    this.applyWorkspace(this.activeWorkspace);
    new import_obsidian3.Notice(`\u5DF2\u65B0\u589E\u6D41\u7A0B\uFF1A${newWorkflow.label}`);
  }
  async editCurrentWorkflow() {
    const workflow = this.getActiveWorkflowPreset();
    const label = window.prompt("\u6D41\u7A0B\u540D\u79F0\uFF1A", workflow.label);
    if (label === null)
      return;
    const shortLabel = window.prompt("\u77ED\u540D\u79F0\uFF08\u6309\u94AE\u663E\u793A\uFF09\uFF1A", workflow.shortLabel);
    if (shortLabel === null)
      return;
    const description = window.prompt("\u6D41\u7A0B\u63CF\u8FF0\uFF1A", workflow.description);
    if (description === null)
      return;
    const strategyInput = window.prompt(
      `\u7B56\u7565\u7C7B\u578B\uFF08${Object.keys(STRATEGY_MODE_LABELS).join(" / ")}\uFF09\uFF1A`,
      workflow.strategyMode
    );
    if (strategyInput === null)
      return;
    const strategyMode = strategyInput.trim();
    if (!this.isStrategyMode(strategyMode)) {
      new import_obsidian3.Notice("\u7B56\u7565\u7C7B\u578B\u65E0\u6548\uFF0C\u672A\u4FDD\u5B58");
      return;
    }
    const actionLabel = window.prompt("\u6267\u884C\u6309\u94AE\u6587\u6848\uFF1A", workflow.actionLabel);
    if (actionLabel === null)
      return;
    const objective = window.prompt("\u6D41\u7A0B\u76EE\u6807\uFF08\u53EF\u7559\u7A7A\uFF09\uFF1A", workflow.objective || "");
    if (objective === null)
      return;
    const synthesisGuidance = window.prompt("\u8F93\u51FA\u6307\u5BFC\uFF08\u53EF\u7559\u7A7A\uFF09\uFF1A", workflow.synthesisGuidance || "");
    if (synthesisGuidance === null)
      return;
    const inputBindingInput = window.prompt(
      "\u9636\u6BB5\u8F93\u5165\u6765\u6E90\uFF08original / previous / original+previous\uFF09\uFF1A",
      workflow.inputBinding || "previous"
    );
    if (inputBindingInput === null)
      return;
    const inputBinding = inputBindingInput.trim();
    if (!this.isWorkflowInputBinding(inputBinding)) {
      new import_obsidian3.Notice("\u8F93\u5165\u6765\u6E90\u65E0\u6548\uFF0C\u672A\u4FDD\u5B58");
      return;
    }
    const stageContract = window.prompt("\u9636\u6BB5\u5951\u7EA6\uFF08\u53EF\u7559\u7A7A\uFF09\uFF1A", workflow.stageContract || "");
    if (stageContract === null)
      return;
    const inputPlaceholder = window.prompt("\u8F93\u5165\u6846\u5360\u4F4D\u6587\u6848\uFF1A", workflow.inputPlaceholder || "");
    if (inputPlaceholder === null)
      return;
    workflow.label = label.trim() || workflow.label;
    workflow.shortLabel = shortLabel.trim() || workflow.shortLabel;
    workflow.description = description.trim() || workflow.description;
    workflow.strategyMode = strategyMode;
    workflow.actionLabel = actionLabel.trim() || workflow.actionLabel;
    workflow.atoms = [...this.selectedAtoms];
    workflow.objective = objective.trim() || "";
    workflow.synthesisGuidance = synthesisGuidance.trim() || "";
    workflow.inputBinding = inputBinding;
    workflow.stageContract = stageContract.trim() || "";
    workflow.inputPlaceholder = inputPlaceholder.trim() || workflow.inputPlaceholder;
    await this.plugin.saveSettings();
    this.applyWorkspace(this.activeWorkspace);
    new import_obsidian3.Notice(`\u5DF2\u66F4\u65B0\u6D41\u7A0B\uFF1A${workflow.label}`);
  }
  async deleteCurrentWorkflow() {
    const workflows = this.getWorkspaceWorkflows(this.activeWorkspace);
    if (workflows.length <= 1) {
      new import_obsidian3.Notice("\u81F3\u5C11\u4FDD\u7559\u4E00\u4E2A\u6D41\u7A0B");
      return;
    }
    const workflow = this.getActiveWorkflowPreset();
    const confirmed = window.confirm(`\u786E\u8BA4\u5220\u9664\u6D41\u7A0B\u300C${workflow.label}\u300D\u5417\uFF1F`);
    if (!confirmed)
      return;
    this.activeWorkspace.workflows = workflows.filter((w) => w.id !== workflow.id);
    if (this.activeWorkspace.defaultWorkflowId === workflow.id) {
      this.activeWorkspace.defaultWorkflowId = this.activeWorkspace.workflows[0].id;
    }
    this.activeWorkflowId = this.activeWorkspace.defaultWorkflowId || this.activeWorkspace.workflows[0].id;
    await this.plugin.saveSettings();
    this.applyWorkspace(this.activeWorkspace);
    new import_obsidian3.Notice(`\u5DF2\u5220\u9664\u6D41\u7A0B\uFF1A${workflow.label}`);
  }
  async setCurrentWorkflowAsDefault() {
    const workflow = this.getActiveWorkflowPreset();
    this.activeWorkspace.defaultWorkflowId = workflow.id;
    await this.plugin.saveSettings();
    this.renderWorkflowButtons(this.activeWorkspace);
    this.applyWorkflow(workflow.id);
    new import_obsidian3.Notice(`\u5DF2\u8BBE\u9ED8\u8BA4\u6D41\u7A0B\uFF1A${workflow.label}`);
  }
  async moveCurrentWorkflow(offset) {
    const workflows = this.getWorkspaceWorkflows(this.activeWorkspace);
    const currentIndex = workflows.findIndex((w) => w.id === this.activeWorkflowId);
    if (currentIndex < 0)
      return;
    const targetIndex = currentIndex + offset;
    if (targetIndex < 0 || targetIndex >= workflows.length)
      return;
    const [current] = workflows.splice(currentIndex, 1);
    workflows.splice(targetIndex, 0, current);
    this.activeWorkspace.workflows = workflows;
    await this.plugin.saveSettings();
    this.renderWorkflowButtons(this.activeWorkspace);
    this.applyWorkflow(current.id);
    new import_obsidian3.Notice(`\u5DF2\u8C03\u6574\u6D41\u7A0B\u987A\u5E8F\uFF1A${current.label}`);
  }
  cloneWorkflows(workflows) {
    if (!Array.isArray(workflows))
      return [];
    return workflows.map((workflow) => cloneWorkflowPreset(workflow));
  }
  async duplicateCurrentWorkspace() {
    const name = window.prompt("\u8BF7\u8F93\u5165\u65B0\u5DE5\u4F5C\u533A\u540D\u79F0\uFF1A", `${this.activeWorkspace.name} \u526F\u672C`);
    if (!name)
      return;
    const clonedWorkspace = {
      id: `workspace_${Date.now()}`,
      name,
      description: this.activeWorkspace.description,
      panelTitle: this.activeWorkspace.panelTitle,
      methodObjective: this.activeWorkspace.methodObjective,
      outputStyleGuide: this.activeWorkspace.outputStyleGuide,
      workflows: this.cloneWorkflows(this.activeWorkspace.workflows),
      defaultWorkflowId: this.activeWorkspace.defaultWorkflowId
    };
    this.plugin.settings.workspaceProfiles.push(clonedWorkspace);
    this.plugin.settings.activeWorkspaceId = clonedWorkspace.id;
    await this.plugin.saveSettings();
    this.initializeWorkspace();
    new import_obsidian3.Notice(`\u5DF2\u521B\u5EFA\u5DE5\u4F5C\u533A\uFF1A${clonedWorkspace.name}`);
  }
  async deleteCurrentWorkspace() {
    const workspaces = this.getWorkspaces();
    if (workspaces.length <= 1) {
      new import_obsidian3.Notice("\u81F3\u5C11\u4FDD\u7559\u4E00\u4E2A\u5DE5\u4F5C\u533A");
      return;
    }
    const confirmed = window.confirm(`\u786E\u8BA4\u5220\u9664\u5DE5\u4F5C\u533A\u300C${this.activeWorkspace.name}\u300D\u5417\uFF1F`);
    if (!confirmed)
      return;
    this.plugin.settings.workspaceProfiles = workspaces.filter((w) => w.id !== this.activeWorkspace.id);
    this.plugin.settings.activeWorkspaceId = this.plugin.settings.workspaceProfiles[0].id;
    await this.plugin.saveSettings();
    this.initializeWorkspace();
    new import_obsidian3.Notice("\u5DE5\u4F5C\u533A\u5DF2\u5220\u9664");
  }
  getDefaultAtoms() {
    return this.plugin.settings.defaultAtoms.split(",").map((s) => s.trim()).filter(Boolean);
  }
  applyWorkflow(workflowId) {
    var _a, _b, _c;
    const workflows = this.getWorkspaceWorkflows(this.activeWorkspace);
    const preset = (_b = (_a = workflows.find((w) => w.id === workflowId)) != null ? _a : workflows[0]) != null ? _b : getBuiltInWorkflowPreset("general");
    this.activeWorkflowId = preset.id;
    const isDefault = this.activeWorkspace.defaultWorkflowId === preset.id;
    for (const [id, button] of this.workflowButtons.entries()) {
      button.toggleClass("tl-workflow-btn-active", id === preset.id);
    }
    const strategyLabel = STRATEGY_MODE_LABELS[preset.strategyMode] || preset.strategyMode;
    const bindingLabel = WORKFLOW_INPUT_BINDING_LABELS[preset.inputBinding || "previous"];
    const contractHint = ((_c = preset.stageContract) == null ? void 0 : _c.trim()) ? ` \u9636\u6BB5\u5951\u7EA6\uFF1A${preset.stageContract.trim()}` : "";
    this.workflowHintEl.textContent = `${isDefault ? "\u9ED8\u8BA4\u6D41\u7A0B | " : ""}${preset.label}\uFF08\u7B56\u7565\uFF1A${strategyLabel} | \u8F93\u5165\uFF1A${bindingLabel}\uFF09${preset.description}${contractHint}`;
    this.inputEl.placeholder = preset.inputPlaceholder;
    this.autoBtn.textContent = "\u4E00\u952E\u6267\u884C\u5DE5\u4F5C\u533A\u6D41\u7A0B";
    if (preset.atoms && preset.atoms.length > 0) {
      this.setSelectedAtoms(preset.atoms);
    } else {
      this.setSelectedAtoms(this.getDefaultAtoms());
    }
    this.updateWorkflowControlStates();
  }
  setSelectedAtoms(atoms) {
    this.selectedAtoms = [...atoms];
    this.syncAtomChipState();
    this.updateChipNumbers();
  }
  toggleAtom(atom, chip) {
    const idx = this.selectedAtoms.indexOf(atom);
    if (idx >= 0) {
      this.selectedAtoms.splice(idx, 1);
      chip.removeClass("tl-chip-active");
    } else {
      this.selectedAtoms.push(atom);
      chip.addClass("tl-chip-active");
    }
    this.syncAtomChipState();
    this.updateChipNumbers();
  }
  syncAtomChipState() {
    this.atomChipsEl.querySelectorAll(".tl-chip").forEach((chip) => chip.removeClass("tl-chip-active"));
    for (const atom of this.selectedAtoms) {
      const chip = this.atomChipsEl.querySelector(`[data-atom="${atom}"]`);
      chip == null ? void 0 : chip.addClass("tl-chip-active");
    }
  }
  updateChipNumbers() {
    this.atomChipsEl.querySelectorAll(".tl-chip-order").forEach((el) => el.remove());
    for (let i = 0; i < this.selectedAtoms.length; i++) {
      const chip = this.atomChipsEl.querySelector(`[data-atom="${this.selectedAtoms[i]}"]`);
      if (chip) {
        const badge = chip.createSpan({ text: String(i + 1), cls: "tl-chip-order" });
        chip.prepend(badge);
      }
    }
    if (this.selectedAtoms.length > 0) {
      this.orderEl.textContent = "\u6267\u884C\u987A\u5E8F\uFF1A" + this.selectedAtoms.map((a) => ATOM_LABELS[a]).join(" -> ");
      this.orderEl.style.display = "";
    } else {
      this.orderEl.style.display = "none";
    }
  }
  setInput(text) {
    if (this.inputEl) {
      this.inputEl.value = text;
    }
  }
  buildExecutionHints(workflow) {
    var _a, _b;
    const mergedObjective = [
      workflow.stageContract ? `Stage contract:
${workflow.stageContract}` : "",
      workflow.objective ? `Workflow objective:
${workflow.objective}` : "",
      ((_a = this.activeWorkspace.methodObjective) == null ? void 0 : _a.trim()) ? `Workspace objective:
${this.activeWorkspace.methodObjective.trim()}` : ""
    ].filter(Boolean).join("\n\n");
    const mergedSynthesisGuidance = [
      workflow.synthesisGuidance ? `Workflow guidance:
${workflow.synthesisGuidance}` : "",
      ((_b = this.activeWorkspace.outputStyleGuide) == null ? void 0 : _b.trim()) ? `Workspace output style:
${this.activeWorkspace.outputStyleGuide.trim()}` : ""
    ].filter(Boolean).join("\n\n");
    return {
      mergedObjective: mergedObjective || void 0,
      mergedSynthesisGuidance: mergedSynthesisGuidance || void 0
    };
  }
  buildRuntimeBlockedNotice(message) {
    if (/\b401\b|\b403\b|鉴权失败/.test(message)) {
      return "\u8FDE\u63A5\u672A\u901A\u8FC7\uFF0C\u5DF2\u963B\u6B62\u6267\u884C\u3002\u8BF7\u5148\u68C0\u67E5 API Key\u3001\u7528\u6237/\u5206\u7EC4\u4FE1\u606F\u548C\u6A21\u578B\u6743\u9650\u3002";
    }
    return `\u8FDE\u63A5\u672A\u901A\u8FC7\uFF0C\u5DF2\u963B\u6B62\u6267\u884C\u3002${message}`;
  }
  buildStageInput(workflow, originalInput, previousOutput) {
    const binding = workflow.inputBinding || "previous";
    const hasPrevious = !!previousOutput.trim();
    if (binding === "original") {
      return originalInput;
    }
    if (binding === "previous") {
      return hasPrevious ? previousOutput : originalInput;
    }
    if (!hasPrevious) {
      return originalInput;
    }
    return [
      "[Original Material]",
      originalInput,
      "",
      "[Current Notes]",
      previousOutput
    ].join("\n");
  }
  async resolveWorkflowAtomsForInput(workflow, stageInput) {
    if (workflow.atoms && workflow.atoms.length > 0) {
      return [...workflow.atoms];
    }
    try {
      const selected = await this.plugin.atomSelector.selectAtoms(stageInput);
      if (selected.length > 0) {
        return selected;
      }
    } catch (e) {
      console.error("[LogicAtom] \u52A8\u6001\u9009\u62E9\u539F\u5B50\u5931\u8D25\uFF0C\u6539\u7528\u9ED8\u8BA4\u539F\u5B50\u94FE\uFF1A", e);
    }
    return this.getDefaultAtoms();
  }
  async runWorkspacePipeline() {
    var _a, _b, _c;
    const input = this.inputEl.value.trim();
    if (!input || this.isRunning)
      return;
    const workflows = this.getWorkspaceWorkflows(this.activeWorkspace);
    if (workflows.length === 0) {
      new import_obsidian3.Notice("\u5F53\u524D\u5DE5\u4F5C\u533A\u6CA1\u6709\u53EF\u6267\u884C\u6D41\u7A0B");
      return;
    }
    this.setRunning(true);
    this.runCounter++;
    this.clearResults();
    try {
      const health = await this.plugin.ensureRuntimeReady(false);
      if (!health.ok) {
        this.addStep("error", health.message);
        new import_obsidian3.Notice(this.buildRuntimeBlockedNotice(health.message));
        return;
      }
      const originalInput = input;
      let previousOutput = "";
      const stageResults = [];
      for (let i = 0; i < workflows.length; i++) {
        const workflow = workflows[i];
        const stageInput = this.buildStageInput(workflow, originalInput, previousOutput);
        const atoms = await this.resolveWorkflowAtomsForInput(workflow, stageInput);
        this.lastExecutionWorkflow = workflow;
        this.addStep("system", `\u4E32\u8054\u9636\u6BB5 ${i + 1}/${workflows.length}\uFF1A${workflow.label}`);
        this.addStep("system", `\u9636\u6BB5\u8F93\u5165\u6765\u6E90\uFF1A${WORKFLOW_INPUT_BINDING_LABELS[workflow.inputBinding || "previous"]}`);
        this.addStep("system", `\u9636\u6BB5\u539F\u5B50\u94FE\uFF1A${atoms.map((a) => ATOM_LABELS[a]).join(" -> ")}`);
        if ((_a = workflow.stageContract) == null ? void 0 : _a.trim()) {
          this.addStep("system", `\u9636\u6BB5\u5951\u7EA6\uFF1A${workflow.stageContract.trim().slice(0, 140)}`);
        }
        if ((_b = this.activeWorkspace.methodObjective) == null ? void 0 : _b.trim()) {
          this.addStep("system", `\u5DE5\u4F5C\u533A\u76EE\u6807\u5DF2\u6CE8\u5165\uFF1A${this.activeWorkspace.methodObjective.trim().slice(0, 120)}`);
        }
        if ((_c = this.activeWorkspace.outputStyleGuide) == null ? void 0 : _c.trim()) {
          this.addStep("system", `\u8F93\u51FA\u98CE\u683C\u5DF2\u6CE8\u5165\uFF1A${this.activeWorkspace.outputStyleGuide.trim().slice(0, 120)}`);
        }
        const { mergedObjective, mergedSynthesisGuidance } = this.buildExecutionHints(workflow);
        const result = await this.plugin.atomEngine.executeChain(
          stageInput,
          atoms,
          (step, index, total) => {
            this.renderStep(step, index + 1, total);
          },
          {
            mode: workflow.strategyMode,
            objective: mergedObjective,
            synthesisGuidance: mergedSynthesisGuidance,
            workspaceName: this.activeWorkspace.name,
            workflowLabel: workflow.label,
            persistExecutionPlan: true
          }
        );
        stageResults.push({ workflow, result, atoms });
        this.renderPipelineStageResult(result, workflow, i + 1, workflows.length);
        previousOutput = result.finalOutput;
      }
      this.renderPipelineSummary(stageResults);
    } catch (e) {
      this.addStep("error", `\u9519\u8BEF\uFF1A${e instanceof Error ? e.message : String(e)}`);
    } finally {
      this.setRunning(false);
    }
  }
  async runAuto() {
    await this.runWorkspacePipeline();
  }
  async runSelected() {
    const input = this.inputEl.value.trim();
    if (!input || this.isRunning || this.selectedAtoms.length === 0)
      return;
    const workflow = this.getActiveWorkflowPreset();
    this.lastExecutionWorkflow = workflow;
    this.setRunning(true);
    this.runCounter++;
    this.clearResults();
    const health = await this.plugin.ensureRuntimeReady(false);
    if (!health.ok) {
      this.addStep("error", health.message);
      new import_obsidian3.Notice(this.buildRuntimeBlockedNotice(health.message));
      this.setRunning(false);
      return;
    }
    await this.executeChain(input, [...this.selectedAtoms], workflow);
    this.setRunning(false);
  }
  async executeChain(input, atoms, workflow) {
    var _a, _b;
    this.addStep("system", `\u5F00\u59CB\u6267\u884C\u94FE\uFF1A${atoms.map((a) => ATOM_LABELS[a]).join(" -> ")}`);
    if ((_a = this.activeWorkspace.methodObjective) == null ? void 0 : _a.trim()) {
      this.addStep("system", `\u5DE5\u4F5C\u533A\u76EE\u6807\u5DF2\u6CE8\u5165\uFF1A${this.activeWorkspace.methodObjective.trim().slice(0, 120)}`);
    }
    if ((_b = this.activeWorkspace.outputStyleGuide) == null ? void 0 : _b.trim()) {
      this.addStep("system", `\u8F93\u51FA\u98CE\u683C\u5DF2\u6CE8\u5165\uFF1A${this.activeWorkspace.outputStyleGuide.trim().slice(0, 120)}`);
    }
    const { mergedObjective, mergedSynthesisGuidance } = this.buildExecutionHints(workflow);
    try {
      const result = await this.plugin.atomEngine.executeChain(
        input,
        atoms,
        (step, index, total) => {
          this.renderStep(step, index + 1, total);
        },
        {
          mode: workflow.strategyMode,
          objective: mergedObjective,
          synthesisGuidance: mergedSynthesisGuidance,
          workspaceName: this.activeWorkspace.name,
          workflowLabel: workflow.label,
          persistExecutionPlan: true
        }
      );
      this.renderFinalResult(result, workflow);
    } catch (e) {
      this.addStep("error", `\u6267\u884C\u5931\u8D25\uFF1A${e instanceof Error ? e.message : String(e)}`);
    }
  }
  renderPipelineStageResult(result, workflow, stageIndex, stageTotal) {
    if (stageIndex === 1) {
      this.resultEl.empty();
    }
    const stageId = `run-${this.runCounter}-pipeline-stage-${stageIndex}`;
    const stageCard = this.resultEl.createDiv({ cls: "tl-result-card" });
    const header = stageCard.createDiv({ cls: "tl-result-header" });
    const title = `\u9636\u6BB5 ${stageIndex}/${stageTotal}\uFF1A${workflow.label}`;
    header.createEl("h4", { text: title });
    const copyBtn = header.createEl("button", { text: "\u590D\u5236", cls: "tl-btn tl-btn-small" });
    copyBtn.addEventListener("click", () => {
      navigator.clipboard.writeText(result.finalOutput);
      copyBtn.textContent = "\u5DF2\u590D\u5236";
      setTimeout(() => {
        copyBtn.textContent = "\u590D\u5236";
      }, 1800);
    });
    const insertBtn = header.createEl("button", { text: "\u63D2\u5165\u7B14\u8BB0", cls: "tl-btn tl-btn-small" });
    insertBtn.addEventListener("click", () => this.insertToActiveNote(result, workflow));
    const stageMeta = stageCard.createDiv({ cls: "tl-step-summary" });
    stageMeta.textContent = `\u539F\u5B50\u94FE\uFF1A${result.atomNames.map((a) => ATOM_LABELS[a]).join(" -> ")} | \u641C\u7D22 ${result.totalSearches} \u6B21 | \u5B58\u50A8 ${result.totalStores} \u6B21`;
    const cards = this.extractPointCards(result.finalOutput);
    this.renderPointCards(stageCard, cards, {
      scope: "final",
      baseCardId: stageId,
      titlePrefix: title
    });
    const rawDetails = stageCard.createEl("details", { cls: "tl-step-details" });
    rawDetails.createEl("summary", { text: "\u67E5\u770B\u8BE5\u9636\u6BB5\u5B8C\u6574\u8F93\u51FA" });
    const rawEl = rawDetails.createDiv({ cls: "tl-result-content" });
    rawEl.innerHTML = this.simpleMarkdown(result.finalOutput);
    stageCard.addEventListener("click", (evt) => {
      const target = evt.target;
      if (this.shouldIgnoreCardSelectionTarget(target))
        return;
      this.selectResultCard(stageCard, {
        cardId: stageId,
        scope: "final",
        title,
        summary: `\u9636\u6BB5 ${stageIndex} \u8F93\u51FA`,
        output: result.finalOutput
      });
    });
  }
  renderPipelineSummary(stageResults) {
    const totalSearches = stageResults.reduce((sum, s) => sum + s.result.totalSearches, 0);
    const totalStores = stageResults.reduce((sum, s) => sum + s.result.totalStores, 0);
    const totalDuration = stageResults.reduce((sum, s) => sum + s.result.totalDuration, 0);
    const finalOutput = stageResults.length > 0 ? stageResults[stageResults.length - 1].result.finalOutput : "";
    this.statsEl.empty();
    this.statsEl.createSpan({
      text: `\u4E32\u8054\u5B8C\u6210\uFF1A${stageResults.length} \u4E2A\u6D41\u7A0B | \u641C\u7D22 ${totalSearches} \u6B21 | \u5B58\u50A8 ${totalStores} \u6B21 | \u8017\u65F6 ${(totalDuration / 1e3).toFixed(1)} \u79D2`
    });
    const summaryId = `run-${this.runCounter}-pipeline-summary`;
    const summaryCard = this.resultEl.createDiv({ cls: "tl-result-card" });
    const header = summaryCard.createDiv({ cls: "tl-result-header" });
    header.createEl("h4", { text: "\u4E32\u8054\u6D41\u7A0B\u603B\u89C8\uFF08\u6700\u7EC8\u4EA7\u7269\uFF09" });
    const copyBtn = header.createEl("button", { text: "\u590D\u5236\u6700\u7EC8\u7ED3\u679C", cls: "tl-btn tl-btn-small" });
    copyBtn.addEventListener("click", () => {
      navigator.clipboard.writeText(finalOutput);
      copyBtn.textContent = "\u5DF2\u590D\u5236";
      setTimeout(() => {
        copyBtn.textContent = "\u590D\u5236\u6700\u7EC8\u7ED3\u679C";
      }, 1800);
    });
    const summaryBody = summaryCard.createDiv({ cls: "tl-step-summary" });
    summaryBody.innerHTML = this.simpleMarkdown(stageResults.map(
      (stage, index) => `${index + 1}. ${stage.workflow.label}\uFF1A${stage.atoms.map((a) => ATOM_LABELS[a]).join(" -> ")}`
    ).join("\n"));
    const finalCards = this.extractPointCards(finalOutput);
    this.renderPointCards(summaryCard, finalCards, {
      scope: "final",
      baseCardId: summaryId,
      titlePrefix: "\u4E32\u8054\u6700\u7EC8\u7ED3\u679C"
    });
    const rawDetails = summaryCard.createEl("details", { cls: "tl-step-details" });
    rawDetails.createEl("summary", { text: "\u67E5\u770B\u6700\u7EC8\u5B8C\u6574\u8F93\u51FA" });
    const rawEl = rawDetails.createDiv({ cls: "tl-result-content" });
    rawEl.innerHTML = this.simpleMarkdown(finalOutput);
  }
  shouldIgnoreCardSelectionTarget(target) {
    if (!target)
      return false;
    return !!target.closest("button, summary, details, a, input, textarea, select");
  }
  selectResultCard(cardEl, selection) {
    if (this.selectedResultCardEl && this.selectedResultCardEl !== cardEl) {
      this.selectedResultCardEl.removeClass("tl-result-card-selected");
    }
    this.selectedResultCardEl = cardEl;
    cardEl.addClass("tl-result-card-selected");
    void this.storeSelectionToProfile(selection);
  }
  async storeSelectionToProfile(selection) {
    var _a;
    if (this.selectionStoredInMemory.has(selection.cardId))
      return;
    const workflow = (_a = this.lastExecutionWorkflow) != null ? _a : this.getActiveWorkflowPreset();
    const content = [
      "[UserProfileSelection]",
      `workspace: ${this.activeWorkspace.name}`,
      `workflow: ${workflow.label}`,
      `scope: ${selection.scope}`,
      `card_title: ${selection.title}`,
      selection.atom ? `atom: ${selection.atom}` : "",
      typeof selection.stepIndex === "number" ? `step_index: ${selection.stepIndex}` : "",
      `summary: ${selection.summary}`,
      "selected_output:",
      selection.output.slice(0, 4e3)
    ].filter(Boolean).join("\n");
    try {
      await this.plugin.apiClient.storeMemory(content, `profile_selection_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`);
      this.selectionStoredInMemory.add(selection.cardId);
      new import_obsidian3.Notice(`\u5DF2\u5C06\u9009\u4E2D${selection.scope === "final" ? "\u6574\u4F53\u7ED3\u679C" : "\u6B65\u9AA4\u7ED3\u679C"}\u5199\u5165\u753B\u50CF\u8BB0\u5FC6`);
    } catch (e) {
      console.error("[LogicAtom] Failed to store profile selection:", e);
      new import_obsidian3.Notice("\u5199\u5165\u753B\u50CF\u8BB0\u5FC6\u5931\u8D25\uFF0C\u8BF7\u68C0\u67E5 EverMind \u8FDE\u63A5");
    }
  }
  extractPointCards(text) {
    const raw = (text || "").replace(/\r\n/g, "\n").trim();
    if (!raw)
      return [];
    const lines = raw.split("\n");
    const headingIndexes = lines.map((line, index) => ({ line: line.trim(), index })).filter((item) => /^#{2,4}\s+/.test(item.line)).map((item) => item.index);
    if (headingIndexes.length >= 2) {
      const cards = [];
      for (let i = 0; i < headingIndexes.length; i++) {
        const start = headingIndexes[i];
        const end = i + 1 < headingIndexes.length ? headingIndexes[i + 1] : lines.length;
        const header = lines[start].replace(/^#{2,4}\s+/, "").trim() || `\u8981\u70B9 ${i + 1}`;
        const body = lines.slice(start + 1, end).join("\n").trim() || "(\u7A7A)";
        cards.push({ title: header, body });
      }
      return cards;
    }
    const bulletRegex = /^(\s*[-*]\s+|\s*\d+\.\s+)/;
    const bulletCards = [];
    let currentLines = [];
    for (const line of lines) {
      if (bulletRegex.test(line)) {
        if (currentLines.length > 0) {
          const body = currentLines.join("\n").trim();
          bulletCards.push({
            title: body.replace(bulletRegex, "").slice(0, 24) || `\u8981\u70B9 ${bulletCards.length + 1}`,
            body
          });
        }
        currentLines = [line];
      } else if (currentLines.length > 0) {
        currentLines.push(line);
      }
    }
    if (currentLines.length > 0) {
      const body = currentLines.join("\n").trim();
      bulletCards.push({
        title: body.replace(bulletRegex, "").slice(0, 24) || `\u8981\u70B9 ${bulletCards.length + 1}`,
        body
      });
    }
    if (bulletCards.length >= 2)
      return bulletCards;
    const paragraphs = raw.split(/\n{2,}/).map((p) => p.trim()).filter(Boolean);
    if (paragraphs.length >= 2) {
      return paragraphs.map((body, index) => ({
        title: `\u8981\u70B9 ${index + 1}`,
        body
      }));
    }
    return [{ title: "\u6838\u5FC3\u7ED3\u679C", body: raw }];
  }
  renderPointCards(container, cards, options) {
    const listEl = container.createDiv({ cls: "tl-point-card-list" });
    cards.forEach((card, index) => {
      const cardEl = listEl.createDiv({ cls: "tl-point-card" });
      const headerEl = cardEl.createDiv({ cls: "tl-point-card-header" });
      headerEl.createSpan({
        text: `${index + 1}. ${card.title}`,
        cls: "tl-point-card-title"
      });
      const actionsEl = headerEl.createDiv({ cls: "tl-point-card-actions" });
      const copyBtn = actionsEl.createEl("button", { text: "\u590D\u5236", cls: "tl-btn tl-btn-small" });
      copyBtn.addEventListener("click", async () => {
        await navigator.clipboard.writeText(card.body);
        copyBtn.textContent = "\u5DF2\u590D\u5236";
        setTimeout(() => {
          copyBtn.textContent = "\u590D\u5236";
        }, 1500);
      });
      const insertBtn = actionsEl.createEl("button", { text: "\u63D2\u5165", cls: "tl-btn tl-btn-small" });
      insertBtn.addEventListener("click", () => {
        this.insertBlockToActiveNote(`${options.titlePrefix} - ${card.title}`, card.body);
      });
      const removeBtn = actionsEl.createEl("button", { text: "\u5220\u9664", cls: "tl-btn tl-btn-small" });
      removeBtn.addEventListener("click", () => {
        if (this.selectedResultCardEl === cardEl) {
          this.selectedResultCardEl = null;
        }
        cardEl.remove();
      });
      const bodyEl = cardEl.createDiv({ cls: "tl-point-card-body" });
      bodyEl.innerHTML = this.simpleMarkdown(card.body);
      cardEl.addEventListener("click", (evt) => {
        const target = evt.target;
        if (this.shouldIgnoreCardSelectionTarget(target))
          return;
        evt.stopPropagation();
        this.selectResultCard(cardEl, {
          cardId: `${options.baseCardId}-point-${index + 1}`,
          scope: options.scope,
          title: `${options.titlePrefix} - ${card.title}`,
          summary: card.title,
          output: card.body,
          stepIndex: options.stepIndex,
          atom: options.atom
        });
      });
    });
  }
  renderStep(step, index, total) {
    const stepId = `run-${this.runCounter}-step-${index}`;
    const stepEl = this.stepsEl.createDiv({ cls: "tl-step tl-step-card" });
    const headerEl = stepEl.createDiv({ cls: "tl-step-header" });
    const iconSpan = headerEl.createSpan({ cls: "tl-step-icon" });
    (0, import_obsidian3.setIcon)(iconSpan, ATOM_ICONS[step.atom]);
    headerEl.createSpan({
      text: `\u6B65\u9AA4 ${index}/${total}\uFF1A${ATOM_LABELS[step.atom]}`,
      cls: "tl-step-title"
    });
    headerEl.createSpan({
      text: `${(step.duration / 1e3).toFixed(1)}\u79D2`,
      cls: "tl-step-time"
    });
    const stepActionsEl = headerEl.createDiv({ cls: "tl-step-header-actions" });
    const copyStepBtn = stepActionsEl.createEl("button", {
      text: "\u590D\u5236\u672C\u6B65\u9AA4",
      cls: "tl-btn tl-btn-small"
    });
    copyStepBtn.addEventListener("click", async () => {
      await navigator.clipboard.writeText(step.output);
      copyStepBtn.textContent = "\u5DF2\u590D\u5236";
      setTimeout(() => {
        copyStepBtn.textContent = "\u590D\u5236\u672C\u6B65\u9AA4";
      }, 1800);
    });
    const insertStepBtn = stepActionsEl.createEl("button", {
      text: "\u63D2\u5165\u7B14\u8BB0",
      cls: "tl-btn tl-btn-small"
    });
    insertStepBtn.addEventListener("click", () => {
      this.insertBlockToActiveNote(
        `\u6B65\u9AA4 ${index}/${total}\uFF1A${ATOM_LABELS[step.atom]}`,
        step.output
      );
    });
    const removeStepCardBtn = stepActionsEl.createEl("button", {
      text: "\u5220\u9664\u5361\u7247",
      cls: "tl-btn tl-btn-small"
    });
    removeStepCardBtn.addEventListener("click", () => {
      if (this.selectedResultCardEl === stepEl) {
        this.selectedResultCardEl = null;
      }
      stepEl.remove();
    });
    if (step.memoriesStored.length > 0) {
      const deleteBtn = stepActionsEl.createEl("button", {
        cls: "tl-btn-delete tl-btn-delete-step",
        attr: { title: `\u5220\u9664\u6B64\u6B65\u9AA4\u7684 ${step.memoriesStored.length} \u6761\u8BB0\u5FC6` }
      });
      (0, import_obsidian3.setIcon)(deleteBtn, "x");
      deleteBtn.addEventListener("click", () => this.deleteStepMemories(step.memoriesStored, deleteBtn));
    }
    stepEl.createDiv({
      text: step.summary,
      cls: "tl-step-summary"
    });
    const statsEl = stepEl.createDiv({ cls: "tl-step-stats" });
    statsEl.createSpan({ text: `\u641C\u7D22\uFF1A${step.searchCount} \u6B21` });
    statsEl.createSpan({ text: `\u5B58\u50A8\uFF1A${step.storeCount} \u6B21` });
    statsEl.createSpan({ text: `\u8BB0\u5FC6\uFF1A${step.memoriesRetrieved.length} \u6761` });
    const pointCards = this.extractPointCards(step.output);
    this.renderPointCards(stepEl, pointCards, {
      scope: "step",
      baseCardId: stepId,
      titlePrefix: `\u6B65\u9AA4 ${index}/${total}\uFF1A${ATOM_LABELS[step.atom]}`,
      atom: step.atom,
      stepIndex: index
    });
    const rawOutputDetails = stepEl.createEl("details", { cls: "tl-step-details" });
    rawOutputDetails.createEl("summary", { text: "\u67E5\u770B\u5B8C\u6574\u6B65\u9AA4\u8F93\u51FA" });
    const outputEl = rawOutputDetails.createDiv({ cls: "tl-step-output" });
    outputEl.innerHTML = this.simpleMarkdown(step.output);
    stepEl.addEventListener("click", (evt) => {
      const target = evt.target;
      if (this.shouldIgnoreCardSelectionTarget(target))
        return;
      this.selectResultCard(stepEl, {
        cardId: stepId,
        scope: "step",
        title: `\u6B65\u9AA4 ${index}/${total}\uFF1A${ATOM_LABELS[step.atom]}`,
        summary: step.summary,
        output: step.output,
        stepIndex: index,
        atom: step.atom
      });
    });
  }
  renderFinalResult(result, workflow) {
    this.lastResult = result;
    this.statsEl.empty();
    this.statsEl.createSpan({
      text: `\u5DE5\u4F5C\u6D41\uFF1A${workflow.label} | \u641C\u7D22 ${result.totalSearches} \u6B21 | \u5B58\u50A8 ${result.totalStores} \u6B21 | \u8017\u65F6 ${(result.totalDuration / 1e3).toFixed(1)} \u79D2`
    });
    this.resultEl.empty();
    const finalId = `run-${this.runCounter}-final`;
    const resultCard = this.resultEl.createDiv({ cls: "tl-result-card" });
    const resultHeader = resultCard.createDiv({ cls: "tl-result-header" });
    const finalTitle = workflow.strategyMode === "general" ? "\u6700\u7EC8\u7EFC\u5408\u7ED3\u679C" : `${workflow.label}\u7ED3\u679C`;
    resultHeader.createEl("h4", { text: finalTitle });
    const copyBtn = resultHeader.createEl("button", { text: "\u590D\u5236", cls: "tl-btn tl-btn-small" });
    copyBtn.addEventListener("click", () => {
      navigator.clipboard.writeText(result.finalOutput);
      copyBtn.textContent = "\u5DF2\u590D\u5236";
      setTimeout(() => {
        copyBtn.textContent = "\u590D\u5236";
      }, 2e3);
    });
    const insertBtn = resultHeader.createEl("button", { text: "\u63D2\u5165\u7B14\u8BB0", cls: "tl-btn tl-btn-small" });
    insertBtn.addEventListener("click", () => this.insertToActiveNote(result, workflow));
    const removeFinalCardBtn = resultHeader.createEl("button", { text: "\u5220\u9664\u5361\u7247", cls: "tl-btn tl-btn-small" });
    removeFinalCardBtn.addEventListener("click", () => {
      if (this.selectedResultCardEl === resultCard) {
        this.selectedResultCardEl = null;
      }
      resultCard.remove();
    });
    const finalPointCards = this.extractPointCards(result.finalOutput);
    this.renderPointCards(resultCard, finalPointCards, {
      scope: "final",
      baseCardId: finalId,
      titlePrefix: finalTitle
    });
    const rawResultDetails = resultCard.createEl("details", { cls: "tl-step-details" });
    rawResultDetails.createEl("summary", { text: "\u67E5\u770B\u5B8C\u6574\u7ED3\u679C\u8F93\u51FA" });
    const outputEl = rawResultDetails.createDiv({ cls: "tl-result-content" });
    outputEl.innerHTML = this.simpleMarkdown(result.finalOutput);
    const allStored = result.steps.flatMap((s) => s.memoriesStored);
    if (allStored.length > 0) {
      const cleanupSection = resultCard.createDiv({ cls: "tl-cleanup-section" });
      const cleanupBtn = cleanupSection.createEl("button", {
        text: `\u6E05\u7406\u6240\u6709\u4E2D\u95F4\u8BB0\u5FC6\uFF08${allStored.length} \u6761\uFF09`,
        cls: "tl-btn tl-btn-delete-batch"
      });
      cleanupBtn.addEventListener("click", () => this.deleteAllMemories(allStored, cleanupBtn));
    }
    resultCard.addEventListener("click", (evt) => {
      const target = evt.target;
      if (this.shouldIgnoreCardSelectionTarget(target))
        return;
      this.selectResultCard(resultCard, {
        cardId: finalId,
        scope: "final",
        title: finalTitle,
        summary: `\u641C\u7D22 ${result.totalSearches} \u6B21 | \u5B58\u50A8 ${result.totalStores} \u6B21`,
        output: result.finalOutput
      });
    });
  }
  getActiveEditor() {
    var _a;
    let editor = (_a = this.app.workspace.activeEditor) == null ? void 0 : _a.editor;
    if (!editor) {
      const mdLeaf = this.app.workspace.getMostRecentLeaf();
      if ((mdLeaf == null ? void 0 : mdLeaf.view) instanceof import_obsidian3.MarkdownView) {
        editor = mdLeaf.view.editor;
      }
    }
    return editor;
  }
  insertBlockToActiveNote(title, body) {
    const editor = this.getActiveEditor();
    if (!editor) {
      new import_obsidian3.Notice("\u6CA1\u6709\u6253\u5F00\u7684\u7B14\u8BB0\uFF0C\u65E0\u6CD5\u63D2\u5165");
      return;
    }
    const content = `

### ${title}
${body}
`;
    const cursor = editor.getCursor();
    editor.replaceRange(content, cursor);
    new import_obsidian3.Notice("\u5DF2\u63D2\u5165\u7B14\u8BB0");
  }
  insertToActiveNote(result, workflow) {
    const editor = this.getActiveEditor();
    if (!editor) {
      new import_obsidian3.Notice("\u6CA1\u6709\u6253\u5F00\u7684\u7B14\u8BB0\uFF0C\u65E0\u6CD5\u63D2\u5165");
      return;
    }
    const atomLabels = result.atomNames.map((a) => ATOM_LABELS[a]).join(" -> ");
    const content = `

---
## \u903B\u8F91\u539F\u5B50\u6267\u884C\u7ED3\u679C
**\u5DE5\u4F5C\u533A\uFF1A** ${this.activeWorkspace.name}
**\u5DE5\u4F5C\u6D41\uFF1A** ${workflow.label}
**\u95EE\u9898/\u6750\u6599\uFF1A** ${result.input}
**\u539F\u5B50\u94FE\uFF1A** ${atomLabels}
**\u7EDF\u8BA1\uFF1A** \u641C\u7D22 ${result.totalSearches} \u6B21\uFF0C\u5B58\u50A8 ${result.totalStores} \u6B21

${result.finalOutput}
---
`;
    const cursor = editor.getCursor();
    editor.replaceRange(content, cursor);
    new import_obsidian3.Notice("\u5DF2\u63D2\u5165\u7B14\u8BB0");
  }
  async deleteStepMemories(memories, btn) {
    if (!confirm(`\u786E\u8BA4\u5220\u9664\u6B64\u6B65\u9AA4\u7684 ${memories.length} \u6761\u8BB0\u5FC6\u5417\uFF1F`))
      return;
    btn.disabled = true;
    btn.addClass("tl-btn-delete-disabled");
    let deleted = 0;
    for (const mem of memories) {
      try {
        await this.plugin.apiClient.deleteMemory(mem.eventId);
        deleted++;
      } catch (e) {
        console.error("[LogicAtom] Failed to delete memory:", mem.eventId, e);
      }
    }
    btn.textContent = "\u5DF2\u5220\u9664";
    btn.addClass("tl-btn-deleted");
    new import_obsidian3.Notice(`\u5DF2\u5220\u9664 ${deleted}/${memories.length} \u6761\u8BB0\u5FC6`);
  }
  async deleteAllMemories(memories, btn) {
    if (!confirm(`\u786E\u8BA4\u5220\u9664\u672C\u6B21\u6267\u884C\u4EA7\u751F\u7684\u5168\u90E8 ${memories.length} \u6761\u4E2D\u95F4\u8BB0\u5FC6\u5417\uFF1F`))
      return;
    btn.disabled = true;
    btn.textContent = "\u5220\u9664\u4E2D...";
    let deleted = 0;
    for (const mem of memories) {
      try {
        await this.plugin.apiClient.deleteMemory(mem.eventId);
        deleted++;
      } catch (e) {
        console.error("[LogicAtom] Failed to delete memory:", mem.eventId, e);
      }
    }
    btn.textContent = `\u5DF2\u6E05\u7406 ${deleted} \u6761`;
    btn.addClass("tl-btn-deleted");
    this.stepsEl.querySelectorAll(".tl-btn-delete-step").forEach((el) => {
      el.disabled = true;
      el.addClass("tl-btn-deleted");
    });
    new import_obsidian3.Notice(`\u5DF2\u6E05\u7406 ${deleted}/${memories.length} \u6761\u4E2D\u95F4\u8BB0\u5FC6`);
  }
  addStep(type, text) {
    const cls = type === "error" ? "tl-step tl-step-error" : "tl-step tl-step-system";
    this.stepsEl.createDiv({ text, cls });
  }
  clearResults() {
    this.stepsEl.empty();
    this.resultEl.empty();
    this.statsEl.empty();
    this.selectedResultCardEl = null;
    this.selectionStoredInMemory.clear();
  }
  setRunning(running) {
    this.isRunning = running;
    this.autoBtn.disabled = running;
    this.executeBtn.disabled = running;
    this.autoBtn.textContent = running ? "\u6267\u884C\u4E2D..." : "\u4E00\u952E\u6267\u884C\u5DE5\u4F5C\u533A\u6D41\u7A0B";
  }
  simpleMarkdown(text) {
    return text.replace(/### (.+)/g, "<h4>$1</h4>").replace(/\*\*(.+?)\*\*/g, "<strong>$1</strong>").replace(/\n/g, "<br>");
  }
  async onClose() {
  }
};

// src/views/memory-sidebar.ts
var import_obsidian4 = require("obsidian");
var MEMORY_SIDEBAR_VIEW = "three-library-memory-sidebar";
var MemorySidebarView = class extends import_obsidian4.ItemView {
  constructor(leaf, plugin) {
    super(leaf);
    this.plugin = plugin;
  }
  getViewType() {
    return MEMORY_SIDEBAR_VIEW;
  }
  getDisplayText() {
    return "\u8BB0\u5FC6\u6D4F\u89C8\u5668";
  }
  getIcon() {
    return "database";
  }
  async onOpen() {
    const container = this.contentEl;
    container.empty();
    container.addClass("three-library-sidebar");
    container.createEl("h4", { text: "\u8BB0\u5FC6\u6D4F\u89C8\u5668", cls: "tl-sidebar-title" });
    const searchBox = container.createDiv({ cls: "tl-search-box" });
    this.searchInputEl = searchBox.createEl("input", {
      cls: "tl-search-input",
      attr: { placeholder: "\u641C\u7D22\u8BB0\u5FC6..." }
    });
    const searchBtn = searchBox.createEl("button", { cls: "tl-btn tl-btn-small" });
    (0, import_obsidian4.setIcon)(searchBtn, "search");
    searchBtn.addEventListener("click", () => this.doSearch());
    this.searchInputEl.addEventListener("keydown", (e) => {
      if (e.key === "Enter")
        this.doSearch();
    });
    this.statusEl = container.createDiv({ cls: "tl-sidebar-status" });
    this.resultsEl = container.createDiv({ cls: "tl-sidebar-results" });
  }
  async doSearch() {
    const query = this.searchInputEl.value.trim();
    if (!query)
      return;
    this.statusEl.textContent = "\u641C\u7D22\u4E2D...";
    this.resultsEl.empty();
    try {
      const results = await this.plugin.apiClient.searchMemories(query, this.plugin.settings.maxSearchResults);
      this.statusEl.textContent = `\u627E\u5230 ${results.length} \u6761\u8BB0\u5FC6`;
      this.renderResults(results);
    } catch (e) {
      this.statusEl.textContent = `\u9519\u8BEF\uFF1A${e instanceof Error ? e.message : String(e)}`;
    }
  }
  extractEventId(raw) {
    const data = raw;
    if (data.event_id && typeof data.event_id === "string")
      return data.event_id;
    if (data.id && typeof data.id === "string")
      return data.id;
    if (data.memcell_id && typeof data.memcell_id === "string")
      return data.memcell_id;
    if (Array.isArray(data.memcell_event_id_list) && data.memcell_event_id_list.length > 0) {
      return String(data.memcell_event_id_list[0]);
    }
    return null;
  }
  async deleteMemory(eventId, card) {
    if (!confirm("\u786E\u8BA4\u5220\u9664\u6B64\u6761\u8BB0\u5FC6\uFF1F"))
      return;
    try {
      await this.plugin.apiClient.deleteMemory(eventId);
      card.addClass("tl-memory-card-deleted");
      card.empty();
      card.createDiv({ text: "\u5DF2\u5220\u9664", cls: "tl-deleted-label" });
      new import_obsidian4.Notice("\u8BB0\u5FC6\u5DF2\u5220\u9664");
      setTimeout(() => this.doSearch(), 500);
    } catch (e) {
      new import_obsidian4.Notice(`\u5220\u9664\u5931\u8D25\uFF1A${e instanceof Error ? e.message : String(e)}`);
    }
  }
  renderResults(results) {
    if (results.length === 0) {
      this.resultsEl.createDiv({ text: "\u6CA1\u6709\u627E\u5230\u76F8\u5173\u8BB0\u5FC6\u3002", cls: "tl-sidebar-empty" });
      return;
    }
    for (const mem of results) {
      const card = this.resultsEl.createDiv({ cls: "tl-memory-card" });
      const headerEl = card.createDiv({ cls: "tl-memory-header" });
      const typeIcon = headerEl.createSpan({ cls: "tl-memory-type" });
      (0, import_obsidian4.setIcon)(typeIcon, mem.type === "memory" ? "brain" : "user");
      headerEl.createSpan({ text: mem.title, cls: "tl-memory-title" });
      const eventId = this.extractEventId(mem.raw);
      if (eventId) {
        const deleteBtn = headerEl.createEl("button", {
          cls: "tl-btn-delete tl-btn-delete-card",
          attr: { title: "\u5220\u9664\u6B64\u8BB0\u5FC6" }
        });
        (0, import_obsidian4.setIcon)(deleteBtn, "x");
        deleteBtn.addEventListener("click", (e) => {
          e.stopPropagation();
          this.deleteMemory(eventId, card);
        });
      }
      if (mem.content) {
        card.createDiv({
          text: mem.content.length > 200 ? mem.content.slice(0, 200) + "..." : mem.content,
          cls: "tl-memory-content"
        });
      }
      const metaEl = card.createDiv({ cls: "tl-memory-meta" });
      if (mem.score) {
        metaEl.createSpan({ text: `\u76F8\u5173\u5EA6\uFF1A${(mem.score * 100).toFixed(0)}%` });
      }
      if (mem.memoryType) {
        metaEl.createSpan({ text: mem.memoryType, cls: "tl-tag" });
      }
      if (mem.createdAt) {
        metaEl.createSpan({ text: mem.createdAt.substring(0, 10) });
      }
    }
  }
  async onClose() {
  }
};

// src/views/search-modal.ts
var import_obsidian5 = require("obsidian");
var SearchMemoryModal = class extends import_obsidian5.Modal {
  constructor(app, plugin) {
    super(app);
    this.query = "";
    this.plugin = plugin;
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.empty();
    contentEl.addClass("tl-search-modal");
    contentEl.createEl("h2", { text: "\u641C\u7D22\u8BB0\u5FC6" });
    new import_obsidian5.Setting(contentEl).setName("\u641C\u7D22").addText(
      (text) => text.setPlaceholder("\u4F60\u60F3\u627E\u4EC0\u4E48\uFF1F").onChange((value) => {
        this.query = value;
      })
    ).addButton(
      (btn) => btn.setButtonText("\u641C\u7D22").setCta().onClick(() => this.doSearch())
    );
    this.resultsEl = contentEl.createDiv({ cls: "tl-modal-results" });
  }
  async doSearch() {
    if (!this.query.trim())
      return;
    this.resultsEl.empty();
    this.resultsEl.createDiv({ text: "\u641C\u7D22\u4E2D...", cls: "tl-modal-status" });
    try {
      const results = await this.plugin.apiClient.searchMemories(
        this.query,
        this.plugin.settings.maxSearchResults
      );
      this.resultsEl.empty();
      if (results.length === 0) {
        this.resultsEl.createDiv({ text: "\u6CA1\u6709\u627E\u5230\u76F8\u5173\u8BB0\u5FC6\u3002", cls: "tl-modal-status" });
        return;
      }
      for (const mem of results) {
        const item = this.resultsEl.createDiv({ cls: "tl-modal-item" });
        item.createDiv({ text: mem.title, cls: "tl-modal-item-title" });
        item.createDiv({
          text: mem.content.length > 150 ? mem.content.slice(0, 150) + "..." : mem.content,
          cls: "tl-modal-item-content"
        });
        item.addEventListener("click", () => {
          this.insertMemory(mem);
          this.close();
        });
      }
    } catch (e) {
      this.resultsEl.empty();
      this.resultsEl.createDiv({
        text: `\u9519\u8BEF\uFF1A${e instanceof Error ? e.message : String(e)}`,
        cls: "tl-modal-status tl-error"
      });
    }
  }
  insertMemory(mem) {
    var _a;
    const editor = (_a = this.app.workspace.activeEditor) == null ? void 0 : _a.editor;
    if (!editor)
      return;
    const content = `> **\u8BB0\u5FC6\uFF1A** ${mem.title}
> ${mem.content}
> *\u76F8\u5173\u5EA6\uFF1A${(mem.score * 100).toFixed(0)}%*

`;
    const cursor = editor.getCursor();
    editor.replaceRange(content, cursor);
  }
  onClose() {
    this.contentEl.empty();
  }
};

// src/main.ts
var ThreeLibraryPlugin = class extends import_obsidian6.Plugin {
  constructor() {
    super(...arguments);
    this.lastHealthCheck = null;
  }
  async onload() {
    console.log("[LogicAtom] Plugin loading...");
    await this.loadSettings();
    console.log("[LogicAtom] Settings loaded:", { apiUrl: this.settings.apiUrl, hasKey: !!this.settings.apiKey });
    this.apiClient = new EverMemOSClient(
      this.settings.apiUrl,
      this.settings.apiKey,
      this.settings.userId,
      this.settings.userName,
      this.settings.groupId,
      this.settings.groupName,
      this.settings.defaultRetrieveMethod
    );
    this.llmClient = new LLMClient(
      this.settings.openRouterApiKey,
      this.settings.llmModel,
      this.settings.llmBaseUrl
    );
    this.atomEngine = new AtomEngine(this.apiClient, this.llmClient);
    this.atomSelector = new AtomSelector(this.llmClient);
    this.atomEngine.registerAtom(new DecomposeAtom(this.apiClient, this.llmClient));
    this.atomEngine.registerAtom(new AssociateAtom(this.apiClient, this.llmClient));
    this.atomEngine.registerAtom(new TransformAtom(this.apiClient, this.llmClient));
    this.atomEngine.registerAtom(new AbstractAtom(this.apiClient, this.llmClient));
    this.atomEngine.registerAtom(new EvaluateAtom(this.apiClient, this.llmClient));
    this.atomEngine.registerAtom(new IterateAtom(this.apiClient, this.llmClient));
    this.registerView(ATOM_PANEL_VIEW, (leaf) => new AtomPanelView(leaf, this));
    this.registerView(MEMORY_SIDEBAR_VIEW, (leaf) => new MemorySidebarView(leaf, this));
    this.addRibbonIcon("brain", "\u6253\u5F00\u903B\u8F91\u539F\u5B50\u9762\u677F", () => {
      this.activateAtomPanel();
    });
    this.addCommand({
      id: "open-atom-panel",
      name: "\u6253\u5F00\u903B\u8F91\u539F\u5B50\u9762\u677F",
      callback: () => this.activateAtomPanel()
    });
    this.addCommand({
      id: "open-memory-sidebar",
      name: "\u6253\u5F00\u8BB0\u5FC6\u4FA7\u8FB9\u680F",
      callback: () => this.activateMemorySidebar()
    });
    this.addCommand({
      id: "search-memories",
      name: "\u641C\u7D22\u8BB0\u5FC6",
      callback: () => new SearchMemoryModal(this.app, this).open()
    });
    this.addCommand({
      id: "run-on-selection",
      name: "\u5BF9\u9009\u4E2D\u6587\u672C\u6267\u884C\u903B\u8F91\u539F\u5B50\u6D41\u7A0B",
      editorCallback: async (editor) => {
        const selection = editor.getSelection();
        if (!selection) {
          new import_obsidian6.Notice("\u6CA1\u6709\u9009\u4E2D\u6587\u672C");
          return;
        }
        await this.activateAtomPanel();
        const leaves = this.app.workspace.getLeavesOfType(ATOM_PANEL_VIEW);
        if (leaves.length > 0) {
          const view = leaves[0].view;
          view.setInput(selection);
        }
      }
    });
    this.addCommand({
      id: "run-on-note",
      name: "\u5BF9\u5F53\u524D\u7B14\u8BB0\u6267\u884C\u903B\u8F91\u539F\u5B50\u6D41\u7A0B",
      editorCallback: async (editor) => {
        const content = editor.getValue();
        if (!content) {
          new import_obsidian6.Notice("\u7B14\u8BB0\u5185\u5BB9\u4E3A\u7A7A");
          return;
        }
        await this.activateAtomPanel();
        const leaves = this.app.workspace.getLeavesOfType(ATOM_PANEL_VIEW);
        if (leaves.length > 0) {
          const view = leaves[0].view;
          view.setInput(content);
        }
      }
    });
    this.addCommand({
      id: "store-selection-to-memory",
      name: "\u5C06\u9009\u4E2D\u6587\u672C\u5B58\u5165\u8BB0\u5FC6",
      editorCallback: async (editor) => {
        const selection = editor.getSelection();
        if (!selection) {
          new import_obsidian6.Notice("\u6CA1\u6709\u9009\u4E2D\u6587\u672C");
          return;
        }
        try {
          await this.apiClient.storeMemory(selection);
          new import_obsidian6.Notice("\u5DF2\u5B58\u5165\u8BB0\u5FC6");
        } catch (e) {
          new import_obsidian6.Notice(`\u5B58\u50A8\u5931\u8D25\uFF1A${e instanceof Error ? e.message : String(e)}`);
        }
      }
    });
    this.addSettingTab(new ThreeLibrarySettingTab(this.app, this));
  }
  async onunload() {
  }
  async loadSettings() {
    this.settings = Object.assign({}, DEFAULT_SETTINGS, await this.loadData());
    const fallbackProfiles = getDefaultWorkspaceProfiles();
    const fallbackKnowledgeBarrier = fallbackProfiles.find((p) => p.id === "knowledge-barrier");
    const knownModes = [
      "general",
      "reading-understand",
      "reading-connect",
      "reading-apply",
      "reading-advanced"
    ];
    const knownModeSet = new Set(knownModes);
    const knownInputBindings = /* @__PURE__ */ new Set([
      "original",
      "previous",
      "original+previous"
    ]);
    const atomNameSet = /* @__PURE__ */ new Set(["decompose", "associate", "transform", "abstract", "evaluate", "iterate"]);
    const isWorkflowMode = (value) => typeof value === "string" && knownModeSet.has(value);
    const isWorkflowInputBinding = (value) => typeof value === "string" && knownInputBindings.has(value);
    const sanitizeAtoms = (atoms) => {
      if (atoms === null)
        return null;
      if (!Array.isArray(atoms))
        return void 0;
      const filtered = atoms.filter((atom) => typeof atom === "string" && atomNameSet.has(atom));
      return filtered.length > 0 ? filtered : [];
    };
    const normalizeWorkflow = (rawWorkflow) => {
      const fallbackMode = isWorkflowMode(rawWorkflow == null ? void 0 : rawWorkflow.strategyMode) ? rawWorkflow.strategyMode : "general";
      const fallback = getBuiltInWorkflowPreset(fallbackMode);
      const atoms = sanitizeAtoms(rawWorkflow == null ? void 0 : rawWorkflow.atoms);
      const inputBinding = isWorkflowInputBinding(rawWorkflow == null ? void 0 : rawWorkflow.inputBinding) ? rawWorkflow.inputBinding : fallback.inputBinding || "previous";
      return {
        ...fallback,
        id: typeof (rawWorkflow == null ? void 0 : rawWorkflow.id) === "string" && rawWorkflow.id.trim().length > 0 ? rawWorkflow.id.trim() : fallback.id,
        strategyMode: fallbackMode,
        label: typeof (rawWorkflow == null ? void 0 : rawWorkflow.label) === "string" && rawWorkflow.label.trim().length > 0 ? rawWorkflow.label.trim() : fallback.label,
        shortLabel: typeof (rawWorkflow == null ? void 0 : rawWorkflow.shortLabel) === "string" && rawWorkflow.shortLabel.trim().length > 0 ? rawWorkflow.shortLabel.trim() : fallback.shortLabel,
        description: typeof (rawWorkflow == null ? void 0 : rawWorkflow.description) === "string" ? rawWorkflow.description : fallback.description,
        actionLabel: typeof (rawWorkflow == null ? void 0 : rawWorkflow.actionLabel) === "string" && rawWorkflow.actionLabel.trim().length > 0 ? rawWorkflow.actionLabel.trim() : fallback.actionLabel,
        inputPlaceholder: typeof (rawWorkflow == null ? void 0 : rawWorkflow.inputPlaceholder) === "string" && rawWorkflow.inputPlaceholder.trim().length > 0 ? rawWorkflow.inputPlaceholder : fallback.inputPlaceholder,
        atoms: atoms !== void 0 ? atoms : fallback.atoms ? [...fallback.atoms] : null,
        objective: typeof (rawWorkflow == null ? void 0 : rawWorkflow.objective) === "string" ? rawWorkflow.objective : fallback.objective,
        synthesisGuidance: typeof (rawWorkflow == null ? void 0 : rawWorkflow.synthesisGuidance) === "string" ? rawWorkflow.synthesisGuidance : fallback.synthesisGuidance,
        inputBinding,
        stageContract: typeof (rawWorkflow == null ? void 0 : rawWorkflow.stageContract) === "string" ? rawWorkflow.stageContract : fallback.stageContract
      };
    };
    const migrateLegacyWorkflows = (rawProfile) => {
      const legacyModes = Array.isArray(rawProfile == null ? void 0 : rawProfile.enabledModes) && rawProfile.enabledModes.length > 0 ? rawProfile.enabledModes.filter((mode) => isWorkflowMode(mode)) : ["general"];
      const legacyAtomsByMode = (rawProfile == null ? void 0 : rawProfile.atomsByMode) && typeof rawProfile.atomsByMode === "object" ? rawProfile.atomsByMode : {};
      return legacyModes.map((mode) => {
        const legacyAtoms = sanitizeAtoms(legacyAtomsByMode[mode]);
        const built = buildWorkspaceWorkflowFromMode(mode, {
          atoms: legacyAtoms !== void 0 ? legacyAtoms : void 0
        });
        return cloneWorkflowPreset(built);
      });
    };
    if (!Array.isArray(this.settings.workspaceProfiles) || this.settings.workspaceProfiles.length === 0) {
      this.settings.workspaceProfiles = fallbackProfiles;
    } else {
      this.settings.workspaceProfiles = this.settings.workspaceProfiles.map((rawProfile, index) => {
        const workflows = Array.isArray(rawProfile == null ? void 0 : rawProfile.workflows) && rawProfile.workflows.length > 0 ? rawProfile.workflows.map((wf) => normalizeWorkflow(wf)) : migrateLegacyWorkflows(rawProfile);
        let dedupedWorkflows = [];
        const workflowIdSet = /* @__PURE__ */ new Set();
        for (const workflow of workflows) {
          let workflowId = workflow.id;
          if (workflowIdSet.has(workflowId)) {
            workflowId = `${workflowId}-${workflow.strategyMode}-${dedupedWorkflows.length + 1}`;
          }
          workflowIdSet.add(workflowId);
          dedupedWorkflows.push({
            ...workflow,
            id: workflowId
          });
        }
        const profileId = typeof (rawProfile == null ? void 0 : rawProfile.id) === "string" ? rawProfile.id : "";
        const hasKnowledgeBarrierMethodIds = dedupedWorkflows.some((wf) => wf.id.startsWith("kb-"));
        const isLegacyKnowledgeBarrierChain = dedupedWorkflows.length > 0 && dedupedWorkflows.every(
          (wf) => ["general", "reading-understand", "reading-connect", "reading-apply", "reading-advanced"].includes(wf.id)
        );
        if (profileId === "knowledge-barrier" && !hasKnowledgeBarrierMethodIds && isLegacyKnowledgeBarrierChain && fallbackKnowledgeBarrier) {
          dedupedWorkflows = fallbackKnowledgeBarrier.workflows.map((wf) => cloneWorkflowPreset(wf));
        }
        if (dedupedWorkflows.length === 0) {
          dedupedWorkflows.push(getBuiltInWorkflowPreset("general"));
        }
        const candidateDefaultWorkflowId = typeof (rawProfile == null ? void 0 : rawProfile.defaultWorkflowId) === "string" && rawProfile.defaultWorkflowId.trim().length > 0 ? rawProfile.defaultWorkflowId.trim() : typeof (rawProfile == null ? void 0 : rawProfile.defaultMode) === "string" ? rawProfile.defaultMode : "";
        const defaultWorkflowId = dedupedWorkflows.some((wf) => wf.id === candidateDefaultWorkflowId) ? candidateDefaultWorkflowId : dedupedWorkflows[0].id;
        return {
          id: (rawProfile == null ? void 0 : rawProfile.id) || `workspace_${Date.now()}_${index}`,
          name: (rawProfile == null ? void 0 : rawProfile.name) || "\u672A\u547D\u540D\u5DE5\u4F5C\u533A",
          description: (rawProfile == null ? void 0 : rawProfile.description) || "",
          panelTitle: (rawProfile == null ? void 0 : rawProfile.panelTitle) || (rawProfile == null ? void 0 : rawProfile.name) || "\u903B\u8F91\u539F\u5B50\u9762\u677F",
          methodObjective: (rawProfile == null ? void 0 : rawProfile.methodObjective) || "",
          outputStyleGuide: (rawProfile == null ? void 0 : rawProfile.outputStyleGuide) || "",
          workflows: dedupedWorkflows,
          defaultWorkflowId
        };
      });
    }
    const workspaceIds = new Set(this.settings.workspaceProfiles.map((w) => w.id));
    if (!this.settings.activeWorkspaceId || !workspaceIds.has(this.settings.activeWorkspaceId)) {
      this.settings.activeWorkspaceId = this.settings.workspaceProfiles[0].id;
    }
  }
  async saveSettings() {
    var _a, _b;
    await this.saveData(this.settings);
    this.lastHealthCheck = null;
    (_a = this.apiClient) == null ? void 0 : _a.updateConfig(
      this.settings.apiUrl,
      this.settings.apiKey,
      this.settings.userId,
      this.settings.userName,
      this.settings.groupId,
      this.settings.groupName,
      this.settings.defaultRetrieveMethod
    );
    (_b = this.llmClient) == null ? void 0 : _b.updateConfig(
      this.settings.openRouterApiKey,
      this.settings.llmModel,
      this.settings.llmBaseUrl
    );
  }
  async testApiConnection() {
    return this.apiClient.testConnection();
  }
  async testLlmConnection() {
    return this.llmClient.testConnection();
  }
  async ensureRuntimeReady(force = false) {
    const now = Date.now();
    if (!force && this.lastHealthCheck && now - this.lastHealthCheck.at < 90 * 1e3) {
      return { ok: this.lastHealthCheck.ok, message: this.lastHealthCheck.message };
    }
    const [api, llm] = await Promise.all([
      this.testApiConnection(),
      this.testLlmConnection()
    ]);
    const ok = api.ok && llm.ok;
    const message = ok ? `\u8FDE\u63A5\u68C0\u67E5\u901A\u8FC7\uFF1A${api.message}\uFF1B${llm.message}` : `\u8FDE\u63A5\u68C0\u67E5\u5931\u8D25\uFF1A${api.message}\uFF1B${llm.message}`;
    this.lastHealthCheck = { at: now, ok, message };
    return { ok, message };
  }
  async activateAtomPanel() {
    const existing = this.app.workspace.getLeavesOfType(ATOM_PANEL_VIEW);
    if (existing.length > 0) {
      this.app.workspace.revealLeaf(existing[0]);
      return;
    }
    const leaf = this.app.workspace.getRightLeaf(false);
    if (leaf) {
      await leaf.setViewState({ type: ATOM_PANEL_VIEW, active: true });
      this.app.workspace.revealLeaf(leaf);
    }
  }
  async activateMemorySidebar() {
    const existing = this.app.workspace.getLeavesOfType(MEMORY_SIDEBAR_VIEW);
    if (existing.length > 0) {
      this.app.workspace.revealLeaf(existing[0]);
      return;
    }
    const leaf = this.app.workspace.getLeftLeaf(false);
    if (leaf) {
      await leaf.setViewState({ type: MEMORY_SIDEBAR_VIEW, active: true });
      this.app.workspace.revealLeaf(leaf);
    }
  }
};
